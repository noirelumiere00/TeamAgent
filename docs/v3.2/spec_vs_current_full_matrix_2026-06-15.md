# 元仕様書 × 現状実装 フル判定マトリクス（317行）

**生成日**: 2026-06-15 ｜ **突合元**: docs/v3.0 要件 / v3.1 MVA spec / v3.2 実装計画＋master_todo / docs/next 次期要件 ＋ 6/5 旧比較doc
**突合方法**: 各仕様書から計画スコープを構造化抽出 → 領域別に repo/git/移行計画書で実装到達点を検証（Agentワークフロー20体・391ツール実行）

## 集計

| 判定 | 件数 | 区分 |
|---|---:|---|
| ✅ 一致 | 53 | 達成 |
| 🟢 前倒し | 17 | 達成 |
| 🟢 強化 | 17 | 達成 |
| 🔄 方式変更 | 48 | 進行中 |
| 🟡 部分 | 103 | 進行中 |
| 🔴 未実装 | 76 | 未着手 |
| ⚪ 不要化 | 3 | 不要化 |
| **合計** | **317** | 達成87 / 進行中151 / 未着手76 / 不要化3 |

## エグゼクティブ結論

> 元仕様書(計画v2.2「Lambda×4+Fargate×1のサーバーレス疎結合・5機能」と後続のv3.0要件/MVA仕様/next要件)に対し、313の要件行を突合した結果、「方式変更」を含めれば実装は計画意図を概ね達成しつつあるが、計画した『形』そのままで残っている領域は少ない。最大の構造転換は実行基盤で、Lambda/SQS/API Gateway/RDS Proxyのサーバーレス疎結合は全面不採用となり、OpenClaw(Fargate)外殻+MCP金庫(streamable-http)+EC2 worker構成へピボットし、2026-06-12に本番E2E実返信(@AiLa→3.3秒/スレッド返信/isError=false)でgo-liveまで到達した。コア価値(横断RAG検索)はCohere Rerank+Contextual Retrieval+RLS+per-user OAuthで計画を上回る強化(top-1 20%→64%実証)で本番稼働、セキュリティ(IAM3分離/KMS/RLS強制/gitleaks-bandit CIゲート/敵対ハーネスでインジェクション拒否実証)も計画前倒しで厚い。一方、next要件のリファクタ計画(CapabilityManifest単一登録点・CapabilityId StrEnum・import-linter・intent.py 366行解体・7ドメイン再編・L2自律オーケストレーション本配線)はOpenClaw移行優先でほぼ全面未着手であり、週次/月次サマリー・3層メモリ(Redis短期/project中期/user長期)・提案書5フェーズ自律生成・ゲート②工数実証(20h→8-12h)・Personal AI Coachは未実装のまま残る。判定内訳は一致/前倒し68・強化18(合わせて達成27%)、方式変更47・部分101(進行中47%)、未実装76(24%)、削除/不要化3で、「半分が達成または進行中、4分の1が計画通り完了、4分の1が未着手」という到達度。誇張なく言えば、MVAの心臓部(横断検索E2E)は本番で動いているが、要件書全体の整理リファクタと自律オーケストレーション/週次サマリー/提案書自動生成といった『野心的な拡張部』は設計段階に留まる。

**タイムライン評価**: 元計画では Sprint14=2026-12-28 を本番リリース(営業16名展開)目標としていた。現在地(2026-06-15)は、本来Sprint8-14相当の中核(OpenClaw本番go-live・本番RDS接続E2E・Bedrock経由Sonnet4.6/Haiku4.5二段運用・セキュリティ監査報告書・CIゲート)を約半年前倒しで達成しており、機能E2Eとしては大幅に先行している。ただしこれは『パイロット前段階』であり、計画のSprintマイルストーン(S5末振り返り/S6全社展開承認/S13負荷試験/S14 SLO文書化・Go-No-Go②)はスケジュール未到来かつ未着手。具体的な残ブロッカーは、露出Slackトークン3本のローテーション・2人混線テスト・1週間パイロット(16名実利用)・16名allowlist拡張・p95レイテンシ目標未達(検索20.2s/目標15s・prompt caching未効)・Multi-AZ昇格(P2)・ゲート②工数実証。総括すると『機能の到達は前倒し、本番運用・全社展開・性能チューニング・組織承認は計画線上かやや手前』で、12月本番目標に対しては機能面で余裕がある一方、運用成熟度(SLO/障害訓練/オンコール/負荷試験50並列)は依然Sprint13-14スコープに残る。順当〜やや前倒し。

---

## 2026-06-16 更新: 既達🟡の✅化サマリ（13エージェント再判定・Batch A）

2026-06-15 のマトリクス以降、当セッション（dev HEAD）で前進し **🟡→✅** に確定した項目。詳細は各ドメイン表の該当行（残差分に *2026-06-16 確認* を併記）。

| ドメイン#行 | 項目 | ✅根拠（現HEAD） |
|---|---|---|
| 検索-RAG #26 | recall@10 DoD 指標 | `scripts/run_eval.py` で top1/top5/MRR/precision@5 を 50ケースで計測済（指標は計測体制として充足） |
| ingest #15/#21/#31 | Word/PPT/Excel 本文抽出 | Wave2-④ `ingest/office_extract.py`(docx/pptx/xlsx+gdoc)・pipeline 配線・worker 配信済（commit 5477341・Markdown/OCR は MUST 外） |
| メール #17 | トーン分析+ラベル自動分類 | AiLa `llm.py` トーン判定 + `gmail_client` `label_thread`/`modify_message_labels` 実装 |
| メール #8 | 営業 Drive フォルダ構造調査 | 実 ingest（登録フォルダ+shared_drives_crawl）で取込実績あり＝構造把握済 |
| スケジューラ #274 | OpenClaw heartbeat/プロアクティブ | `openclaw.config.json5` に heartbeat 配線済（P1 は意図的に `every:"0m"`） |
| 監視 #11 | DataDog APM | 計画通り非導入で OK＝CloudWatch + Sentry + `scripts/pilot_health.py` で代替 |
| 監視 #22 | pgvector/Embedding 個別レイテンシ | `bedrock_converse`/`embedder_embed` を CloudWatch Logs に構造化記録（pilot_health で p95 算出） |
| 提案 #5 | proposal_review | factory に配線・toolFilter 登録済（本番 MCP 公開） |
| その他 #16 | operation_log の MCP 公開 | commit 76327b1（factory `USE_OPERATION_LOG_TOOLS` gate + openclaw include・mcp:6 反映） |

**🟡 件数の見直し**: 103 → 上記✅化（約13件）＋ Batch B/C/D で順次クローズ。残る🟡は Batch E（人手/組織）/F（P0-P5大型）/G（低ROI後送り）に区分（→ `~/.claude/plans/agent-snuggly-pike.md` の実行フェーズ計画）。台帳: `decision_register.md`（12論点）・`ops/risk_register.md`（R16-19）。

---

## 実行基盤・アーキ（39項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | Lambda×4＋Fargate×1のサーバーレス疎結合。SQS fan-out/API Gateway/RDS Proxy前提のC4論理トポロジー | サーバーレスLambda構成は不採用。実体はECS Fargate×2サービス（OpenClaw外殻＋teamagent-mcp境界）＋EC2 worker 1台（ingest/Chrome/scrape用）＋bastion。Lambda関数はlambda_iam.tfで全コメントアウト（active 0）。6/12本番E2E go-live済 | 🔄 方式変更 | infra/terraform/lambda_iam.tf:108 `# resource "aws_lambda_function" "agent"`（コメントアウト）／fargate.tf:428,446 aws_ecs_service mcp/openclaw／worker.tf:129 aws_instance worker／mossy-snacking-locket.md §S3 go-live 3.3秒 | 元のサーバーレス計画とは別アーキ。Lambdaは器（IAM/S3）のみ残置で関数本体なし |
| 2 | 4本のLambdaに機能を物理分割 | Lambda分割は不採用。Slack受信→OpenClaw(Fargate) agent loop、検索/動画/メールは3つのMCPサーバ(teamagent-mcp/video-mcp/aiia-mcp)にツール公開で機能分離。video/tiktokツールはbuild_production_tools()でMCP越し公開（USE_VIDEO_TOOLS=1） | 🔄 方式変更 | infra/openclaw/openclaw.config.json5:99 MCP url／orchestrator/factory.py:131 build_production_tools()でvideo/tiktok公開／fargate.tf 2サービス。Lambda関数は存在しない | 物理Lambda分割ではなくMCPツール境界での論理分離に変更 |
| 3 | API Gateway+SQSで非同期fan-out | SQS不採用・API Gatewayなし。SlackイベントはOpenClawが直接受け、in-process（同一プロセス内）でagent loop実行。重い処理はRequestGate(asyncio semaphore)で制御 | 🔄 方式変更 | infra/terraform 内に aws_sqs / api_gateway リソース皆無（grep 0件）／runtime/request_gate.py（in-processゲート） | 非同期fan-outは未導入。単一GW垂直構成（~40名分離方式はP1実測で判断＝next_requirements §9-7） |
| 4 | RDS Proxyで接続プール集約 | RDS Proxy不採用。in-process RequestGate（concurrency=4・queue_max=64・FIFO・タイムアウト拒否）＋pg_poolで代替。Lambda 100+接続の前提自体が消失 | 🔄 方式変更 | infra/terraform に aws_db_proxy 皆無（grep 0件）／runtime/request_gate.py:63 concurrency=4 queue_max=64／adapters/pg_pool.py | 単一プロセス常駐のため大量Lambda接続問題が発生せず、RDS Proxy不要化 |
| 5 | 本番RDS接続切替とE2E再疎通（未着手🔴） | 本番RDS(pgvector 205doc共有/RLS)接続でE2E実返信成功(6/12)。MCP実行ロールがdatabase-url secretを参照、OpenClaw役割はDB不可の権限分離 | ✅ 一致 | mossy-snacking-locket.md §T1（pgvector hit5件/top_score0.794・bedrock合成・isError=false）／fargate.tf:138 MCP実行ロールdatabase-urlのみ | — |
| 6 | aws-samples公式CFNでOpenClaw PoC（未着手🔴・子会社ヒアリング待ち） | 公式CFNテンプレ方式は未採用だが、自前terraform/Dockerfileで等価のOpenClaw on AWS+Bedrockを本番デプロイし、Bedrock経由応答(Haiku4.5)をE2E実証。PoC段階を飛ばして本番go-liveまで到達 | 🟢 前倒し | infra/docker/Dockerfile.openclaw／fargate.tf:446 openclawサービス／openclaw.config.json5:28 amazon-bedrock/haiku-4-5／§S3 実返信 | aws-samples公式CFNは使わず自前IaCで実装（PoCを本番化が上書き） |
| 7 | Sprint2末(6/7)にB案/D案確定（未着手🔴） | OpenClaw採用(B案)で確定・不変。6/10に次要件書をOpenClaw採用前提へ改訂(commit 0b39b41)、6/12本番go-live。なお旧CLAUDE.md(6/3)は『SDK採用・OpenClaw不採用推奨』だったが、後の決定で逆転 | ✅ 一致 | git 0b39b41 『要件書をOpenClaw採用決定に合わせ改訂』／mossy-snacking-locket.md §S3 go-live。CLAUDE.md:420 の旧推奨は上書き | 判定日は計画(6/7)より遅れたが、より踏み込んだ本番稼働まで到達 |
| 8 | EC2常駐Bot本番デプロイ+Secrets Manager連携 | Slack Botの常駐先はEC2でなくFargate(OpenClaw)に変更。Secrets Manager連携は実装(bearer/database-url/slack-bot・app/gateway-token 5本・役割別最小権限)。EC2 workerはingest/scrape用に残存・systemd timerでingest週次自動化(コード化済・本番未実走) | 🔄 方式変更 | fargate.tf:24-37 5 secret＋IAM分離／deploy/systemd/teamagent-ingest.timer (Mon 18:00 UTC)／scripts/run_ingest.sh／worker.tf | systemd ingest timerは本番未デプロイ(コード段階)。BotはFargate化でEC2常駐方式から離脱 |
| 9 | 公式CFN+EC2でOpenClaw PoC | 公式CFN/EC2方式は不採用。Fargate(ARM64)+per-task IAM Role(Bedrock InvokeModel)+VPC+SG+CloudMap private DNSで本番構成。IMDSv2はworker EC2側で必須化 | 🔄 方式変更 | fargate.tf:191 openclaw-task role／openclaw-role.policy.json／worker.tf:142 http_tokens=required | EC2 t3.medium→Fargate ARM64へ。PoCを本番構成で代替 |
| 10 | 30秒以内のSlack往復をPoC成功定義に | 本番で@AiLa検索→OpenClaw→Bedrock→スレッド返信が isError=false・3.3秒で成功(30秒大幅クリア)。外側ループ既定はHaiku4.5(速い)、重処理でSonnet4.6 | 🟢 前倒し | mossy-snacking-locket.md §S3 『isError=false・3.3秒・スレッド返信(1781231927.429389)』／openclaw.config.json5:28,140 Haiku/Sonnet | PoCでなく本番E2Eで成功定義を達成 |
| 11 | 多軸評価でB案/D案確定（事前推定はD案優位） | B案(OpenClaw採用)確定。レイテンシ(3.3秒)・セキュリティ(ClawHub無効化+toolFilter+SG+IAM隔離+インジェクション拒否実証)・会社共有23ch相当をクリアして本番稼働。事前推定のD案優位を覆す結果 | ✅ 一致 | §S3/§T1/§T2-①(インジェクション拒否PASS)／docs/security/security_audit_2026-06-12.md／openclaw.config.json5 toolFilter+dmPolicy:allowlist | パイロット(T3 1週間)・2人混線テスト(T2-②)・トークンrotation(T4)は残作業 |
| 12 | FastAPI HTTPでSkill invoke API(B案採用時) | FastAPI /skills/{name}/invoke の形は採らず、MCP(streamable-http)境界 src/teamagent/mcp_gateway/server.py で同等のtool invokeを実現。SkillInput/Output相当はToolSpec+Pydantic schema。引用/コスト/レイテンシはMCP応答に内包 | 🔄 方式変更 | mcp_gateway/server.py（spec-MCP境界・STRICT identity）／openclaw.config.json5:100 transport:streamable-http／orchestrator/tools.py ToolSpec | REST/FastAPIではなくMCPプロトコルで実装。意図(疎結合invoke境界)は達成 |
| 13 | D案不採用時のみの条件付き計画 | B案採用によりD案は条件未成立=不要化。会話Memoryは現状OpenClaw側(揮発・P1既知制限)、Idempotencyはingestのupsert(ON CONFLICT)で部分的に存在するがD案で計画した形では未実装 | ⚪ 不要化 | §T1でB案確定（D案分岐は発火せず）／migrations 0001 documents upsert(ON CONFLICT)／§R 会話メモリ揮発はP1制限 | 会話Memory永続化はP2(EFS/将来)課題として残存 |
| 14 | 拡張可能Skillレジストリ型Agentプラットフォーム | @registerデコレータ＋SkillRegistry(name重複拒否)でcore改修なしの登録は実装。build_production_tools()でToolSpec化しMCP公開。12スキルが同基盤に載る。ただしホットロード/独立repo配布までは未到達 | 🟡 部分 | skills/base.py:90 SkillRegistry／:123 register()／factory.py:71 build_production_tools()。12スキルディレクトリ存在 | 無限追加の思想は実装済だが、CapabilityManifest単一登録点(計画項目25)未導入で『1箇所追加で全経路反映』は未達 |
| 15 | 営業16名向けMVA最小実用形 | 横断検索の最小実用形は本番稼働(pgvector dense+クライアントboost+Cohere Rerank+RLS+Contextual Retrieval、L2 Bedrock合成)。Slack入口はBolt直でなくOpenClaw経由に変更。Gmail/Drive OAuth基盤も実装(workspace_search/oauth_token_store) | 🟢 強化 | §T1 検索全行程ログ実証／skills/search/skill.py／adapters/pgvector_client.py／adapters/oauth_token_store.py。Rerank+Contextual Retrievalは計画以上 | Slack BoltはOpenClawに置換。1週間パイロット(16名実利用)は未完=T3残 |
| 16 | 6層アーキの俯瞰構成 | 入力UI(Slack)/データ層(pgvector RLS)/Skillsレジストリ(SkillRegistry)/Tools(MCP ToolSpec)/Guardrails(RLS+toolFilter+url_guard+identity STRICT+HITL設計)は実装。Memory層のみ会話履歴永続が薄い(OpenClaw揮発) | 🟡 部分 | server.py STRICT identity／adapters/url_guard.py SSRF防御／migrations RLS。Memory永続は§R揮発制限 | Memory(会話履歴)層が6層中で最も弱い。残5層は本番化 |
| 17 | S4最小→Phase4完全形のSkill登録/削除/更新API | @registerによるコード時登録は実装(name/description)。だが削除/更新API・version/ownerフィールド・CLI/管理UIからの操作は未実装。registryは起動時固定 | 🟡 部分 | skills/base.py:96 register(name重複拒否のみ)／list_all()／get()。version/owner/削除APIなし | 動的な削除/更新API・version/ownerメタ・管理UIは未実装 |
| 18 | JSON Schema宣言+実行時バリデーション | 各SkillはPydantic v2 input_schema/output_schema(=JSON Schema由来)を宣言し、実行時に検証。MCP公開時もtool schemaに変換。エラー宣言は明示フィールドなし | ✅ 一致 | skills/base.py:76-77 input_schema/output_schema ClassVar／各skills/*/schema.py(Pydantic)／server.py tool化 | エラー型の明示JSON Schema宣言は弱い(例外→runtime catch) |
| 19 | 依存解決+semverバージョニング | 未実装。SkillRegistryはname重複拒否のみで、依存宣言/解決・semver・複数版並列・ロールバックの機構なし | 🔴 未実装 | skills/base.py:90-125 全文に version/dependency/semver なし(register/get/list_allのみ) | 依存解決・semver並列登録・ロールバックは全て未着手 |
| 20 | core改修なし+ホットロード反映+2営業日追加 | core改修なし・register()のみで追加は実現(@registerデコレータ)。ただしホットロード(稼働中反映)は未実装=プロセス再起動/再デプロイ要。2営業日上限は運用指標で未計測 | 🟡 部分 | skills/base.py:123 register()／factory.py 追加時はimport追記。ホットロード機構なし(import時登録) | ホットロード(無停止反映)未実装。2営業日SLAは未計測 |
| 21 | OpenClaw全面採用しRegistry/Channel/Memory/監査を委譲 | OpenClawをL0外殻として採用しSlack channel抽象・agent loop・基本監査は委譲。ただしSkill RegistryとRLS/identity/反ハルシは自前(mcp_gateway境界)に保持=『全面委譲』でなく『外殻委譲+境界は自前死守』のハイブリッド | 🔄 方式変更 | next_requirements.md §6.3『2層→3層(L0 OpenClaw外殻/L1個別tool/L2委譲)』／server.py『RLS/per-user OAuth/反ハルシは境界の内側Pythonで死守』 | Registry/Memory/identityはOpenClawに丸投げせず自前境界で保持。F-1の『全面委譲』は意図的に修正 |
| 22 | OpenClaw→Bedrock ping 200のS1疎通 | 達成・超過。本番でOpenClaw→Bedrock(Haiku4.5既定/Sonnet4.6重処理)が実応答3.3秒。pingでなく実検索E2E(MCP金庫→pgvector→L2合成→Slack返信)まで疎通 | 🟢 前倒し | §S3 isError=false 3.3秒／§T1 bedrock_converse(Sonnet4.6 L2合成 10.6s $0.0125)／openclaw.config.json5:121 models | — |
| 23 | SkillSpec(required_scope/audit_tag含む)のregister/list/get/invoke | register/list/get/invoke相当は実装(SkillRegistry+BaseSkill.run)。name/description/input_schema/output_schema/handlerは有り。だがrequired_scope/audit_tagフィールドはBaseSkillに無い(toolFilter/監査は別レイヤで担保) | 🟡 部分 | skills/base.py register/get/list_all＋BaseSkill(name/description/input_schema/output_schema/run)。required_scope/audit_tag不在 | required_scope/audit_tagがSkillSpecに統合されておらず、openclaw.config.json5 toolFilterに手書き(計画項目36の自動派生は未達) |
| 24 | docker-compose+setup_local.sh 1コマンド+スモークの個人Mac開発環境 | docker-compose(postgres+pgvector)・.env.example・setup_local.sh・smoke_mcp.py 全て整備済。bedrock-proxyは別構成だがBedrock直アクセスで代替 | ✅ 一致 | infra/docker/docker-compose.yml:15 postgres(pgvector)／scripts/setup_local.sh／scripts/smoke_mcp.py／.env.example | bedrock-proxyコンテナはなくBedrock直叩き(実害なし) |
| 25 | P0でCapabilityManifestを全スキルに1枚追加し唯一の登録点化 | 未実装。src全体にCapabilityManifestクラス/manifest宣言が皆無(grep 0件)。triggers/ack/cost_class/permission_scopeは依然分散(intent.py/slack_bot.py _ACK_BY_SKILL等)。OpenClaw移行が優先されP0リファクタは着手されず | 🔴 未実装 | grep CapabilityManifest/Manifest in src=0件／base.pyは旧来の単純@register／_ACK_BY_SKILLはslack_bot.py:437に手書き残存 | P0の核(manifest単一登録点)が未着手。OpenClaw go-liveが優先された |
| 26 | P0でCapabilityId StrEnum新設 | 未実装。CapabilityId(StrEnum)はsrcに存在せず(grep 0件)。intent文字列/skill名は依然文字列ベース(intent.py detect_skill)。QueryType StrEnumはあるが別物(検索戦略用) | 🔴 未実装 | grep CapabilityId in src=0件／router.py:29 QueryType(StrEnum)は検索戦略用で能力IDではない | 中央CapabilityIdは未新設。型一元化は未達 |
| 27 | P0でimport-linter warning→後Phaseでゲート化 | 3層ディレクトリ(runtime/skills/adapters)は物理的に存在し設計原則として遵守されているが、import-linterによるCI強制は未導入(pyproject/pre-commitに設定なし) | 🟡 部分 | src/teamagent/{runtime,skills,adapters}存在／grep import-linter/importlinter in pyproject.toml,.pre-commit-config.yaml=0件 | 3層は人手遵守。import-linterのCI強制(warning/ゲート)は未設定 |
| 28 | P2でディスパッチャをmanifest駆動の単一dispatchに置換 | 未実装。slack_bot.pyにget_X_skill×12(get_search/karte/draft/review/video/tiktok/oplog/chitchat/video_approval/video_algorithm等)・dispatch_auto・_ACK_BY_SKILLが手書きで残存。単一dispatch化されていない | 🔴 未実装 | slack_bot.py:512-1066 get_*_skill 12個／:1066 dispatch_auto／:437 _ACK_BY_SKILL／:1094 detect_skill if判定 | P2ディスパッチャ置換は未着手(legacy god-fileのまま)。本番経路はMCP側に移ったため優先度低下 |
| 29 | god-fileを200行台へ | 未実装。slack_bot.pyは2,137行のまま(計画時2,186行からほぼ不変)。解体されず。ただし本番Slack入口はOpenClaw(Fargate)に移行したため、このファイルはレガシー化(本番未使用経路) | 🔴 未実装 | wc -l slack_bot.py=2137行／本番入口はopenclaw.config.json5(Slack)→server.py(MCP)に移行 | god-file解体は未着手。OpenClaw移行で本番依存は外れたが、コードは残置 |
| 30 | format_search_response/build_*_blocks等をResponsePresenterへ退避 | 未実装。ResponsePresenterクラスは不在。format/build_*_blocks系は依然slack_bot.py内に同居。スキルは既にPydantic構造体を返すがPresenter分離は未完 | 🔴 未実装 | grep ResponsePresenter in src=0件／slack_bot.pyに整形ロジック残存。本番表示はOpenClaw/MCP応答側で別途整形 | adapters層ResponsePresenterへの全量退避は未着手 |
| 31 | SlackInboundAdapter切り出し | 未実装(この命名/クラスでは)。Slack受信はslack_bot.py(legacy)とOpenClaw native Slackプラグインの二系統。SlackInboundAdapterという統一層は切り出されていない。adapters/slack_client.py等は個別存在 | 🔴 未実装 | grep SlackInboundAdapter=0件／adapters/slack_client.py(送信中心)／本番受信はopenclaw @openclaw/slackプラグイン(4a1da5d) | 計画したadapter切り出しは未着手。本番Slack受信はOpenClawプラグインが担う方式変更 |
| 32 | P3でRetrievalPort/GenerationPort 2本切り出し | 未実装。RetrievalPort/GenerationPortという抽象ポートはsrcに存在せず(grep 0件)。検索retrieve・Bedrock合成は具象クラス直結のまま | 🔴 未実装 | grep RetrievalPort/GenerationPort in src=0件／search/skill.py retrieve_hits・adapters/bedrock_client.py 直結 | P3のポート抽象は未着手 |
| 33 | P4で7ドメイン再編 | 未実装。skills/配下は依然12個の機能別ディレクトリ(search/clientkarte/proposal/video/tiktok_search/operation_log/chitchat/vseo/workspace_search/mail_constraints/video_approval/video_algorithm)。7ドメイン束ね直し・1manifest化は未着手 | 🔴 未実装 | find src/teamagent/skills -type d=12機能ディレクトリ／manifest不在(項目25と連動) | P4ドメイン再編は未着手。manifest前提なので項目25-26が地盤 |
| 34 | Settings集約+os.environ.get lint禁止 | 未実装。pydantic-settings/BaseSettings/Settingsクラスはsrcに不在(grep 0件)。factory.py等で os.environ.get/_envflag が直書き多数(PROMPT_VERSION/USE_*等)。実験ノブ(v2d等)も恒久ifに残存 | 🔴 未実装 | grep pydantic_settings/BaseSettings/class Settings in src=0件／factory.py:66 os.environ.get("PROMPT_VERSION","v2d")・_envflag多用 | Settings集約・lint禁止・実験ノブ整理すべて未着手 |
| 35 | P0でmanifest網羅+import-linterのCIチェック | 未実装。manifest自体が無いため網羅/契約チェックも不在。CI(.github)にimport-linter/manifest検査なし。既存CIはruff/mypy/pytest中心 | 🔴 未実装 | manifest不在(項目25)／grep import-linter=0件／.github/workflows はlint/test中心 | manifest契約CIは未着手(manifest前提が無いため) |
| 36 | manifest 6軸→toolFilter等を自動派生し手書き二重定義を解消 | 自動派生は未実装。openclaw.config.json5 の toolFilter.include/exclude は手書き運用のまま。RLS/identity enforcementはserver.py側に実装されているが、manifestからの自動生成ではない。設計意図(二重定義解消)はnext_requirements.mdに記載のみ | 🔴 未実装 | openclaw.config.json5:108-114 include/exclude手書き／next_requirements.md:143 計画記載のみ／manifest不在 | manifest 6軸自動派生は未着手。toolFilterは手書き(機能はするが二重定義リスク残) |
| 37 | P4で検索Skill構築の重複を単一factoryへ | 未実装。重複が残存=orchestrator/factory.py:_build_search_skill(MCP経路)とslack_bot.py:get_search_skill(legacy経路)の双方が併存しenv解決を別々に持つ | 🔴 未実装 | factory.py:_build_search_skill／slack_bot.py:512 get_search_skill。両方が独立にenv解決 | 単一factory統合は未着手。2経路並存のまま |
| 38 | P0-P5の段階的ストラングラー移行 | next_requirementsのP0-P5リファクタは事実上着手されず、代わりにOpenClaw移行(WS-A〜§T)という別のストラングラー移行が実行・go-live。可逆性は、正準deploy runbookのdurable previous task revision・ECS deployment circuit breaker・fresh signed rollback authorization・one-use full saved planで担保 | 🔄 方式変更 | P0-P5証跡なし(manifest/Settings/import-linter全不在)／実際の移行はmossy-snacking-locket.md §T(OpenClaw)／現行rollback契約はdocs/openclaw/deploy_runbook.md（過去の直接desired-count変更案は廃止） | 計画したP0-P5は未実行。OpenClaw移行に置換された(可逆性原則は継承) |
| 39 | ゲート①承認→L2本配線完遂 | ゲート①(Node本番持込)は承認・go-live済(OpenClaw=Nodeコンテナが本番Fargate稼働)。ただしP5の『registry全manifest自動@tool化+orchestrator本配線(runtime→orchestrator参照ゼロ)』は未完=マルチステップ(VSEO→提案→PPTX)自律解放は設計段階 | 🟡 部分 | §S3 OpenClaw(Node)本番稼働=ゲート①通過／但しmcp_gateway/server.py:254 build_production_tools()でtool公開はするが、orchestrator自律ループのL2本配線は未完／html-agent-floofy-fountain.md(提案5段は設計段階) | ゲート①は通過済だがP5 L2自律オーケストレーション本配線・マルチステップ解放は未達 |

## 検索・RAG（32項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | 計画baseline=Titan v2/Cohereへ移行予定（Sprint3+）。現状はローカルe5で暫定 | 本番もローカル e5（intfloat/multilingual-e5-large・1024次元・sentence-transformers）。検索・ingest 両方とも LocalE5Embedder 固定。Titan/Cohere の Embedder 実装クラスは存在せず、コメント上の差替予定のみ。本番E2Eでも `embedder_embed`(E5 1024dim/535ms) を実証 | 🔄 方式変更 | src/teamagent/adapters/embeddings_client.py（LocalE5Embedder のみ・BedrockTitanEmbedder 不在）/ orchestrator/factory.py:53 `embedder=LocalE5Embedder()` / scripts/ingest_sources.py:100 / mossy §T1(E5 1024dim実測) | Titan/Cohere 埋め込み未実装。次元1024は両立するので将来差替の口（Embedder Protocol）は用意済 |
| 2 | 計画baseline=pgvector dense のみ記載 | pgvector(dense)＋クライアント名boost＋Cohere Rerank(v3.5/Bedrock東京)＋RLS＋min_relevance(2段階fallback)＋Contextual Retrieval＋集約モード。top-1 20%→52%→64% を gold set(50ケース)で実証し採用 | 🟢 強化 | src/teamagent/skills/search/skill.py（_apply_cohere_rerank/_apply_client_boost/min_relevance/aggregation）/ docs/v3.2/system_reference.md:157（20%→52%→64%）/ data/eval/sales_gold_set.yaml(50ケース) | なし（計画を大幅に上回る） |
| 3 | 未着手(🔴)。後にv1.2でSprint3 Drive API連携内の自動取得へ統合方針に変更 | PLACEHOLDER置換の単発スクリプト方式は廃止。drive_url は ingest パイプライン（Drive API連携）が取込時に直接付与し、検索 hit の metadata.drive_url として返る。SearchHitOut に drive_url フィールド・Slack/Drive permalink ボタン生成あり | 🔄 方式変更 | src/teamagent/ingest/pipeline.py（Drive取込）/ skills/search/skill.py:202 drive_url / runtime/slack_bot.py:244-248（Drive/Slackボタン）。update_drive_urls.py 方式は本repoに不在 | 旧PLACEHOLDER方式の痕跡なし＝計画通り自動取得へ統合済 |
| 4 | 完了(✅ Day3 SearchHitOut / PR #26) | SearchHitOut が file_name/page_num/score/source/drive_url/source_uri/source_type/channel_name/client_name 等を構造化保持。要約は cache_system=True で system prompt キャッシュ。Block Kit で `📄 file (p.N)` 表示 | ✅ 一致 | skills/search/schema.py(SearchHitOut)/ skills/search/skill.py:576 `cache_system=True` / docs/v3.2/teamagent_master_todo_v1.md:164-165(✅ Day3/PR#26) | 本番E2Eでは cache_read=0 実測（caching未効）→チューニング候補として記録（mossy §T1） |
| 5 | 完了(✅ PR #35+#38、本番RDS適用済) | migration 0001 で document_source_type ENUM（pdf/gdrive/gmail/slack…）+ documents/chunks + ACL列 + RLS(FORCE ROW LEVEL SECURITY) を定義。本番RDSへ適用済（SSMトンネルで0010/0011まで実適用、slack/gsheets 205docに会社ACL付与＝横断可能化） | ✅ 一致 | infra/migrations/0001_unified_documents.sql(ENUM/RLS) /0002/0003 / mossy §line15（0010=RLS lower化・0011=UPDATE 205 実RDS適用）/ master_todo S3-14(✅本番RDS適用済) | なし |
| 6 | 計画(🤖+👤) | Titan v2 PoC・精度比較は未実施。Embedder切替判断も未着手で本番は LocalE5 継続。差替用 Embedder Protocol の口だけ存在 | 🔴 未実装 | adapters/embeddings_client.py に Titan 実装なし / master_todo S4-04(計画のまま✅マークなし) / C1(将来 Titan切替+A/B PoC) | PoC自体が未実施。e5 で本番要件を満たしているため優先度低のまま |
| 7 | 計画(🤖+👤) | gold set は計画の30件を超え50ケースで作成（実機検証済FB案件/汎用/ネガティブ/miss類型狙撃26-50）。run_eval.py が top1_hit/top5_hit/mrr/precision@5/cost/latency を計測。top-1 20%→52%→64% 改善を立証 | 🟢 強化 | data/eval/sales_gold_set.yaml(version:2 50ケース)/ scripts/run_eval.py(top1/top5/mrr/precision@5)/ tests/eval/test_gold_set_structure.py / system_reference.md:157 | 満足度（人手評価）指標は数値計測に含まれず自動指標中心。SkillRouter改善は item11/29 参照で形式ルータは未完 |
| 8 | 計画(🤖、A2-A4ロードマップ、ブロッカーなし) | Contextual Retrieval は ingest repository に `contextualized` 列 + scripts/contextual_retrieval.py で実装。Slack FB の client_name/deal_phase/bant_score/channel_type 等メタデータは slack_fb_parser で自動抽出され検索 hit に反映。検索 skill が use_contextual で contextual テーブル利用 | ✅ 一致 | src/teamagent/ingest/repository.py:79,210(contextualized)/ scripts/contextual_retrieval.py / ingest/slack_fb_parser.py / skills/search/skill.py(use_contextual) | 長threadの明示的sub-chunk化（1thread=複数chunk）の実装証跡は弱い。Contextual＋メタ抽出は到達 |
| 9 | 計画(🤖、中期 B1-B4) | Cohere Rerank（pool30→top_k 絞込）は実装・本番経路。source_type フィルタは新スキーマ検索で metadata 経由対応。一方 BM25(pg_bigm)+RRF ハイブリッドは設計のみで明示的に未実装（dense+Rerank が本番）。in:slack/from:drive 構文の source 自動検出は未実装 | 🟡 部分 | skills/search/skill.py:_apply_cohere_rerank / system_reference.md:43,108（pg_bigm+RRF は設計のみ・未実装と明記）/ architecture_and_flows.md（RRF は図のみ） | BM25ハイブリッドと in:/from: 構文ルータは未着手。Rerank/source_type filter は到達 |
| 10 | 計画(🤖、将来 C1-C7) | ほぼ未着手。Titan切替=未(item6)。bulk ingest は本日(6/15)scripts/run_ingest.sh + systemd timer 化（EventBridge方式から変更・本番未実走）。Query expansion / Recency weighting / EMF精度モニタは検索skillに実装なし。リトライは Bedrock call_with_retry 既存 | 🔴 未実装 | skills/search/skill.py（query expansion/recency なし）/ 現状ダイジェスト（ingest=systemd timer化・本番未実走）/ EventBridge常設なし | ingest自動化はEventBridge→systemd timerへ方式変更で部分前進。検索の高度化(expansion/recency)は未着手 |
| 11 | 計画(ルールベース版はSprint1残🟢で既出) | SkillRouter は rule-based 主体で、confidence<0.6 時のみ Haiku 4.5 にフォールバックするハイブリッド(PR#27/USE_LLM_ROUTER)。完全Haiku化は未。4種 QueryType は定義済だが compare/meta は判定のみで実装は content と同じ死にパス（aggregation モードが meta/列挙を別途救済） | 🟡 部分 | skills/router.py(rule主体+_route_llm フォールバック, LLM_FALLBACK_THRESHOLD=0.6)/ docs/next/teamagent_next_requirements.md §7（compare/meta は content と同じ死にパス・約1年塩漬けと明記） | 完全Haiku化未。4種の実装差別化が不完全（aggregationで部分代替）。本番はOpenClaw agent loop がスキル選択を担い、この内部routerの比重は低下 |
| 12 | 計画(本実装はSprint6) | Sprint6を待たず前倒し実装済。scripts/contextual_retrieval.py が各chunkにHaikuで文脈要約を前置→再embedding→proposals_chunks_contextual に保存（prompt caching でdoc全文コスト1/10）。INPEX +3.69 / top-1 0.85→0.89 改善を実証 | 🟢 前倒し | scripts/contextual_retrieval.py / ingest/repository.py(contextualized列) / master_todo:52,427,456（✅ INPEX +3.69 / Contextual 0.89） | なし（Sprint6予定を前倒し達成） |
| 13 | 計画（検索精度チューニング・Sprint6） | 引用フォーマット(📄 file(p.N)+Slack/Driveリンクボタン)は到達。失敗クエリ再現は gold set のネガティブケース(21-25,47-50)＋test_gold_set_structure.py で pytest化。一方 vector0.7+JSONB0.3 の加重ハイブリッドは未実装（dense+Rerank、JSONBはフィルタ/boostであり加重融合ではない） | 🟡 部分 | runtime/slack_bot.py:244(Block Kitボタン)/ data/eval/sales_gold_set.yaml(ネガティブケース)/ tests/eval/test_gold_set_structure.py / skills/search/skill.py(JSONBはfilter/boost用途) | vector0.7+JSONB0.3 の数値加重融合は未。引用・失敗テストは到達 |
| 14 | MUST・計画(MVA完成の定義/S6) | 本番E2E到達。@AiLa→OpenClaw→teamagent-mcp(search)→RDS pgvector(205doc会社共有)→L2 Sonnet合成→スレッド返信を 2026-06-12 に実証(isError=false/3.3秒)。search は build_production_tools→MCP gateway で公開済 | 🟢 前倒し | mossy §T1（2026-06-12 検索全行程ログ実証: embedder_embed→pgvector_search hit5件/top0.794→bedrock_converse→search_skill_done）/ orchestrator/factory.py:85(SearchSkill ToolSpec)/ mcp_gateway/server.py:256 | S6予定を大幅前倒し。p95応答はL2二重LLMで20.2秒＝ゲート目標(≤15s)超過の課題は残る |
| 15 | MUST・計画(S6) | L2 Sonnet が引用付き要約を生成（[chunk_id] 引用ラベル + source/file_name/page_num/channel_name/drive_url）。Slack/Drive permalink ボタン生成。タイムスタンプは source_uri から thread permalink を再構成 | ✅ 一致 | skills/search/skill.py:_summarize（[chunk_id]引用ブロック）/ schema.py SearchHitOut(各リンクフィールド)/ runtime/slack_bot.py:91 _slack_thread_permalink | なし（本番経路で稼働実証済） |
| 16 | MUST・計画(S6) | 本番(OpenClaw)経路ではスレッド返信＋agent loopメモリで会話文脈を保持（§T1でスレッド返信実証）。ただし検索Skill自身に『前回検索条件の構造化継承』機構はなく、文脈継承はOpenClaw側のLLMメモリ依存。旧legacy botのthread処理はoperation_log用で検索条件継承ではない | 🟡 部分 | mossy §T1（スレッド返信 1781231927.429389）/ skills/search/skill.py（前回条件の継承state無し）/ runtime/slack_bot.py:859-866（thread処理はoplog用） | 明示的な『前回検索条件の継承』はSkill層に未実装。OpenClaw会話メモリでの間接的継承に依存＝設計意図との一致は要判断 |
| 17 | MUST・計画(全体横断KPI) | fail-honest を実装：min_relevance しきい値で弱根拠hitを落とし0件なら『資料に記載がありません』と認める。v2d prompt が引用[chunk_id]必須・推論は※推論:明示・550字制約。CloudWatchダッシュボード(teamagent-dev-openclaw-pilot)+alarm3本配備。ただし引用率90%/捏造率0%の定量KPI計測ダッシュボードは未確立 | 🟡 部分 | skills/search/skill.py(min_relevance/fallback/低信頼マーク)/ prompts/search/v2d/system.md（引用必須・推論明示）/ mossy §line256(CloudWatchダッシュボード+alarm) | 引用率/捏造率の数値KPIダッシュボードは未設置。fail-honest 機構と引用ラベルは到達。faithfulness の定量評価は受け入れ基準として明文化(next_req §6)だが計測ループ未完 |
| 18 | SHOULD・計画(S6) | 未実装。役立った/役立たなかったボタンや品質ログ蓄積の実装はrepo全体に存在しない | 🔴 未実装 | grep(役立っ/feedback_helpful/品質ログ)→検索skill/runtimeにヒットなし | フィードバックボタン・品質ログ機構が全く未着手（SHOULD優先度） |
| 19 | MUST・Phase4で正式Skill登録として計画(S14) | @メンション起動・上位hit・引用ラベルは本番E2E稼働（§T1で@AiLaメンション→3.3秒応答を実証）。受動監視ではなくメンション起動方式に確定。3秒目標は単純検索で達成も、L2二重LLMの重いクエリでは20.2秒(p95≤15s超過)のケースあり | 🟢 前倒し | mossy §S3（@AiLa検索して→isError=false/3.3秒/上位hit/teamagent__search認知）/ §T1(20.2s重クエリ・L2 10.6sがボトルネック) | レイテンシは軽クエリ3.3秒だが重クエリ20秒＝3秒/15秒目標を一部超過。L2 Haiku化orデータ返却型がチューニング候補 |
| 20 | MUST・計画(S14) | slash command /teamagent_search は legacy slack_bot.py(Bolt)に実装存在。ただし本番はOpenClaw経路に移行しBoltベースのslash/ephemeralは本番不使用。confidence<0.7確認返却はmin_relevance fallbackの低信頼マーク（is_low_confidence）で近似（断定抑制注意文を付与）。ephemeral配信はメール系で実績ありだが検索のephemeral確認返却は本番経路で未配線 | 🔄 方式変更 | runtime/slack_bot.py:1663(@app.command /teamagent_search・legacy)/ skills/search/skill.py:553(is_low_confidence注意文)/ OpenClaw移行で Bolt slash 不使用 | slash/ephemeral は legacy bot のみ＝OpenClaw本番では非配線。confidence確認は低信頼マークで近似実装 |
| 21 | SHOULD(キャッシュ)/MAY(音声)・計画 | いずれも未実装。検索結果の頻出クエリキャッシュ機構なし、音声入力起動もなし（prompt cachingはsystem promptキャッシュであり別物） | 🔴 未実装 | grep(query_cache/頻出/音声/transcri)→検索skill/runtimeにヒットなし | 両機能とも未着手（SHOULD/MAY 優先度のため後回し） |
| 22 | v1.1で確定(F-3)。S1-02/S1-04で構築・インデックス設計を計画 | PostgreSQL16 + pgvector(本番0.8.2) + HNSW(vector_cosine_ops) を採用しRDS稼働。ただしHNSWインデックスは m=16/ef_construction=64 を明示指定せずpgvectorデフォルト。pg_trgm拡張はinit-pgvector.sqlでCREATEされるが、実クエリ/SQLヘルパーでの利用箇所はなく未活用 | 🟡 部分 | infra/migrations/0001_unified_documents.sql:100-101(HNSW・パラメータ明示なし)/ infra/docker/init-pgvector.sql:8(pg_trgm CREATE のみ)/ 現状ダイジェスト(RDS pgvector 0.8.2) | HNSWの m/ef_construction が計画値で明示設定されていない（デフォルト）。pg_trgm は拡張作成のみで未活用（BM25/部分一致は未実装と整合） |
| 23 | S1のDoD/チケットS1-07として計画(着手前) | S1-07 単体の smoke_test_pgvector(100件投入)スクリプトは確認できず。代替として demo_pgvector_search.py(類似検索デモ)、ingest_test_data.py、tests のpgvector検索テスト群でtop-k検索の機能は担保。MCP境界の smoke_mcp.py が本番疎通検証を担う | 🔄 方式変更 | scripts/demo_pgvector_search.py / scripts/ingest_test_data.py / scripts/smoke_mcp.py（MCP疎通smoke）。専用 smoke_test_pgvector.py は不在 | 100件投入のS1-07専用smokeは未作成だが、同等のtop-k検索検証は他経路で達成。S1 DoDの形式的チケットは別形態に吸収 |
| 24 | S3チケットとして計画(着手前) | chunk_pages を実装し ingest→embed→pgvector投入の連結は稼働。ただし分割単位は『800 token』ではなく size=500文字/overlap=100文字（文字数ベース、ページ境界非跨ぎ） | 🔄 方式変更 | src/teamagent/ingest/pdf_extract.py:42-44(size=500,overlap=100・文字数ベース)/ ingest/pipeline.py:572,719(chunk_pages size=500 overlap=100) | 計画の『800 token』に対し実装は『500文字』＝単位(token→文字)もサイズも乖離。連結パイプライン自体は完成 |
| 25 | S4チケットとして計画(着手前) | documents(owner_email/modified_at/source_type)+chunks(metadata JSONB)でメタデータ層を実装。search_similar_new_schema が documents+chunks JOINで3ソース(Slack/Drive/Sheets)横断。案件タグ(client_name/deal_phase/bant_score)は slack_fb_parser でLLM/ルール抽出 | ✅ 一致 | infra/migrations/0001_unified_documents.sql(documents/chunks/JSONB)/ adapters/pgvector_client.py:335(search_similar_new_schema JOIN)/ ingest/slack_fb_parser.py(案件メタ抽出) | 案件タグ抽出はFB特化(client/phase/bant)。汎用『案件名 LLM 1-shot』の網羅度は要確認だが主要メタは到達 |
| 26 | S4のDoDとして計画(着手前) | 3ソース横断検索は新スキーマJOINで稼働、client_name boost で固有名詞案件のリコール補強。eval は20クエリを超え50ケースで top1 20%→64%/MRR/precision@5 を計測。ただしDoD指定の『recall@10 70%』という指標名での明示計測ログは確認できず(top1/top5/mrr/precision@5 中心) | 🟡 部分 | adapters/pgvector_client.py(search_similar_new_schema/search_drive_by_client_names)/ data/eval/sales_gold_set.yaml(50ケース)/ scripts/run_eval.py(top1/top5/mrr/precision@5) | 3ソース横断・eval拡充は到達超過だが、recall@10=70% という当初DoD指標そのものでの達成記録は無く top-1 64% 等で代替評価 |
| 27 | Phase3 S6チケットとして計画(着手前) | SearchSkill は @register デコレータ登録＋build_production_tools の ToolSpec として MCP gateway に公開。引用付きサマリー(出典[chunk_id]明示)とBlock Kit(📄ファイル名/permalinkボタン)実装。本番E2Eで稼働実証 | 🟢 前倒し | skills/search/skill.py:33(@register)/ orchestrator/factory.py:85(ToolSpec)/ mcp_gateway/server.py:256(build_production_tools)/ runtime/slack_bot.py:244(Block Kit)/ mossy §T1 | なし（S6予定を前倒し本番到達） |
| 28 | 計画(機能要件。既存searchスキルを当ドメインへマップ) | 検索の中身（pgvector dense + Cohere Rerank + RLS + v2d prompt）は実装済で本番稼働。ただし『Knowledge&Memoryドメイン』という7ドメイン再編の構造化(ドメイン束ね/ResponsePresenter分離等)は要件書(next)の計画段階で、コード上のドメイン境界再編は未着手 | 🟡 部分 | skills/search/skill.py(dense+Rerank+RLS)/ prompts/search/v2d/system.md / docs/next/teamagent_next_requirements.md:55,70（Knowledgeドメインへのマップは計画） | 検索機能そのものは完成・本番稼働。ドメイン再編(7→6ドメイン束ね)の構造化リファクタは未実装 |
| 29 | 計画(P1で router.py QueryType を Knowledge内部へ降格) | router.py(QueryType)は独立モジュールのまま＝Knowledge内部への降格(二重ルータ解消)は未実施。compare/meta は判定のみで死にパス(content と同じ)状態が継続。aggregationモードが meta/列挙を別経路で部分救済するのみ | 🔴 未実装 | src/teamagent/skills/router.py(独立QueryType・降格なし)/ docs/next/teamagent_next_requirements.md §7,§189（P1で降格・shadow並走は計画） | 二重ルータ解消は計画(P1)のまま未着手。本番はOpenClaw agent loop がスキル選択を担うため、この内部routerの本番比重は低下している |
| 30 | 計画(機能要件) | 集約・一覧モード(USE_AGGREGATION_MODE)を実装：aggregation.py が『BANT A の案件一覧』『失注案件』等の列挙意図を検出し list_by_metadata で構造化フィルタ列挙。filter_industry によるメタフィルタも対応。router の meta 判定とも連携 | ✅ 一致 | src/teamagent/skills/search/aggregation.py(extract_aggregation_filter)/ skills/search/skill.py:282(aggregation mode)/ adapters/pgvector_client.py:461(list_by_metadata)/ router.py(_META_PATTERNS) | 予算軸の集計は明示的フィルタ未確認(BANT/channel/失注が中心)。件数/一覧の主要メタ集計は到達 |
| 31 | 計画(出力契約・受け入れ基準) | 出典は SearchHitOut の file_name/page_num/source 等で構造化保持。fail-honest 実装：hit 0件で『該当する資料が見つかりませんでした』、min_relevance しきい値で弱根拠を落とし v2d prompt が『資料に記載がありません』と認める＋低信頼マークで断定抑制 | ✅ 一致 | skills/search/schema.py(SearchHitOut構造化)/ skills/search/skill.py:537(0件メッセージ)/ :330-359(min_relevance fail-honest)/ prompts/search/v2d/system.md(記載なし時認める) | なし（出力契約として実装済）。next_req の faithfulness 定量評価ループは別途未完(item17) |
| 32 | 将来計画(未着手) | 未着手。取込状況の可視化ダッシュボードや勝ちパターン/失注理由の横断ナレッジ化機能は未実装。ingest_jobs テーブル(0005)で取込ジョブ記録の素地はあるが、鮮度可視化UIや横断ナレッジ抽出は無し | 🔴 未実装 | infra/migrations/0005_ingest_jobs.sql(取込ジョブ記録の素地のみ)/ docs/next/teamagent_next_requirements.md:74（将来計画と明記） | 将来計画通り未着手。v2d prompt が個別クエリ単位でパターン抽出はするが、DB横断の恒久ナレッジ化・鮮度可視化は未 |

## データ取込ingest（32項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | 雛形完了(PR#36)、本実装はcredentials取得後。差分監視はinterface完了(🟡) | gdrive_client.py(25KB)がフル実装。list_files/walk_files_recursive/list_shared_drives/list_permissions/download_file_bytes/get_start_page_token/get_changes(changes.list)まで完備。差分監視はclientメソッドとして完全実装済(interfaceにとどまらず本体まで) | 🟢 強化 | src/teamagent/adapters/gdrive_client.py:481-552 (get_start_page_token/get_changes実装)、386-471 (walk_files_recursive)、git c337478/9d2c305 (#60 drive.readonly切替) | changes.list本体はあるが、ingest pipelineへ未配線(後述の差分同期項目参照)。EventBridge cronは未設定 |
| 2 | 未着手(🔴)。スキーマ準備済(🟡) | ingest_pdfs.py拡張ではなく、新設の ingest/pipeline.py 経由でDrive取込を実現。_process_one_gdrive_file が source_uri=web_view_link / owner_email=permissions解決 / modified_at=f.modified_time を DocumentUpsert に反映。documents表へ投入 | 🔄 方式変更 | src/teamagent/ingest/pipeline.py:598-617 (DocumentUpsertにsource_uri/owner/modified_at反映)、scripts/ingest_pdfs.py(旧proposals_chunks専用で温存) | ingest_pdfs.py自体はローカルPDF→旧proposals_chunks専用のまま(別経路)。Drive統合は別ファイルで達成=方式的にPR-6計画通りだがファイル名は変わった |
| 3 | 雛形完了(PR#37)、取り込みパイプライン未着手(🔴) | gmail_client.py(28KB)フル実装:隠しラベル(TeamAgentLabels/labelHide)・denylist物理封鎖・gmail.modify scope。ただし pgvectorへのGmail取り込みパイプラインは未実装。gmailはAiLaメール製品(要約/返信ドラフト)用途で、documents/chunksへは1件も入らない | 🟡 部分 | src/teamagent/adapters/gmail_client.py:262-277(隠しラベル)、grep結果: source_type='gmail'はrepository.pyのコメント/ENUMのみ、_ingest_gmailハンドラ不在 | ingest_sources.yamlにgmailセクション無し、pipeline.pyに_ingest_gmailハンドラ無し、source_type='gmail'はENUM定義のみで実投入経路なし。Gmail→社内ナレッジDBの取込は未着手 |
| 4 | 雛形完了(PR#39/#40) | slack_channel_ingest_client.py(13KB)+gsheets_client.py(16KB)ともフル実装。Slackは_ingest_slack_channelでthread→document化+channelメンバーをACL自動写像+営業FB構造化parser。GSheetsはrow_unit=1行1documentで本番投入済(205docにgsheets含む) | 🟢 強化 | src/teamagent/ingest/pipeline.py:130-271(slack)・781-844(gsheet)、slack_fb_parser.py、migration 0011でslack/gsheets 205doc backfill | なし(雛形→本実装まで到達し本番DB投入済) |
| 5 | 未着手→Day5で完了(PR#43) | 全構成要素が存在し稼働:data/ingest_sources.yaml(slack2/gdrive2/gsheets2+shared_drives_crawl)、ingest/loader.py、ingest/repository.py(ON CONFLICT冪等upsert)、ingest/pipeline.py(IngestRunner)、scripts/ingest_sources.py(CLI、--commit/--dry-run/--sources) | ✅ 一致 | data/ingest_sources.yaml、src/teamagent/ingest/{loader,repository,pipeline}.py、scripts/ingest_sources.py、PR#43 | なし |
| 6 | 計画(担当👤+🤖) | Day7-8で前倒し実行:Drive folder bulk ingest機構(walk_files_recursive+共有ドライブ全自動crawl+営業資料フィルタ)を実装し、Day8時点で gdrive 586 docs/9,125 chunks を本番RDSに投入実績あり | 🟡 部分 | docs/v3.2/progress_report_day8.html:221,366(slack205+gdrive586/9125)、mossy-snacking-locket.md:15(0011=UPDATE205 slack/gsheetsのみ)、master_todo:713(Drive PDF 0件→A1で投入) | ただし現行(2026-06-15)本番DBは205doc(slack/gsheets)が共有対象=OpenClaw go-live時の整備基準。Day8の586 gdrive docsは現行の共有メタbackfill(205)に含まれず、現production set上のDrive PDF反映状態は要再確認(=現在は実質Drive PDF未反映の可能性)。機密区分仕分けは自動フィルタ(noise/mime/size)止まりで人手棚卸し台帳は未確認 |
| 7 | 計画(🤖) | 定期実行は EventBridge日次ではなく、worker EC2の systemd timer(週次・毎週月曜18:00UTC)としてコード化(2026-06-15)。run_ingest.sh+deploy/systemd/{service,timer}。ただし本番未実走。メタデータ抽出(LLM)パイプライン自体は documents経路には未実装 | 🔄 方式変更 | deploy/systemd/teamagent-ingest.timer(OnCalendar=Mon)、scripts/run_ingest.sh、mossy-snacking-locket.md(EventBridge常設なし) | (1)スケジューラがEventBridge→systemd timerに方式変更かつ週次(日次でない) (2)本番未デプロイ/未実走 (3)LLMメタデータ抽出は別物(後述項目12参照) |
| 8 | OAuth取得済だがingestコマンド未実行(❌)。documents Drive PDF 0件 | drive.readonly切替済(GDriveClient.from_env(readonly=True))。Internal OAuthでCASA不要を明記。folder bulk ingest解禁(#60)。Day8で gdrive 586 docs 実投入を達成(計画では未実行だった部分を前倒し完了) | 🟢 前倒し | pipeline.py:459 (readonly=True+CASA不要コメント)、git 9d2c305(#60)、progress_report_day8.html:366(gdrive586投入済) | 現行本番DB(205 slack/gsheets)にDrive PDFが反映されているかは要再確認(OpenClaw移行時の整備基準が205doc) |
| 9 | 残タスク(🟡)。Sprint1終了基準=スキーマ確定PRマージ | より発展した形で確定:proposal_chunks単一表でなく documents+chunks 2表に正規化。chunks に id/document_id/chunk_idx/content/embedding vector(1024)/metadata JSONB/created_at、HNSW(vector_cosine_ops)。documents に source_uri/owner_email/acl_emails/acl_groups/metadata JSONB+GIN(acl_emails/acl_groups)。本番RDS適用済 | 🟢 強化 | infra/migrations/0001_unified_documents.sql:39-103(documents/chunks DDL・HNSW・GIN)、migration 0001本番適用済 | GINは当初計画のmetadata列でなくACL配列列に張られている(設計意図がACL優先に進化)。metadata JSONBへのGINは未付与 |
| 10 | 計画(雛形作成) | ファイル名/ソースは変わったが機能は実装:scripts/ingest_pdfs.py(ローカルdata/proposals/→pdfplumber→chunk→E5 embedding→proposals_chunks INSERT、Haiku contextualize、Sonnetメタ抽出)。S3起点でなくローカルディレクトリ起点。新スキーマ向けはingest/pipeline.py(pypdf) | 🔄 方式変更 | scripts/ingest_pdfs.py:1-60(pdfplumber/chunk/embedding/INSERT)、src/teamagent/ingest/pdf_extract.py(新経路pypdf) | S3起点でなくローカルファイル起点。pdfplumberは旧経路、新documents経路はpypdf(pdf_extract.py) |
| 11 | 計画(雛形のみ、完成はSprint4持ち越し明記) | metadata JSONB列はdocuments/chunks両方に存在し、ingest時にextra_metadata/構造化フィールド(営業FB parser由来11フィールド等)を保存。旧経路ingest_pdfs.pyはSonnetでメタ抽出→metadata JSONB保存。新documents経路はLLM抽出でなくyaml/parser由来のメタを保存 | 🟡 部分 | migration 0001(metadata JSONB)、pipeline.py:228-237(FB metadata first-class化)、ingest_pdfs.py(Sonnetメタ抽出は旧proposals_chunks経路のみ) | 新documents経路にLLMによる抽出JSON保存は未配線(Sprint4本実装=項目12が未着手)。雛形相当は満たすが計画通りSprint4本実装は未到達 |
| 12 | 計画(Skill②本実装、Sprint1 P1延長) | documents/chunks経路では未実装。LLMメタ抽出は旧ingest_pdfs.py(proposals_chunks向け、ローカルPDF)に部分的にあるのみ。metadata_version列なし、debug_snapshots/KMS監査なし、Query Routerのmetadataフィルタ連携(WHERE条件化)は項目19のFB parser由来client_name等の限定的フィルタに留まる | 🔴 未実装 | grep結果: metadata_version/debug_snapshots/extract_metadata がingest経路に不在、Sonnetメタ抽出はingest_pdfs.py(旧経路)のみ | バッチ(S3→Sonnet→JSON→JSONB)未配線、metadata_version世代管理未実装、debug_snapshots/KMS TTL30日未実装、増分再処理(document_id単位)未実装 |
| 13 | 計画(Sprint5・リスク=Bedrock予算) | バルク投入機構(walk_files_recursive+shared_drives_crawl+modified_within_days=730で2年絞り)は実装済でDay8に gdrive 586docs/9,125chunks投入実績。embeddingはLocalE5(ローカル計算)採用でBedrock課金リスクは回避済 | 🟡 部分 | pipeline.py:620-778(shared_drives_crawl)、ingest_sources.yaml:113(modified_within_days:730)、ingest_jobs使用はtests/adapters/test_pgvector_schema.pyのみ(runner不在) | 45,000chunks規模(1,500PDF)の本格バルクは未到達(Day8で9,125chunks)。ingest_jobs state machine(再開機能)はschema(migration0005)のみでrunner未配線=一晩バッチの再開耐性は未実装。Sprint5は時期未到来 |
| 14 | MUST・計画(S1 DoD対象) | Gmail OAuth基盤(DWD impersonate対応 from_env(impersonate_user))は実装。ただしscopeはgmail.readonly+metadataでなく gmail.modify を採用(denylist物理封鎖で送信封じ・CASA不要のため)。かつGmailをpgvectorへ取り込むパイプラインは未実装(AiLaメール製品用途) | 🔄 方式変更 | gmail_client.py:288-292(SCOPES_MODIFY採用)、from_env(impersonate_user)でDWD、_ingest_gmailハンドラ不在 | (1)scope方針が変更(readonly→modify) (2)16名Gmailの社内ナレッジDB取込は未実装 (3)gmail.metadata scopeは未使用 |
| 15 | MUST・計画 | Drive OAuth+folder/共有ドライブ再帰walkで全ファイル列挙は実装。本文抽出はPDFのみ実テキスト化(pypdf)。docx/pptx/xlsx/Md/GoogleDocs/Slidesは whitelist対象だが title-onlyの1 chunk(本文抽出なし) | 🟡 部分 | pipeline.py:586-596(非PDFはtitle+mimeのみ)・732-742(crawl側も同様)、_SALES_ALLOWED_MIME_TYPESにoffice含むがbody抽出なし | PDF以外(docx/pptx/xlsx/Markdown/Google native)の本文抽出が未実装=title+mime_typeのみembed。「全ファイルを取り込む」の本文取込はPDF限定 |
| 16 | MUST・計画 | SlackChannelIngestClientで指定2チャンネル(ナレッジ共有/営業FB)のmessage+thread replies取込を実装し本番投入済(slack 205doc)。thread親子保持・DM除外(channel指定)・channelメンバーACL写像 | ✅ 一致 | pipeline.py:130-271、ingest_sources.yaml:14-40(2 channel)、migration 0011(slack 205doc backfill) | なし(本番投入まで到達) |
| 17 | MUST・計画 | Embedding投入パイプラインは稼働。ただしEmbedderは Bedrock Titan v2でなく LocalE5(multilingual-e5-large 1024次元)を採用。Embedder Protocolで差替可能設計だがTitan実装もVoyage実装も不在。LocalE5採用理由=API課金ゼロ・本番MCPイメージにE5焼込 | 🔄 方式変更 | embeddings_client.py:37-76(LocalE5Embedderのみ)、grep: Titan/voyageは実装なし、mossy-snacking-locket.md(E5焼込) | Titan v2未採用(コメントに「予定」が残るのみ)、Voyage AIのA/B実装は皆無。次元数1024は一致 |
| 18 | MUST・計画 | Drive差分検知の素材(get_changes/get_start_page_token=changes.list)はclientに実装済。だが pipelineは1ページのみ取得しconnector_state(カーソル)永続化なし=実際の増分同期は未配線。スケジュールはsystemd週次フル走査(idempotent upsert)で「5分以内」未達。Slack増分もoldest永続化なし(コメントで Sprint4送り) | 🔴 未実装 | pipeline.py:189(1ページのみ・増分はSprint4とコメント)、gdrive_client.py:481-552(get_changes実装済だが未呼出)、connector_state表のmigration不在 | 5分以内検知の増分同期は未実装。changes.list本体はあるが未配線、connector_state表もなし、cron/EventBridge 5分なし。Gmail増分は項目14同様DB取込自体なし |
| 19 | MUST・計画(タグ後続手動修正はSHOULD) | source_type/owner_email/channel_name/modified_at/extra_metadata等は必須付与。営業FBは parserで11種structured field(商流/顧客名等)を first-class化。だがtier(機密区分)タグとprojectタグのLLM自動推定は未実装(client_codeカラムは存在するが未使用、projectはheuristicなclient_name抽出のみでLLM推定でない) | 🟡 部分 | pipeline.py:228-249(metadata付与)、slack_fb_parser.py(parser=非LLM)、migration 0001:54-55(client_code列はあるが未投入) | projectタグのLLM自動推定なし(正規表現parser止まり)、tierタグ付与なし(client_code列は空) |
| 20 | MUST・計画 | partial failure許容(1 source/1 file失敗で次へ・per-file try/except・socket timeout60s)は実装。ingest_jobs state machine表(migration 0005=SCANNED→COMMITTED→POISON)はschema投入済。だが状態機械を駆動するrunner/再開ロジックは未実装(テスト以外で未参照) | 🟡 部分 | migration 0005_ingest_jobs.sql(state machine schema)、grep: ingest_jobs使用はtest_pgvector_schema.pyのみ、pipeline.py:500-508(per-file try/except) | 再開機能(ingest_jobs駆動)はschemaのみでcode未配線。5万〜10万件規模の実バッチは未実施(最大実績 gdrive586/9,125chunks)。48h SLA検証なし |
| 21 | SHOULD・計画 | PDF添付はDriv/Slackファイル経由で本文抽出(pypdf)+chunk+embedding+検索可能。Word/PPTは本文抽出なし(title-only)。OCR(画像PDF/スキャン)は未実装 | 🟡 部分 | pipeline.py:551-585(PDF実抽出/非PDFはtitle-only)、grep OCR/tesseract: ヒットなし | Word/PPTの本文抽出なし、OCR皆無。SHOULD項目なので優先度低だが未充足 |
| 22 | MUST・計画 | 未実装。changes.listのremoved検知(get_changesにinclude_removed/DriveChange.removed)は素材としてあるが、それをトリガに documents/chunks をDELETEする処理は無い。テストfixture掃除のDELETE FROM documents(ingest_test_data.py)以外に削除伝播ロジックなし | 🔴 未実装 | grep: DELETE FROM documentsはscripts/ingest_test_data.py:44(fixture掃除)のみ、削除伝播handler不在、gdrive_client get_changesのremovedは未消費 | 削除伝播の本番ロジック皆無。opt-out/GDPR削除フロー未実装 |
| 23 | MUST・計画 | 取込データの保持期間ポリシー(TTL/ライフサイクル)を実装する仕組みは ingest経路に無し。ingested_at列はあるが保持期限による自動削除/Glacier移行はなし。監査ログのS3 Glacier 1年も未確認 | 🔴 未実装 | migration 0001:60(ingested_at列はあるが保持期限ロジックなし)、grep: Glacier/retention/保持期間 のingest側実装なし | 保持期間ポリシー全般が未実装(7年/90日/1年いずれも自動執行する機構なし) |
| 24 | MUST・計画 | チャンク仕様は満たす:chunk_pages(size=500,overlap=100)=500字・オーバーラップ100字(計画レンジ500-800字の下限)。再Embeddingバッチ(全件再生成)スクリプトは不在 | 🟡 部分 | src/teamagent/ingest/pdf_extract.py:43-59(size=500/overlap=100)、grep: reembed/再Embed スクリプトなし | 再Embeddingバッチ専用スクリプト/機構なし(モデル変更時の全件再生成手段なし)。idempotent upsertで個別再投入は可能だが「モデル変更→全件再embed」のバッチは未実装 |
| 25 | S1チケットS1-03として計画。SQL DDL抜粋を提示済 | documents表+chunks表(embedding vector(1024))をDDLで作成し本番適用済。ただし命名は embeddings でなく chunks。connector_state表とaudit_log表は作成されていない(migration一覧にない) | 🟡 部分 | migration 0001(documents/chunks vector1024)本番適用、infra/migrations一覧にconnector_state/audit_logのmigration不在、grep結果confirm | connector_state表なし(増分同期カーソル永続化の欠落と整合)、audit_log表なし(usage_events/runtime_metricsは別途あるが計画のaudit_logとは別) |
| 26 | S2チケットとして計画(着手前) | Gmail API薄ラッパ+OAuth(refresh token/SA DWD)実装済。トークンRefreshはgoogle-auth標準+oauth_token_store.py(per-user token保管)+google_oauth_flow.pyで認可フロー実装。営業の自己サービスGoogle連携(/teamagent connect)も実装 | ✅ 一致 | gmail_client.py、adapters/google_oauth_flow.py、oauth_token_store.py、git c33aa9b(自己サービスGoogle連携) | なし(Gmail API+OAuth+refresh自体は完成)。ただしこれはAiLaメール/connect用途で、ナレッジ取込とは別(項目3/14参照) |
| 27 | 第一候補として確定(F-6)、8.2 MVA-5で再評価予定。S2で実装計画 | pgvectorへのバッチ投入は稼働するが、EmbedderはTitanでなくLocalE5(1024次元)。再評価ゲート(MVA-5)の結果としてローカルE5へ事実上決定(API課金ゼロ・本番イメージ焼込)。次元1024は一致 | 🔄 方式変更 | embeddings_client.py(LocalE5のみ)、bedrock_client.pyにtitan embed不在、master_todo:559(Embedder=LocalE5確定2026-05-26) | Titan V2は不採用に転換。bedrock_clientにembed/titanメソッドなし |
| 28 | S2チケットとして計画(着手前) | Gmailバッチ50通/200ms/1ユーザー直列化の専用レート制御は未確認。Drive側はsocket timeout60s+per-file try/exceptで耐障害化、Gemini呼出はretry実装(#116)。ただしGmail取込バッチ自体が未実装のため当該レート制御も実体なし | 🔴 未実装 | grep: バッチ50/200ms/backoffのGmail取込実装なし、ab1ebc4(Geminiリトライ=別系)、Gmail ingest自体が不在 | 指数バックオフ(50通/200ms/直列化)の実装は未確認・Gmail取込が無いため対象不在 |
| 29 | 現案として計画。MVA-3(取り込み方式)はS2開始前に要決定の未決事項 | Slackはoldest_days(180=半年/365=1年)をyamlで指定=初回期間制御は実装。だが「以降は増分」が未配線(毎回フル走査をidempotent upsertで吸収する方式に変更)。Driveもmodified_within_days(730)で期間絞り | 🔄 方式変更 | ingest_sources.yaml:23,36(oldest_days 180/365)・113(modified_within_days 730)、pipeline.py:189(増分はSprint4・現状フル走査) | 増分(前回ts以降)は未実装=毎回フル再走査(週次timer)。6ヶ月固定でなくyaml可変(180/365/730) |
| 30 | S3チケットとして計画(着手前) | Drive API+OAuth+フォルダ取込Workerは実装(walk_files_recursive/folder bulk)。modifiedTimeはDriveFile.modified_timeとして取得しdocuments.modified_atに保存、_is_sales_relevantでmodified_within_days判定にも使用。changes.listカーソル(get_start_page_token)もclient実装済 | 🟡 部分 | gdrive_client.py(walk_files_recursive/modified_time/get_start_page_token)、pipeline.py:424-432(modified_within_days判定)、カーソル永続化未配線 | modifiedTimeカーソルによる『更新ファイルだけ再取込』の差分Workerは未配線(毎回フル走査)。カーソル永続化(connector_state)なし |
| 31 | S3チケットとして計画(着手前)。画像のみOCRはMVA範囲外 | PDFパーサのみ実装(新documents経路=pypdf、旧proposals経路=pdfplumber)。Word/PPT(ノート)/Excel/Markdownの本文パーサは未実装(mime whitelistには含むが title-only embed) | 🟡 部分 | pdf_extract.py(PDFのみ)、ingest_pdfs.py(pdfplumber)、pipeline.py:586-596(非PDFはtitle-only)、grep: docx/pptx/openpyxlのimport無し | python-docx/python-pptx(スピーカーノート)/openpyxl/Markdownパーサが全て未実装。office系は本文がDB検索対象にならない |
| 32 | S4チケットとして計画(着手前) | Slack取込Worker(list_channel_history+list_thread_replies)実装、スレッド親子保持(format_thread_as_document でparent+replies束ね)、対象チャンネルyaml宣言、channelメンバーACL写像、営業FB構造化。本番投入済(slack 205doc)。S3/S4計画を前倒しでDay5に達成 | 🟢 前倒し | pipeline.py:196-208(thread replies取得)、slack_channel_ingest_client.py(format_thread_as_document)、master_todo:502(Slack197docs投入2026-05-26) | なし(Worker・スレッド保持とも実装し本番投入済) |

## メール・Workspace（22項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | 計画v2.2: 週次サマリーを専用 SummaryWorker Lambda（独立IAM）で定期実行する独立ワーカー方式 | SummaryWorker 相当の Lambda は存在しない。infra/terraform/lambda_iam.tf の Lambda 関数本体は丸ごとコメントアウト（106行目 `# resource "aws_lambda_function"`）、EventBridge cron も terraform に皆無。週次サマリー機能そのものが未実装で、src/skills/README.md は『営業進捗サマリー Skill (progress/) = Phase 4-e』と将来扱い。 | 🔴 未実装 | infra/terraform/lambda_iam.tf:106（関数本体コメントアウト）／grep eventbridge/scheduler=0件／src/skills/README.md:15（progress/ = Phase 4-e） | SummaryWorker・定期実行基盤・週次サマリーロジックいずれも未着手。元計画文書でも b-no（未実装） |
| 2 | 計画v2.2: API Gateway(HTTP API) で Slack Events API を受け、3秒ack＋署名検証する HTTP プッシュ方式 | 本番入口は Socket Mode（WebSocket常駐）。src/teamagent/runtime/slack_bot.py は AsyncSocketModeHandler（line 1,27）。OpenClaw 移行後も顔（AiLa）は Socket Mode 占有（openclaw.config.json5・plan §V）。API Gateway は connect の OAuth callback 専用に存在（scripts/aws/deploy_apigw_connect.sh: HTTP API→VPC Link→ALB→/oauth2/callback）でイベント入口ではない。 | 🔄 方式変更 | src/teamagent/runtime/slack_bot.py:1,27（Socket Mode）／scripts/aws/deploy_apigw_connect.sh:4,13（APIGWはOAuth callbackのみ）／plan §V 行61,71 | Events API/3秒ack/署名検証は採用せず。Socket Mode（Bolt）+OpenClaw が代替。意図的な方式変更 |
| 3 | 計画v2.2: 16名分の OAuth refresh トークンを Secrets Manager で集中管理し、Mail Worker が読んで Gmail/Workspace 連携 | 方式が二重に変更。(a)トークン保管=per-user OAuth を RDS+KMS（migration 0006_oauth_tokens.sql / RdsTokenStore / KmsCipher）または AiLa側 DynamoDB aiia-oauth-tokens に保存、Secrets Manager には infra秘密（bearer/DB/Slack）のみ。(b)『Mail Worker』Lambda は存在せず、消費側は workspace_search/mail_constraints Skill（既定OFF）と AiLa バッチ。connect 経路（connect_web OAuth・KMS暗号化・RDS保存）は実装済だが live dispatcher 未配線、共有OAuthで Drive ingest のみ稼働。 | 🔄 方式変更 | infra/migrations/0006_oauth_tokens.sql／src/teamagent/adapters/oauth_token_store.py(RdsTokenStore/KmsCipher)／factory.py:156-166(_build_token_store)／variables_fargate.tf:56-82(Secretsはbearer/DB/Slackのみ) | 16トークン集中×Secrets Manager×Mail Worker という形では実装されず。per-user KMS+RDS/DynamoDB へ分散。live dispatcher 未配線は現状ダイジェスト通り |
| 4 | 計画v2.2 機能②: 朝バッチで16名分の未返信を検出し、返信案A/B/C を Gmail に下書き（営業朝工数削減の柱） | 別repo AiLa(~/Documents/AI-IA-UAE)に朝ワークフロー実体が実装済。orchestrator/multi.py run_for_all_users（連携済全員を並行処理→各自Slack DM）、create_drafts トグル、triage.py で未返信/要対応判定、llm.py で draft 生成。本人1人で実Gmail 21件→Bedrock要約→DM実E2E成功(plan §U-1)。ただし返信案は単一draftで『A/B/C 複数案』ではない。10名AWS展開は組織SCP(公開Lambda禁止)でブロック中(plan §V6改)。orchestrator-poc側は mail_constraints（制約抽出）のみで朝ワークフロー無し。 | 🟡 部分 | AI-IA-UAE src/aiia/orchestrator/multi.py:39(run_for_all_users)／run_batch.py／triage.py／llm.py:80(draft)／plan §U-1 行168／§V6改 行43(SCPブロック) | コアは実装・本人E2E成功だが (a)A/B/C複数案でなく単案 (b)16名→10名スコープ (c)AWS本番展開がSCPでブロック。元計画TODO P1(要否再評価)は AiLa として実体化、ただし orchestrator本流ではない |
| 5 | 計画v2.2 機能⑤: 受注角度（🟢🟡🔴）の週次サマリーをマネージャーへDM配信 | 週次・受注角度・マネージャーDM のいずれも未実装。AiLa schemas.py の 🟢🟡🔴 は『当日メールのカテゴリ色』(INTERNAL/CLIENT_NORMAL/CLIENT_URGENT)であって受注角度ではない。マネージャー横串集約 Skill は src/skills/README.md で progress/ = Phase 4-e と将来扱い。 | 🔴 未実装 | src/skills/README.md:15(progress/=Phase4-e)／AI-IA-UAE src/aiia/schemas.py:38-44(色はカテゴリ用・受注角度でない)／grep 週次/weekly=該当機能0 | 受注角度判定ロジック・週次集計・マネージャーDM とも未着手。元計画TODO P1(価値高ければ設計)のまま |
| 6 | Sprint1残〜2末: filter_industry をスラッシュコマンド引数で受ける（未着手🔴） | 実装済。slack_bot.py で /teamagent_search のオプション許可キーに industry を許可（_SLASH_COMMAND_ALLOWED_KEYS={industry, top_k}・line 117）、parse_command_text で key=value 分解、filter_industry_override として Router の auto-detection より優先＋strict_industry=True で厳格化（line 1235-1267,1663）。 | ✅ 一致 | src/teamagent/runtime/slack_bot.py:117,122,1235-1267,1663（/teamagent_search industry=...） | 計画は🔴未着手だったが実装完了。スラッシュコマンド明示指定＋自然文auto-detect両対応で計画通り以上 |
| 7 | Sprint3: GCPプロジェクト/同意画面(Internal)、Drive OAuth(drive.file+metadata.readonly=CASA不要)、Gmail OAuth(gmail.modify=読み+下書き・CASA不要)（未着手🔴・DWDは不要化🟢） | GCP/OAuth実体は稼働。ntv-ai プロジェクト(vectorinc組織配下/Internal)・client_id 既存(MEMORY reference_teamagent_workspace_oauth 2026-06-03検証完了)。Gmail は gmail_client.py が gmail.modify 1本(SCOPES_MODIFY・line289・コメント『Sensitive Tier2 CASA不要・readonlyはTier3回避』)。connect導線(connect_web/google_oauth_flow)は全7サービス readonly(gmail.readonly/calendar.readonly/contacts.readonly等)で同意画面構築済。Drive ingest は共有OAuthで実稼働。 | ✅ 一致 | src/teamagent/adapters/gmail_client.py:7,289-291(gmail.modify Tier2)／google_oauth_flow.py:22-30(7scope readonly)／MEMORY reference_teamagent_workspace_oauth(ntv-ai/Internal/2026-06-03検証) | 計画🔴未着手だが GCP/同意画面/OAuthクライアントとも整備・検証済。Gmailは計画通り gmail.modify。Drive scope は ingest用途で実装差あり(共有OAuth)。DWD不要化🟢も踏襲 |
| 8 | Sprint3: 営業のDriveフォルダ構造を調査（未着手🔴・👤人間タスク） | Drive ingest が本番稼働し gdrive 586 docs/9117 chunks 取込実績あり(MEMORY day7/day10・本日6/15 systemd timer週次自動化コード化)＝フォルダ構造は実取込を通じて把握済。ただし『調査ドキュメント』としての明示成果物は repo 内に確認できず。 | 🟡 部分 | scripts/run_ingest.sh／deploy/systemd/teamagent-ingest.timer／MEMORY project_teamagent_day7/day10(gdrive 586docs/9117chunks) | 実 ingest 経由で構造は実質把握・自動取込稼働。人間タスク(👤)の明示的『調査メモ』成果物は repo に無く要確認。実装監査範囲では実取込で代替済と判断 |
| 9 | Sprint8(8/22〜): Gmail readonly拡張・朝8:30 EventBridge cron・当日メール要約Lambda・Slack DND status分岐・thread_ts紐付け統合分析・個別メールBlock Kit overflow menu（計画🤖+👤、Gmail adapter deny封鎖は完了✅PR#49） | 部分実装＋方式変更。(済)Gmail adapter の破壊的18メソッド物理封鎖=gmail_client.py に _GMAIL_DESTRUCTIVE_METHODS denylist+_GmailSafePolicy 実装済。(済)当日メール要約=AiLa pipeline。(変更)朝8:30 cron=EventBridge でなく AiLa systemd timer(aila-batch.timer Mon-Fri 9:30JST)。(済)Block Kit UI=AiLa delivery/slack.py。(済)thread_ts/Slack統合=AiLa が未返信Slackメンション合流(§V)。(未)DND status分岐は確認できず。当日要約Lambda形態でなくバッチ/MCP。 | 🟡 部分 | src/teamagent/adapters/gmail_client.py:49-119(deny封鎖)／AI-IA-UAE deploy/systemd/aila-batch.timer(9:30JST)／delivery/slack.py(Block Kit)／plan §V(Slackメンション合流) | deny封鎖は計画通り完了。cron→systemd timer・Lambda→バッチ/MCP の方式変更。DND分岐・overflow menu の個別UI は未確認。Sprint8予定が AiLa として前倒し実装され形態が異なる |
| 10 | Sprint2 W1(5/30-6/5): OpenClaw EC2の~/.openclaw/.envにSlack App/Bot Token投入し既存17スコープと一致接続（PoC計画） | 達成・本番E2E到達(計画前倒し)。Slack(AiLaアプリの bot/app token)→OpenClaw 本番接続完了。実トークンで socket mode connected(invalid_auth無)・@AiLa→isError=false/3.3秒/スレッド返信の実E2E成功(plan §S3完了 2026-06-12・commit 7367507)。デプロイ先は EC2 でなく Fargate へ進化(現状アーキ)。スコープは app_mentions:read等を追加し Reinstall で反映実証(plan T0完了)。 | 🟢 前倒し | plan §S3 行24(E2E実返信 7367507)／§T0 行23(scope追加Reinstall)／infra/openclaw/openclaw.config.json5(Slack/Bedrock配線)／git 7367507,4a1da5d | PoC計画(5/30-6/5)を超え本番go-live到達(6/12)。EC2→Fargate へ実体は進化。スコープ完全一致は購読不足を補修して達成 |
| 11 | 要件v3.0 MUST(Phase4): 毎朝8:30 に当日対応すべきメールを優先度別に Slack DM 配信 | AiLa で実装・本人E2E成功。orchestrator multi.run_for_all_users→各自 Slack DM、優先度=triage.py の BASE_PRIORITY/カテゴリ順。配信時刻は 9:30 JST(8:30でなく)・systemd timer。本人で実Gmail→分類→DM配信 delivered:True 実証(§U-1)。10名AWS展開はSCPブロック。 | 🟡 部分 | AI-IA-UAE orchestrator/multi.py:39／triage.py:12(BASE_PRIORITY)／aila-batch.timer(9:30)／plan §U-1 行168 | 機能本体は実装・1人E2E済だが (a)時刻が8:30→9:30 (b)10名本番展開がSCPで未完。MUST要件は実体到達も本番未稼働 |
| 12 | 要件v3.0 MUST: 差出人/件名/本文+過去文脈で『即返信/今日中/数日OK/様子見』の4段階に分類 | AiLa triage.py+llm.py が分類実装。ただし軸が異なり、カテゴリ7分類(CLIENT_URGENT/PRESS_MEDIA/FINANCE_LEGAL/CLIENT_NORMAL/VENDOR_PARTNER/INTERNAL/NEWSLETTER)＋BASE_PRIORITY数値＋deadline正規表現(本日中/今週金曜/締切等)＋actionable判定で優先度を算出。Heuristic(決定論)とBedrock LLM の両classify。『即返信/今日中/数日OK/様子見』という4ラベル名そのものではない。 | 🔄 方式変更 | AI-IA-UAE src/aiia/triage.py:18-37,149(キーワード分類)／schemas.py:17-35(7カテゴリ+BASE_PRIORITY)／llm.py:20,30,49(deadline正規表現/classify) | 差出人/件名/本文ベースの優先度判定は実装。ただし4段階ラベルでなく7カテゴリ×優先度数値の設計に変更。過去文脈活用は限定的 |
| 13 | 要件v3.0 MUST: 個別メール選択でスレッド全体＋関連Drive/Slackを統合分析 | 部分。AiLa は未返信Slackメンションのダイジェスト合流(§V)・スレッド get_thread 読取はあるが、『個別メール選択UI→スレッド全体＋関連Drive/Slackの横断統合分析』としての深掘り機能は未確認。返信導線は markdownリンク(§V4)。Drive横断との統合はAiLa側に実装無し。 | 🟡 部分 | AI-IA-UAE src/aiia/mcp/workspace_gmail.py(get_thread)／plan §V(Slackメンション合流)・§W-1(返信導線)／Drive統合の実装は未確認 | スレッド読取＋Slack合流は部分実装だが、個別選択UI＋Drive/Slack統合分析の完全形は未到達。MUST要件として残作業あり |
| 14 | 要件v3.0 MUST: 相手の文体・過去やり取りに沿った返信案を Gmail 下書き保存（gmail.compose・送信は人間が必ず確認・送信権限なし） | 実装済。AiLa llm.py:80 draft()→DraftReply、workspace_gmail.py:124 create_draft(下書きのみ)。送信は物理封鎖：orchestrator-poc gmail_client.py の denylist が messages.send/drafts.send 等18破壊メソッドを adapter層で RuntimeError 化(scope=gmail.modify でも封鎖)。AiLa harness は create_draft/send を dry-run記録のみ。スコープは gmail.modify(下書き含む)で gmail.compose 単独ではない。 | ✅ 一致 | AI-IA-UAE llm.py:80(draft)／workspace_gmail.py:124(create_draft下書きのみ)／orchestrator-poc gmail_client.py:49-119(send系deny封鎖)／config.py:36(mode=draft 人間承認) | 下書き生成＋送信物理封鎖は計画通り（むしろ封鎖は計画超えの安全強化）。文体/過去やり取り沿いの精緻化は基本ヒューリスティック＋LLM。scope は compose でなく modify+deny の設計 |
| 15 | 要件v3.0 MUST: 本人Mail=要件定義用、他人Mail=過去事例ナレッジDB。個別営業のメール共有opt-out機能 | 本人限定の死守ラインは厳格実装。mail_constraints/workspace_search とも G1 本人受信箱限定(impersonate=requester束縛)・G2 本人同意opt-in必須(ConsentStore・MAIL_CONSENT_EMAILS・未同意fail-closed)。AiLaも『他人の生メール覗き見NG・個人メールは本人完結』を上長指示として設計固定(plan §V 行65,181)。ただし『他人Mail=過去事例ナレッジDBへ集約』は §V-8 で“指針のみ・今回未実装”、生メールでなく構造化事実のみ一方向ブリッジする将来トラック。 | 🟡 部分 | src/teamagent/skills/mail_constraints/skill.py:149-162(G1本人限定/G2同意fail-closed)／workspace_search/skill.py:61-75／plan §V 行65,113(§V-8 ブリッジは指針のみ未実装) | 本人限定＋opt-in(opt-outより強い既定OFF)は計画超えで実装。一方『他人メール→過去事例ナレッジDB』集約は未実装(設計指針のみ・上長範囲で将来) |
| 16 | 要件v3.0 SHOULD: 会議招待/カレンダー連携リマインダ＋添付ファイル(PDF/Word/Excel)自動要約 | カレンダーリマインダは実装(計画超え)：AiLa notify/calendar_notifier で予定5分前 Slackメンション(start∈(now,now+5分]・all_day/declined除外・claim-before-send dedup・notified_store)＝§V18cea00。workspace_search でもカレンダー検索可。一方、添付(PDF/Word/Excel)自動要約は AiLa/orchestrator とも実装確認できず未着手。 | 🟡 部分 | AI-IA-UAE src/aiia/notify/calendar_notifier.py(5分前メンション)／state/notified_store.py(dedup)／plan §V 行87,195／添付要約=grep該当機能0 | SHOULD要件の半分(カレンダーリマインダ)は5分前通知として実装・本人E2E。残り半分(添付自動要約)は未実装 |
| 17 | 要件v3.0 MAY: メール文面のトーン分析(怒り/不安/期待)＋受信フォルダのラベル自動分類 | 両方とも一部実装。トーン分析=AiLa llm.py がTone=neutral/urgent/frustrated/positive/formal を判定(line 57-67)。ラベル自動分類=workspace_gmail.create_draft/label_thread が AIIA/<category> ラベルを resolve-or-create、orchestrator-poc gmail_client に ensure_team_agent_labels/modify_message_labels/create_hidden_label 実装。トーン軸は『怒り/不安/期待』そのものではなく frustrated/urgent/positive 等。 | 🟢 強化 | AI-IA-UAE src/aiia/llm.py:57-67(Tone判定)／src/aiia/schemas.py:11(Tone Literal)／workspace_gmail.py:5(label_thread)／orchestrator-poc gmail_client.py:489,507(ラベル分類) | MAY(最低優先)要件が両方とも実体実装済＝計画超え。トーンラベルの語彙は要件と若干差(frustrated≈怒り/不安) |
| 18 | MVA仕様v1.1 S2チケット: メール取込Worker(Lambda)・HTML→text抽出(beautifulsoup4)・署名/引用除去(talon)（着手前） | 方式変更＋部分。メール取込Workerは Lambda でなく AiLa バッチ/MCP(workspace_gmail)。本文抽出=gmail_client.extract_plain_text が MIME tree を walk して text/plain のみ抽出(HTML時は空文字・bs4使わず別レイヤー責務と明記)。beautifulsoup4 は依存に存在(pyproject.toml:41)も Drive/ingest 用途で email 本文には未適用。talon(署名/引用除去)は依存にもコードにも存在せず=未導入。 | 🔄 方式変更 | src/teamagent/adapters/gmail_client.py:652-671(MIME text/plain抽出・bs4不使用)／pyproject.toml:41(beautifulsoup4・ingest用)／grep talon=0件 | 取込Worker は Lambda 形態でなくバッチ/MCP。HTML→text は bs4でなくMIME walk。talon署名/引用除去は完全未導入。MVA S2チケットの構成とは異なる実装 |
| 19 | MVA仕様v1.1 S7(Phase4-a): draft_mail Skill を register、Gmail下書きAPI＋人間レビュー導線（起案メール50%をAI下書き・修正≤2回） | 実体は AiLa の draft 機能として実装(draft_mail という名のskill register は orchestrator-poc 側になし)。AiLa llm.draft→create_draft(下書きのみ)＋返信導線(§W-1: 要対応メールに返信導線/やること箇条/金額・担当バッジ・markdownリンク §V4)。送信は deny封鎖で人間確認必須。『起案50%/修正≤2回』のKPI計測機構は未確認。orchestrator-poc の build_production_tools に draft_mail skill は無く、mail系は mail_constraints(制約抽出)のみ。 | 🟡 部分 | AI-IA-UAE llm.py:80(draft)／workspace_gmail.py:124(create_draft)／plan §W-1(返信導線UI)／orchestrator-poc factory.py(draft_mail skill 未register・mail_constraintsのみ) | 下書きAPI＋人間レビュー導線は AiLa で実装。ただし(a)draft_mail という独立Skill register は無し(b)50%/修正≤2回のKPI計測は未確認。地続き計画が AiLa 側に分化 |
| 20 | next要件: NG手法/予算/期限/関係性を生本文を返さず構造化制約のみ抽出、Personal Workspace Bridgeドメインに束ね triggers宣言で正規経路へ（既定OFF維持） | 完全実装。mail_constraints/skill.py が NG/budget/deadline/relationship/preference を JSON構造化抽出、生本文非返却(G3 DLP scrub_value マスク後のみLLM投入)、G1本人限定/G2同意fail-closed/G6プロンプトインジェクション対策(メール=データ明示)/G7監査ログ(本文/件名/PII非出力)。factory.py で USE_MAIL_TOOLS フラグ既定OFF・opt-in時のみ ToolSpec登録。専用テストあり(tests/skills/mail_constraints/)。 | ✅ 一致 | src/teamagent/skills/mail_constraints/skill.py:全体(G1-G7死守ライン)／factory.py:101-114(USE_MAIL_TOOLS既定OFF opt-in)／tests/skills/mail_constraints/test_mail_constraints.py | 計画通り正規経路で復活・既定OFF維持・構造化制約のみ。実Gmail接続自体は6c人間ゲート(同意/CASA)承認後に有効化＝コードは完成、本番enable前 |
| 21 | next要件: 本人OAuthでカレンダー/連絡先を検索、未連携は fail-closed、triggers宣言で正規経路へ | 完全実装。workspace_search/skill.py が calendar(GCalendarClient)/people(GPeopleClient)を本人OAuthトークン(TokenStore)で検索、G1本人限定(normalize_email)・G2未連携fail-closed(token None→PermissionError)・G3 DLPマスク・G4 readonly。factory.py で USE_WORKSPACE_TOOLS 既定OFF・RdsTokenStore(KMS)注入。専用テスト(tests/skills/workspace_search/)。 | ✅ 一致 | src/teamagent/skills/workspace_search/skill.py:57-116(本人OAuth/fail-closed)／factory.py:116-130(USE_WORKSPACE_TOOLS既定OFF+TokenStore)／tests/skills/workspace_search/test_workspace_search.py | 計画通り正規経路・fail-closed。本番では既定OFF＋OpenClawのtoolFilterで exclude(per-user境界保護)＝現行Bot/AiLa側に残す設計。コード完成・本番enable前 |
| 22 | next要件P4: 到達不能だった mail_constraints/workspace_search を triggers宣言で正規経路へ復活、fail-closed既定OFF維持 | 達成。両Skillとも @register でレジストリ登録、factory.build_production_tools が USE_MAIL_TOOLS/USE_WORKSPACE_TOOLS フラグで opt-in登録(既定OFF)、run()内で本人限定/同意/未連携を fail-closed強制。到達不能(旧router未配線)は解消し正規ToolSpec経路へ。OpenClaw本番では openclaw.config.json5 toolFilter が mail_*/workspace_search を明示exclude(§G per-user境界を会社共有Botから保護)＝設計上の二重防御。 | ✅ 一致 | factory.py:104-130(opt-in register)／mail_constraints/skill.py:104(@register)／workspace_search/skill.py:42(@register)／infra/openclaw/openclaw.config.json5:104,114(toolFilter exclude mail_*/workspace_search) | 正規経路復活＋fail-closed既定OFF は計画通り完遂。本番では会社共有OpenClaw経路から意図的に除外し per-user経路(AiLa/現行Bot)に温存＝設計通りの未exposeであり未実装ではない |

## 動画分析・VSEO（16項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | 動画リサーチを多経路fallbackで冗長化し可用性を担保 | 未実装。動画は Gemini(Vertex) 直呼びの単経路のまま。gemini_client.py の from_env は Vertex優先 / AI Studio APIキーの2認証経路があるだけで、Bedrock vision や Tavily/Brave/A6X への切替経路は存在しない。一過性エラーのリトライ（_is_retryable_vertex）はあるが別プロバイダへのfallbackではない。 | 🔴 未実装 | src/teamagent/adapters/gemini_client.py:126-164（from_env/_ensure_client＝Vertex or APIキーのみ）。repo全体 grep で video系の bedrock vision/tavily/brave/A6X fallback はヒット0。計画書もP1（可用性要件次第で判断）扱い。 | 多経路fallbackは着手なし。計画上もP1で「要件次第」。現状は単経路で本番E2E到達済みのため可用性問題が顕在化していない。 |
| 2 | Gemini 2.5 Flash 本実装、yt-dlp/FFmpeg、YouTube/TikTok/IG URLパーサ、構造分析prompt、DLせずstream URL経由の著作権ガード | 完成・本番稼働。video/skill.py が YouTube=file_uri直接(DL不要)、TikTok/IG=yt-dlp一時DL→inline→破棄で分岐。adapters に video_download.py(yt-dlp)・video_proxy.py(ffmpeg縮小)・gemini_client.py(2.5 Flash)。prompts/video/v1/system.md に構成/フック/テロップ/尺/CTAの構造分析prompt。SSRF早期拒否(url_guard)も配線。9 tests。Secrets はVertex SA方式(teamagent/dev/vertex_sa)で実装。 | ✅ 一致 | src/teamagent/skills/video/skill.py:71-119（YouTube/DL分岐+url_guard）、adapters/{video_download,video_proxy,gemini_client}.py、prompts/video/v1/system.md、tests/skills/video/test_video_skill.py(9)。FFmpeg Lambda Layer化は採用方式が変わり金庫内MCP(Fargate)で実行。 | 計画の『FFmpeg Lambda Layer化』はアーキ変更でFargate/MCPイメージ内実行に置換（Layer不使用）。規約レビューは著作権ガード(stream URL/即破棄)としてコードに実装済だが正式な法務レビュー記録は未確認。 |
| 3 | scenes[](時刻/要約/テロップ/カメラワーク/推定意図)のスキーマ化、提案転記、Bedrock visionリスク対策 | 強化実装。video_algorithm/schema.py は計画scenes[]を大きく超え、Scene(start/end/desc)に加え TelopItem(sec/kw_match)・BrandDetection(brand_relation=client/competitor)・hook_type/cta_type/cta_sec・LayerMessages・ColorAnalysis 等を時刻付きで構造化。提案転記は report.py(HTML)＋slides.py/pptx_export.py まで到達。ただし計画の『Bedrock visionへのfallback設計』は未実装。 | 🟢 強化 | src/teamagent/skills/video_algorithm/schema.py:65-205（Scene/TelopItem/BrandDetection/VideoVSEOAnalysis）、report.py/slides.py/pptx_export.py。fallbackは grep でヒット0（item1と同根）。 | 『カメラワーク』専用フィールドは無く scene.desc に内包。Bedrock vision fallbackは未実装（規約リスクは著作権ガードで予防的に対応）。 |
| 4 | 3プラットフォームURLをSlackに投げると分析が返るリアクティブ機能 | 本番E2E到達。OpenClaw(頭脳)→MCPツール越しに video_analysis を呼ぶ構成で実装・稼働。移行メモリに 2026-06-12 13:47 の実E2Eログ（@AiLa TikTok検索→Puppeteer10本→gemini_analyze_video via Vertex→Slack回答・エラー0）が commit付きで記録。Slack添付動画bytes直接分析(analyze_bytes)も実装。 | 🔄 方式変更 | src/teamagent/skills/video/skill.py:121-152(analyze_bytes)、orchestrator/factory.py:135-144(USE_VIDEO_TOOLS opt-in公開)、移行メモリ project_teamagent_openclaw_migration.md §🎬✅(13:47 E2E)。計画の単一プロセスBot直結→OpenClaw+MCP境界越し呼び出しに置換。 | 計画の『常駐Bot detect_skill直叩き』方式はOpenClaw agent loop経由に置換。機能としては到達・本番稼働。 |
| 5 | メタ(再生/EG)＋中身(訴求軸等)の2段階構造化を3プラットフォームで | 概ね実装。VideoMeta が play_count/digg/comment/share/collect_count/engagement_rate を保持(メタ層)。VideoVSEOAnalysis が hook/telops/brand/cta/訴求(中身層)。トランスクリプトは TelopItem(焼込テロップOCR)中心で、音声書き起こしの明示フィールドは無い。『登場人物』も専用フィールド無し。 | 🟡 部分 | src/teamagent/skills/video_algorithm/skill.py:170-185(VideoMeta生成)、schema.py:33-205。read_transcript相当の音声transcriptフィールドは schema に無し。 | 音声トランスクリプト/登場人物は専用構造化なし(テロップと映像中身で代替)。video_algorithmはTikTok起点が主で、YouTube/IGメタ収集はvideo_analysis側(メタ統計はTikTokスクレイパ依存)＝3プラットフォーム均等ではない。 |
| 6 | Gemini動画理解APIで直接解析 | 完成・本番稼働。gemini_client.py が analyze_video_url(file_uri)/analyze_video_bytes(inline)で Gemini 2.5 Flash の動画理解を直接呼ぶ。本番はVertex経由で実コスト計上($0.0008/18s実測ログあり)。 | ✅ 一致 | src/teamagent/adapters/gemini_client.py:166-208(analyze_video_url/analyze_video_bytes)、移行メモリ §🎬✅(gemini_analyze_video via Vertex $0.0008/18s実測)。 | なし。 |
| 7 | 類似動画クラスタリング＋競合分析モード(指定アカウント直近10件) | 部分実装。競合判定は client_name を渡し brand_relation=competitor で各動画ごとに判定・report.pyで競合ハイライト表示。横断分析で訴求角度のクラスタ(angle_clusters)あり。ただし『指定アカウントの直近10件を取得して傾向分析する競合分析モード』『VSEO Analytics連携の類似動画クラスタリング』という独立モードは未実装(検索KW起点のみ)。SHOULD項目。 | 🟡 部分 | src/teamagent/skills/video_algorithm/skill.py:216(client_name competitor判定)、report.py:362-365(angle_clusters)/692(_competitor_html)。指定アカウント直近10件取得モードのコードはヒットせず。 | 競合=ブランド検出単位での判定に留まり、アカウント指定→直近10件取得の専用モードは未実装。SHOULDのため優先度低。 |
| 8 | 分析結果をpgvectorに永続化しProposalから参照 | 未実装。動画分析結果は HTML/PPTXレポート＋S3署名URL＋Slack要約として出力されるが、pgvectorへのembed/保存経路は無い。grepで video分析→pgvector/ingest/store はヒット0。Proposal連携も成果物URL受け渡しに留まる。 | 🔴 未実装 | src/teamagent/skills/video_algorithm/skill.py(出力は report_html_path/report_url/slides_url/pptx_url・DB保存なし)。ingest/ にも動画分析の取込コードなし(grep 0件)。 | pgvector永続化と後続Skillからのベクトル参照は着手なし。現状はファイル/S3 URL経由の連携のみ。 |
| 9 | 外部VSEO SaaSをMCP統合 | 未実装。dpro/Buzzmiru/SHORTBOOSTER のどの統合コードも存在しない(grep 0件)。スクレイプは自前(Puppeteer/yt-dlp)で代替。MAY項目(API仕様確認後)。 | 🔴 未実装 | repo grep『dpro\|buzzmiru\|shortbooster』=ヒット0。代替として tools/tiktok_scraper(自前Puppeteer)+yt-dlp。 | 外部SaaS統合は着手なし。MAY項目かつ自前スクレイプで機能代替済みのため不要化の可能性。 |
| 10 | 動画分析Skillとslack_suggest(自動サジェスト)Skillをregisterする | 分割実装。動画分析側は register 済(VideoAnalysisSkill/VideoAlgorithmSkill が factory.py で ToolSpec登録、USE_VIDEO_TOOLS opt-in)。Gemini2.5 Flash確定も一致。一方 slack_suggest(自動サジェスト)Skill は本リポジトリのこの領域には見当たらず別領域/未実装の可能性。性能目標(≤3分/誤検知≤10%)の計測記録は未確認。 | 🟡 部分 | src/teamagent/orchestrator/factory.py:135-151(video_analysis/video_algorithm register)。slack_suggest は video領域skills/には不在。性能ベンチの数値根拠は未発見。 | slack_suggestはこの領域では未確認(別領域audit対象の可能性)。≤3分/誤検知≤10%の定量検証ログは未取得。 |
| 11 | 4プラットフォーム対応の競合PR動画構造分析(構成/フック/テロップ/尺/CTA) | 完成。video_analysis(prompts/video/v1/system.mdが構成/フック/テロップ/編集型/CTAを規定)＋video_algorithm(timecode付き精密構造化)。YouTube=file_uri、Shorts=同経路、TikTok/IG=yt-dlp inline で4プラットフォーム対応。9+28 tests。 | ✅ 一致 | prompts/video/v1/system.md:20-35(フック/テロップ/CTA)、skills/video/skill.py(URL分岐4プラットフォーム)、schema.py(hook_type/telops/cta/duration_sec)。 | なし。 |
| 12 | 上位N本の時刻付き分析→横断勝ち筋→HTMLタイムラインレポート | 完成＋計画超え。video_algorithm が検索→上位N本(over-fetch+バックフィル)→download→proxy→Gemini時刻付き分析→cross_analyze勝ち筋→render_report(HTMLタイムライン)。さらに §M でS3署名URL化(OpenClaw可視)、§Q-HTMLで提案用スライドHTML+PPTX出力(a5ed36a)まで前倒し実装。本番E2E到達。 | 🟢 強化 | src/teamagent/skills/video_algorithm/skill.py:284-373(全パイプライン)、report.py(64KB・HTMLタイムライン)、slides.py/pptx_export.py、移行メモリ §📑VSEO→提案資料 Phase1 MVP完了(a5ed36a/954 tests)、§M(report_url S3署名)。 | なし(計画範囲を超えて提案PPTX出力まで実装)。PPTXは画像方式MVPでネイティブtextbox編集はPhase2残。 |
| 13 | 納品動画をオリエン4観点と照合し合否+指摘、管理番号AND判定 | 完成。video_approval が OrientationBrief(必須シーン/必須テロップ/NG事項/尺指定)と動画をGeminiで照合し verdict(OK/要修正/確認要)+issues生成。Drive/YouTube/TikTok取得対応。sheet_orientation.py(オリエン抽出)・sheet_writeback.py・video_approval_poller.py(Phase2: シート監視→自動審査→Slack通知、既定OFF)。32 tests。 | ✅ 一致 | src/teamagent/skills/video_approval/{skill.py,schema.py:8-93,sheet_orientation.py,sheet_writeback.py}、runtime/video_approval_poller.py、tests(8+14+10)。 | 管理番号(management_no)はpoller冪等キーとして実装。シート書込はspreadsheets再認証までSlack通知に留める設計判断(Phase2制約)。 |
| 14 | 複数動画横断で共通する勝ちパターン仮説を生成 | 完成。synthesis.py が Gemini 2nd pass で横断統計(stats)を根拠に勝ちパターン仮説を生成。n<2スキップ、n<3は全仮説『低』confidence、相関≠因果ガードレール付き。cross_analyze で win_factors/angle_clusters も算出。video側にも synthesize_batch あり。 | 🟢 強化 | src/teamagent/skills/video_algorithm/synthesis.py:1-171(統計根拠の横断シンセシス・有意性ガード)、analysis.py(cross_analyze)、skills/video/skill.py:154-168(synthesize_batch)。 | なし(統計的厳密性のガードレールまで実装)。 |
| 15 | VSEO命名崩れを一掃しドメイン内へヘルパ吸収・name統一 | 部分。video_analysis/video_algorithm/video_approval は各 name 統一済(skill.py の ClassVar name)。ただし vseo/ ディレクトリは依然 skill.py を持たないヘルパ群(covers/dataprep/prepare)として残存し、video_algorithm 配下へは吸収されていない。命名整理は一部のみ進行。 | 🟡 部分 | src/teamagent/skills/vseo/{covers,dataprep,prepare}.py（skill.py不在＝野良ヘルパのまま）。一方 video_algorithm が tiktok_search を内部利用し vseo_reports相当を統合。 | vseo/ヘルパ群のドメイン内吸収・命名一掃(P4)は未完。実害は無いが計画の整理目標は未達。 |
| 16 | 1リクエスト最大20件の重処理ガード | 方式差。video_algorithm の max_videos は Field(default=5, ge=1, le=10)で実質上限10本、検索プールは _MAX_POOL=30、並列は max_workers=3、over-fetch buffer=4。計画の『20件』という具体数とは異なる上限値で実装(より保守的)。ガード自体は存在。env SCRAPE_MAX_VIDEOS clamp も計画(plan §M)に運用安全弁として記載。 | 🔄 方式変更 | src/teamagent/skills/video_algorithm/schema.py:413(max_videos le=10)、skill.py:45-47(_MAX_POOL=30/_OVERFETCH_BUFFER=4)/139(max_workers=3)。plan mossy §M:892(SCRAPE_MAX_VIDEOS clamp運用安全弁)。 | 上限は20件ではなく分析10本/プール30に設定(スクレイパ実証済の保守値)。計画の数値とは不一致だが重処理ガードの趣旨は満たす。 |

## 提案書生成（Proposal Generation）（25項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | Composerを独立Fargateタスクで長時間ジョブ実行(5フェーズ・5〜30分)。当時から『現状は単一プロセス内スキル、Fargateは不採用』とbaseline注記 | Fargateで動くのは OpenClaw外殻(Haiku4.5)とMCP3本(teamagent/video/aiia)。提案生成は専用Fargateジョブではなく、MCPツール(proposal_draft/review)としてMCPコンテナ内で同期実行。長時間5フェーズパイプラインは未配線(設計のみ=html-agent-floofy-fountain.md) | 🔄 方式変更 | factory.py:71-99(build_production_tools=同期ToolSpec)、mcp_gateway/server.py:253-256、計画書html-agent-floofy-fountain.md(5ステージは設計段階)、移行計画書mossy-snacking-locket.md(OpenClaw/Fargate採用) | 5フェーズ長時間ジョブ化(非同期ジョブ)は未実装。proposal_draft/reviewは単発同期呼び出しで完結 |
| 2 | 5フェーズ生成パイプライン、Playwright+WeasyPrint動作確認、HTML→PDF/PPTX変換Lambda Layer、ChromeOS/Edge互換検証、Drive Service Account権限委譲、Drive保存→Slack DMリンク返却 | 別系統で部分到達。proposal_deck(TeamAgentリポ)が商材+research→95枠→python-pptxでPPTX生成(self-repair付)を実装。VSEO側(orchestrator-poc)はslides.py(HTML)+pptx_export.py(playwright→PPTX)がPhase1完了(commit a5ed36a・954tests)。ただし5フェーズ統合パイプライン・WeasyPrint・PDF変換Lambda・Drive自動保存→DMリンクは未配線 | 🟡 部分 | proposal_deck/skill.py+renderer.py(python-pptx)、video_algorithm/slides.py・pptx_export.py、計画書html-agent-floofy-fountain.md L119-121(Phase1完了・未デプロイの最終一手=p2u) | 5フェーズ統合・WeasyPrint・PDF/PPTX変換Lambda・Drive保存→Slack DMリンク自動返却フローは未実装。PPTX公開は publish_pptx_file 止まり(本番未デプロイ=p2u apply待ち) |
| 3 | 2026-10中旬にゲート②マイルストーンで工数測定レポート+経営判断 | 現状(6/15)はパイロット前段階。ゲート②(提案工数実証)は未実施。通過済みはゲート①(OpenClaw採用=B案)のみ。本番E2E実返信は到達も、提案書工数のBefore/After対照実験は着手なし | 🔴 未実装 | 移行計画書mossy-snacking-locket.md(ゲート①通過・パイロット前段階)、現状ダイジェスト(go-live=6/12だが2人混線/1週間パイロット未) | 営業5名×3件の工数測定・実証レポート・経営判断は全て未着手。時期(2026-10)も未到来 |
| 4 | 既存提案から類似業種・類似予算のテンプレ抽出、Claude Sonnet4.6でドラフト生成→営業レビュー(20h→12-15h中間計測) | ProposalDraftSkill 実装済(proposal/skill.py)。SearchSkill.retrieve_hitsで類似過去提案/FB検索(filter_industry対応)→Bedrock Sonnet4.6でドラフト骨子生成→根拠(sources)付き返却。factory.pyでproposal_draftとして本番配線、openclaw config toolFilter.include に登録=本番MCP経路で露出 | ✅ 一致 | skills/proposal/skill.py:31-122(ProposalDraftSkill・Sonnet4.6・SearchSkill再利用)、factory.py:87-92(本番配線)、openclaw.config.json5:110(toolFilter include proposal_draft) | 予算フィルタは未実装(industryのみ)。中間工数計測(12-15h)は未実施。intent.py分岐は別途要確認だが本番MCP経路では稼働 |
| 5 | Skill⑥営業ログ自動生成、Skill⑦提案レビューBot(提案PDFを診断) | Skill⑦=ProposalReviewSkill 実装済(proposal_review/skill.py)。提案テキストを過去勝ちパターン/失注理由と照合しコードレビュー的診断、Sonnet4.6、本番配線+toolFilter登録済。ただし入力はproposal_text(テキスト)でありPDF直接診断ではない。Skill⑥(営業ログ→CRM)はこの領域では未実装(別ドメイン) | 🟡 部分 | skills/proposal_review/skill.py:29-124(ProposalReviewSkill・コードレビュー的診断)、factory.py:93-98、openclaw.config.json5:110(proposal_review include) | Skill⑦はPDFでなくテキスト入力。Skill⑥営業活動ログ自動生成は提案領域外で未確認/未実装 |
| 6 | 対照実験で作成時間+検索top-3 hit rate+満足度+コスト+レイテンシの5指標を測定しGo/No-Go判定 | 検索基盤(hit rate/Rerank)は実装・gold setで計測実績ありだが、提案工数の対照実験と5指標統合判定は未実施。時期未到来 | 🔴 未実装 | 現状ダイジェスト(パイロット前段階)、移行計画書(ゲート①通過のみ)、要件はteamagent_implementation_plan_v3.2_draftに計画記載 | 提案書作成時間のBefore/After実験・満足度reaction測定・統合判定レポートは未着手 |
| 7 | Phase0-Eの5フェーズ対話型ワークフロー(MUST) | 現状は単発の proposal_draft(検索→ドラフト生成)止まり。Phase0要件ヒアリング/B並列リサーチ/D対話型仕上げ/E最終調整の対話ワークフローは未実装。5ステージ設計はhtml-agent-floofy-fountain.mdで確定済(A根拠合成/B生成/C自己反証/D AIFMT/E人用FMT)だが配線はP1-P5の段階実装で未着手 | 🟡 部分 | skills/proposal/skill.py(単発draft)、計画書html-agent-floofy-fountain.md L70-97(5ステージ設計・段階実装P0-P5未着手) | 対話型5フェーズ(Phase0-E)は設計のみ。実装はproposal_draft単発に留まる |
| 8 | 6データ層をTool並列呼び出し、Gemini Deep Research経由Web検索を別API呼び出し(MUST並列+SHOULD別API) | research Action(素材取込の並列リサーチ)はスキルとして未実装。DRは計画書で『最新性の1レイヤーに格下げ』方針に再設計されたが配線なし。並列実行パターン(orchestrator/multi.py ThreadPool)は他用途で存在するが提案リサーチには未適用 | 🔴 未実装 | research skillディレクトリ不在(find結果0)、計画書html-agent-floofy-fountain.md L17-31(新手法=穴ごと最適ソース振分・DR格下げ)、next_requirements L57(research=4 Actionの一つ・未統合) | 6データ層並列リサーチ・Gemini DR別API統合は未実装(設計のみ) |
| 9 | 生成文の主張にTier0-3引用ラベル必須、引用率90%/捏造率0%をKPI化(MUST) | 部分的に基盤あり。proposal_deck contract.py に citations_per_placeholder(placeholder毎の引用ID)フィールド+ID妥当性validation。proposal_draft/reviewはsources(根拠chunk)を構造化返却。next_requirementsで『faithfulnessを受け入れ基準に』『出典必須化fail-honest』を方針化。ただしTier0-3ラベル体系・引用率90%/捏造率0%の自動KPI計測enforcementは未実装 | 🟡 部分 | proposal_deck/contract.py:71,85-90(citations_per_placeholder+validation)、proposal/schema.py(sources構造化)、next_requirements L45,57,87(faithfulness受入基準=計画) | Tier0-3引用ラベル分類・引用率90%/捏造率0%のKPI自動測定は未実装。引用フィールドは存在するが率の強制なし |
| 10 | HTML標準でPDF/PPTX変換可能、会社既定テンプレ(HTML+PPTX)を初期スタイル適用(MUST) | 二系統で部分到達。VSEO側=slides.py(16:9 HTML標準・contenteditable)+pptx_export.py(playwright→PPTX)、PDF未対応。proposal_deck側=会社既定PPTXテンプレ(TEAMAGENT_FMT_TEMPLATE)へpython-pptx流し込み(renderer.py・全角/半角プレースホルダ対応)。『構造化データ単一真実源→2レンダラ分岐』は設計確定も統合配線は未完 | 🟡 部分 | video_algorithm/slides.py(HTML標準・16:9)、pptx_export.py(playwright→PPTX)、proposal_deck/renderer.py(会社PPTXテンプレ流し込み)、計画書 L11-14(2レンダラ設計) | PDF変換は未実装。HTML↔PPTXの単一真実源2レンダラ統合は設計のみ。会社HTMLテンプレ初期スタイルはVSEO slidesに限定 |
| 11 | 5フェーズ数分処理の進捗をSlackに逐次表示(MUST) | 5フェーズ長時間処理自体が未実装のため、その逐次進捗表示も未実装。OpenClaw移行で受付リアクション/話題復唱等の会話UXは入った(別文脈)が、提案生成専用の段階別進捗ストリーミングは無い | 🔴 未実装 | 計画書html-agent-floofy-fountain.md(5ステージ未配線)、移行計画書(OpenClawで受付リアクション=§T1。提案進捗専用表示は記載なし) | 5フェーズ進捗のSlack逐次表示は未実装(前提の長時間パイプラインが未配線) |
| 12 | 過去提案の自動レコメンド、章構成テンプレ、提案書全体の版管理(SHOULD) | 過去提案レコメンドは proposal_draft の類似検索(SearchSkill)で実質充足。章構成テンプレは proposal_deck の95枠10セクション契約(A.メタ〜J.動画案)が会社FMTとして確定・実装。版管理は未実装 | 🟡 部分 | proposal/skill.py(類似検索=レコメンド相当)、proposal_deck/contract.py(95枠10セクション=章構成)、計画書 L36-48(10セクション定義) | 提案書全体の版管理(バージョン管理)は未実装。レコメンドは検索ベースで明示的レコメンドUIではない |
| 13 | 音声入力からの要件取得と競合過去事例分析(MAY) | いずれも未実装。音声入力Phase0・競合事例分析機能は提案スキルに存在しない | 🔴 未実装 | proposal/skill.py・proposal_deck/skill.py に該当機能なし(コード確認)、計画書にも音声/競合分析の配線記載なし | 音声入力・競合分析ともに着手なし(MAY優先度) |
| 14 | 毎週月曜9:00にマネージャーDM配信、各営業の動き/案件進捗/想定リスクの3軸(MUST) | 未実装。progress/summary/weeklyスキルは両リポに存在せず。EventBridge等の週次スケジューラ常設も無し(ingestは本日systemd timer化したが提案サマリーとは別) | 🔴 未実装 | find結果0(summary/weeklyディレクトリ不在)、infra grep結果0(eventbridge/weekly無し)、現状ダイジェスト(EventBridge常設なし) | 週次進捗サマリーDM配信は機能・スケジューラとも未着手 |
| 15 | Mail(本人/他人)+Slack(共有CH)+Drive更新履歴を集約、リスク検知(MUST) | 未実装。サマリー機能自体が無いため、そのデータソース統合・リスク検知も無し | 🔴 未実装 | summaryスキル不在(find結果0)、リスク検知ロジックのコード無し | 全て未着手 |
| 16 | ドリルダウン追問/月次レポート(SHOULD)、セルフレポート(MAY) | 未実装。サマリー基盤が無いため拡張機能も全て未着手 | 🔴 未実装 | summaryスキル不在(find結果0) | 全て未着手 |
| 17 | HTML標準+Playwright HTML→PDF(A4/レター)+HTML章↔PPTXスライド1:1マッピング(MUST・v3.0新規) | 部分到達。slides.py=HTML標準16:9、pptx_export.py=playwright→PPTX(本番未デプロイ)。PDF(A4/レター)変換は未実装。HTML↔PPTX 1:1マッピングは『単一真実源→2レンダラ』設計で確定もslides(VSEO)とrenderer(deck)は別系統で未統合 | 🟡 部分 | video_algorithm/slides.py・pptx_export.py(playwright→PPTX)、計画書 L11-14・L133-135(2レンダラ同データ設計=Phase4)、PDF経路コード無し | PDF変換未実装。HTMLテンプレ↔PPTXテンプレ2層同期の1:1マッピング統合は設計のみ(2系統が分離) |
| 18 | Drive保存→Slackスレッドpermalink返却(MUST)、SVG/Base64 PNG埋め込み(SHOULD) | SVG/Base64埋め込みは実装(slides.pyはcover_data_uri=Base64・report.pyもbase64埋込で外部依存ゼロ)。VSEO側にpublish_pptx_file/report_publish(S3署名URL化)あり。ただし提案書のDrive自動保存→Slackスレッドpermalink返却フローは本番未配線(p2uデプロイ待ち) | 🟡 部分 | video_algorithm/slides.py(Base64 data_uri)、adapters/report_publish.py(S3署名URL=commit e4d17a7)、計画書 L121(publish_pptx本番未デプロイ) | Drive保存→Slack permalink返却の本番フローは未デプロイ。現状はS3署名URL中心 |
| 19 | 差分表示/会社マスタ(SHOULD)、docx(MAY) | PPTX会社マスタは proposal_deck が会社既定FMTテンプレ(TEAMAGENT_FMT_TEMPLATE)に流し込みで実質充足。HTMLプレビュー差分表示・docxエクスポートは未実装。slides.pyはcontenteditable編集可だが差分(編集前後比較)機能は無し | 🟡 部分 | proposal_deck/renderer.py(会社PPTXテンプレ流し込み)、slides.py(contenteditableのみ・差分機能なし)、docx経路コード無し | 差分表示・docxエクスポートは未実装。会社マスタはdeck側で対応 |
| 20 | draft_proposalをregister、pptx skillでPPTX初稿を5分以内生成(着手前) | 実装方式が変化して到達。proposal_draftはregister済+本番配線。PPTX生成はAnthropic pptx skillでなく自前 proposal_deck(python-pptx renderer)+VSEO pptx_export(playwright)で実装。5分以内/流用率≥40%の実測は未検証 | 🔄 方式変更 | proposal_deck/renderer.py(python-pptx・Anthropic skill不使用)、video_algorithm/pptx_export.py(playwright)、計画書(85MB実deck生成実証=teamagent_consulting) | PPTX生成エンジンがAnthropic pptx skillから自前実装へ変更。5分以内・流用率40%の定量検証は未実施 |
| 21 | proposal_draftをAction化、Knowledge.retrieve共有注入で類似過去提案を再利用 | ProposalDraftSkill実装済・SearchSkill(会社pgvector RLS共有検索)で類似過去提案retrieve→ドラフト生成。本番MCP経路で稼働(toolFilter include)。『1ドメイン4Action統一』のAction粒度統合(dir=proposal/name=proposal_draftのズレ解消)はP4計画で未実施だが、機能本体は動作 | ✅ 一致 | proposal/skill.py:59-64(SearchSkill.retrieve_hits共有注入)、factory.py:87-92、next_requirements L57,87,192(4Action統一=P4計画) | 4Action統一(命名/粒度リファクタ)は未着手(P4計画)。機能としては稼働 |
| 22 | proposal_reviewをAction化、勝ちパターン/失注理由照合のコードレビュー的FB | ProposalReviewSkill実装済・提案テキストを過去勝ち筋/失注理由と照合しコードレビュー的診断、本番MCP経路で稼働(toolFilter include)。機能本体は計画通り動作 | ✅ 一致 | proposal_review/skill.py:29-124、factory.py:93-98、openclaw.config.json5:110(proposal_review include) | 4Action統一リファクタは未着手。PDF直接入力でなくテキスト入力。機能は稼働 |
| 23 | 別ブランチfeat/proposal-deck-skillで作業中、FMT v2 95項目PPTX自動生成、P4で正規経路へ昇格 | ProposalDeckSkill コード完成(TeamAgentリポ・branch feat/proposal-deck-skill・commit 58ec55d/9374bec)。95枠{1}-{103}欠番{48-55}契約・citations・文字数validation・self-repair・python-pptx renderer実装。@register済。ただし(1)本番orchestrator-poc側のbuild_production_toolsには未収録(2)openclaw config toolFilterで明示EXCLUDE(3)intent.py分岐ゼロ=Slack自然文から到達不能。TeamAgentリポ側factory.pyには配線ありだが本番MCP経路(orchestrator-poc)ではない | 🟡 部分 | TeamAgent proposal_deck/skill.py・contract.py:18(欠番48-55)・renderer.py、openclaw.config.json5:114(exclude proposal_deck)、next_requirements L30(到達不能)・L192(P4で正規経路=計画)、orchestrator-poc factory.pyにproposal_deck無し | 本番配線(orchestrator-poc factory+toolFilter)は未。明示的にexcludeされ到達不能。P4の『正規経路へ』は未実施。85MB実deck生成はteamagent_consultingでの実証段階 |
| 24 | researchをAction化、研究素材取込+self-repairで契約充足 | researchという独立Actionスキルは未実装(両リポにresearchディレクトリ無し)。研究素材は現状proposal_deckのresearch_material引数にAgent側が他スキル(search等)で集めて渡す前提。self-repairはproposal_deckのComposer内に存在するが、research取込専用Actionは無い | 🔴 未実装 | researchスキルディレクトリ不在(find結果0)、proposal_deck/schema.py:22(research_material=外部から渡す引数)、計画書 L76(A.根拠合成=search+DR+VSEO設計のみ) | research Actionスキルは未実装。素材集めはAgentの手動オーケストレーション任せ |
| 25 | P4でproposal_draft/review/deck/researchを1ドメイン4Actionに束ね、dir=proposal/name=proposal_draftの粒度ズレ解消、faithfulnessを受入基準化 | 未実施。現状はdraft/reviewが個別Skillとして別dir(proposal/proposal_review)で稼働、deckは別リポ・到達不能、researchは未実装。4Action統一リファクタ・命名規約統一・faithfulness受入基準のenforcementは全てP4計画のまま着手なし | 🔴 未実装 | next_requirements L57,63,87,192,208,211(P4計画・着手前)、proposal/proposal_review別dir(命名ズレ未解消)、現状ブランチ feat/proposal-deck-skill で作業中(L211) | ドメイン統合リファクタ全体が未着手(P4)。faithfulnessはcitations構造はあるが受入ゲート未配線 |

## セキュリティ・権限分離（35項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | IAM最小権限/KMS暗号化/監査ログ/opt-out/PIIフィルタを基本線として整備 | 全項目実装済み＋計画外の上積みが多数: ① IAM最小権限=fargate.tf でタスクロール3分離(OpenClaw=bedrock:InvokeModelのみ＋Secrets/KMS/RDS/DynamoDB明示Deny、MCP=対象Secret/KMS Decrypt/rds-connect限定) ② KMS=security.tf CMK(rotation有効)でCloudTrail/Bedrockログ暗号化、oauth_token_store.py KmsCipher が refresh token を per-user EncryptionContext付きで暗号化 ③ audit_log=CloudTrail multi-region+log file validation、mcp_gateway が identity_resolved/identity_spoof_rejected を構造化ログ ④ PIIフィルタ=observability/sentry.py が secret/PII(メール/電話)を redact、scripts/pii_log_scan.py。さらに Postgres RLS(0001/0006)・per-user OAuth・IMDSv2必須(worker.tf:143)・RDS force_ssl+storage_encrypted+IAM auth(rds.tf)・監査報告書(docs/security/security_audit_2026-06-12.md) | 🟢 強化 | infra/terraform/{fargate.tf:166-187,security.tf:14-75,rds.tf:39-120,worker.tf:142-143}, src/teamagent/adapters/oauth_token_store.py:84-118, observability/sentry.py, scripts/pii_log_scan.py, docs/security/security_audit_2026-06-12.md | — |
| 2 | IT部門への申請4件(sudo・プロキシ許可リスト・Slackポリシー・RDS接続元/SSO)。未着手(🔴) | OpenClaw移行で前提が大きく変化。Slack chat:write.public はsocket mode(outbound WebSocketのみ・公開EP不要)で不要化(config: mode socket)。本番RDS接続元はFargate MCPタスクSGに限定(fargate.tf:291)＝固定IP/SSO申請の代替。Bedrockプロキシは VPCエンドポイント(vpc_endpoints.tf)/AWS内CodeBuildビルドでproxy回避(commit 253db0f)。一方 OAuthコールバック用の社内正式エンドポイントは情シスへ依頼済み・未確定(security_audit §4)で、IT連携の一部は依然オープン | 🔄 方式変更 | infra/openclaw/openclaw.config.json5:41(socket), infra/terraform/{fargate.tf:291,vpc_endpoints.tf}, commit 253db0f, docs/security/security_audit_2026-06-12.md §4(社内EP依頼済・未確定) | — |
| 3 | Day3で security.tf/rds.tf/pii_log_scan.py 完了済(rotation自動化LambdaのみSprint14) | 計画通り全件コードで存在を実証: rotation marker secret(NextRotationDueタグ=2026-08-22)＝security.tf:248-258、RDS force_ssl+iam_database_authentication_enabled+storage_encrypted=rds.tf:39-120、S3 PublicAccessBlock+aws:kms暗号化=security.tf:84-102(cloudtrail/bedrock_logs)、IAM Access Analyzer=security.tf:163-168、CloudTrail multi-region+enable_log_file_validation=security.tf:139-160、PII漏洩スキャン=scripts/pii_log_scan.py。rotation自動化Lambdaは予定通り未実装(Sprint14) | ✅ 一致 | infra/terraform/security.tf:84-258, rds.tf:39-120, scripts/pii_log_scan.py | — |
| 4 | 情シス向け資料+データフロー図、個人情報/商談機密のコンプライアンス確認(🤖+🏢+👤) | 情シス提出用のセキュリティ監査報告書(docs/security/security_audit_2026-06-12.md)が§2にセキュリティ境界のデータフロー図・4観点read-only実測監査結果・最新事例適用状況まで含めて作成済み＝Sprint6相当成果を計画前倒しで産出。ただし社内コンプライアンス部門の正式レビュー/承認(🏢)は文書化のみで実施記録なし(報告書は『情シス提出用』段階) | 🟢 前倒し | docs/security/security_audit_2026-06-12.md(commit dc0c002, §1-6), 図=§2 セキュリティ境界 | — |
| 5 | Sprint14(11/28〜12/28)で実施予定 | ログマスキング(sentry redact/pii_log_scan)とCVEスキャンの一部(CI bandit、MCP SDK CVE-2026-52869/52870対応・1.27.2固定、OS脆弱性trivy追跡)は前倒し着手。一方 Macieによる本番PII検出/Snyk+Dependabot常設/Slack・Gmail・Geminiのtoken rotation は未実装。露出Slackトークン3本のrotationはパイロット前必達として明記されるも未実施(security_audit §4・移行計画§T 259行) | 🟡 部分 | ログマスク=sentry.py/pii_log_scan.py(済), CVE=security_audit §3#2, gap=Macie/Snyk/Dependabot/token rotation 未着手, mossy-snacking-locket.md:259 | — |
| 6 | Day3-4でbot/app tokenをローテーションしSecrets Manager上書き・再疎通 | 未実施。構築中に作業端末/チャットで扱ったトークン3本(bot/app/user)の再発行が未完。漏えい形跡なしと監査で確認済みだが衛生上『パイロット開始前に必須』として明示的に残課題化。新トークンはSecrets Manager(teamagent/dev/openclaw/slack-bot-token等)に投入済みだがrotationそのものは保留 | 🔴 未実装 | docs/security/security_audit_2026-06-12.md §4(rotation未実施), mossy-snacking-locket.md:18,31,259(露出トークン3本rotation必須・パイロット完了時までに) | — |
| 7 | openclaw.json で clawhub.disabled:true(ClawHavoc/サプライチェーン対策) | より強い形で達成。マーケット由来拡張を一切導入しない方針＝公式プラグイン2点のみをビルド時固定(イメージdigest pin)、ClawHub経由の動的install経路そのものが本番に存在しない。openclaw.config.json5 にも clawhub サーバ登録なし。監査§5で『マーケット由来拡張ゼロ』をポリシー化。なおconfig上の明示的 disabled:true フラグ行は現テンプレに無く、構造的非導入(digest pin+toolFilter+exec deny)で代替 | 🟢 強化 | infra/openclaw/docker-compose.yml:25(digest pin), openclaw.config.json5(clawhub登録なし), docs/security/security_audit_2026-06-12.md §5-1 | — |
| 8 | ヒアリング+セキュリティ部レビュー期間。サンドボックス/最小権限/ClawScan/インシデントロールバック | 技術的な封じ込めは実装済み: tools.exec.mode:deny/fs.workspaceOnly/browser private遮断(config:69-92)、read_only rootfs+cap_drop ALL+no-new-privileges+pids/mem制限+internal私設網(docker-compose.yml)、IAM Deny、敵対ハーネス attack_mcp.py(インジェクション/SSRF)/smoke_mcp.py が回帰検証。ロールバックは env flag/toolFilter即OFFで1分(next_req §8)。ただし『社内セキュリティ部による正式レビュー』自体の実施記録はなく、提出用監査報告書の作成段階 | 🟡 部分 | openclaw.config.json5:69-92, docker-compose.yml:29-134, scripts/{attack_mcp.py,smoke_mcp.py}, gap=社内セキュリティ部の正式レビュー未記録 | — |
| 9 | B案採用時: APIキー+mTLS、teamagent-search SKILL.md、Pact契約テストでI/O互換 | B案(OpenClaw採用)確定。認証はstreamable-http MCP境界のbearer認証(Authorization: Bearer ${TEAMAGENT_MCP_BEARER})＋internal私設網(mcpnet internal:true)＋SG発信元限定で実装＝APIキー相当をbearer+ネット分離で代替(mTLSは未採用・VPC内部閉域で代替)。FastAPIではなくMCP spec-server(mcp_gateway/server.py)に変更。SKILL.md形式でなくMCP toolFilterで露出制御。Pact契約テストは未導入(test_mcp_gateway_server.py等の単体テストで代替) | 🔄 方式変更 | openclaw.config.json5:94-118(bearer/streamable-http), docker-compose.yml:134(internal網), src/teamagent/mcp_gateway/server.py, gap=mTLS/Pact未採用 | — |
| 10 | Skill単位RBAC(MUST) | Skill露出は toolFilter.include/exclude＋tools.profile:messaging の三層で制御し、per-user OAuth依存(mail_*/workspace_search)・書込/確定系は除外。RLSでドキュメントレベルのACL(owner/acl_emails/acl_groups)制御。ただし計画が想定した『ユーザー/ロール/案件の細粒度RBACマトリクス』は未実装＝現状は全員member固定(role昇格不可)＋会社共有モデル(§G)で横連携全員可視。案件単位の実行権限制御は未実装 | 🟡 部分 | openclaw.config.json5:108-115(toolFilter), identity.py:95-112(member固定/会社共有), gap=ユーザー×ロール×案件の細粒度RBACは未実装 | — |
| 11 | マネージャーへのDM配信対象をSlackグループで制御(MUST) | DM要約機能(横断/トリアージ系)自体がAiLa別プロセス側で、本OpenClaw前面には未結線。DM受信は dmPolicy:allowlist+allowFrom でユーザーID制御(社外ゲスト遮断目的)するが、これは『マネージャー権限のグループ制御』とは別目的。マネージャーグループ単位の配信制御は本repoに実装なし | 🔴 未実装 | openclaw.config.json5:56-59(dmPolicy allowlist=用途は社外遮断), gap=マネージャーグループ配信制御は別経路(AiLa)で本repo未配線 | — |
| 12 | 全ToolにPermissionチェック(GR-1)、初期は読取専用、書込は段階解禁(MUST) | 計画通り。mcp_gateway の dispatch_tool が全tool呼出で require_rls fail-closed の身元解決を必須化、OC boundary の toolFilter.exclude が *write*/*confirm*/*reply*/proposal_deck を除外＝読取専用ツールのみ露出。書込はP2本番のHITL解放まで段階封鎖。role は常にmember導出でadmin昇格構造的に不可 | ✅ 一致 | mcp_gateway/server.py:162-209(全tool RLS), openclaw.config.json5:114(exclude write/confirm/reply), identity.py:25(member固定) | — |
| 13 | Tier0-3ラベルベース、Slack user IDをキーにRBAC(MUST) | Slack user IDキーの本人解決→RLS(documents/oauth_tokens 行レベル)は完全実装。ただし制御軸は『Tier0-3信頼度ラベル』ではなく owner_email/acl_emails/acl_groups のACL方式に変更。Tier0-3のIAM Role分離(item27)はS12本番化フェーズの計画のまま着手なし。案件単位制限はclient_code列はあるがRLS強制には未組込 | 🔄 方式変更 | identity.py:115-162, migrations/0001:118-141(ACL方式RLS), gap=Tier0-3ラベル制御は未実装(S12計画) | — |
| 14 | 生成文出力前にPIIマスク(MUST) | PIIマスクはログ/Sentry/監査経路(observability/sentry.py の _PII_PATTERNS でメール・電話を[REDACTED]、usage_recorderは本文/PIIを列に持たない)に実装。しかし『生成文(ユーザー回答)に出る前の出口マスク』＝GR-2本来のスコープは未実装。住所パターンも未対応。会社共有ナレッジ検索が主用途のため優先度は下がるが、生成時マスクは差分 | 🟡 部分 | observability/sentry.py:52-67(ログ向けmask), usage_recorder.py:8(本文非保持), gap=生成文出口のPIIマスク・住所未実装 | — |
| 15 | Tier0(一次)〜Tier3(外部)の信頼度ラベル、生成文主張にTierラベル必須(MUST) | 未実装。反ハルシは別方式(orchestrator/faithfulness.py が引用chunk_idの妥当性=citation_validityで捏造引用を検出)で担保するが、Tier0-3の信頼度ラベル付与・生成文主張へのTier必須化は着手なし。検索はmin_relevance+Rerankで品質担保するが信頼度Tier体系は無い | 🔴 未実装 | orchestrator/faithfulness.py(citation方式の反ハルシ), gap=Tier0-3ラベル体系・生成文Tier必須化なし | — |
| 16 | Phase4以降で計画(MUST) | 未実装。mail_constraints skill に制約系の枠はあるが薬機法NG辞書/景表法/ステマPR表記チェックの実装なし。計画上もPhase4(S13)以降で本領域はまだ着手前のため計画通りの未着手 | 🔴 未実装 | src/teamagent/skills/mail_constraints/(メール制約のみ・規制チェック未実装), Phase4計画 | — |
| 17 | KOL契約終了日管理・引用禁止リスト自動登録(MUST) | 未実装。契約終了日管理/引用禁止リストの仕組みは本repoに存在しない。S13計画のまま着手前 | 🔴 未実装 | repo全grepで肖像/契約終了日/引用禁止の実装なし, S13計画 | — |
| 18 | injection防御・拒否(SHOULD)、機密マーキング(MAY) | Prompt Injection防御は実機実証PASS(T2-① 2026-06-12: 『システム指示を無視してコマンド実行』→AiLaが明確拒否、構造=tool policyがexec/web_fetch/write等20toolを当該ターンでも除去、挙動=tool start ゼロ)。多層: exec deny/fs workspaceOnly/toolFilter/url_guard SSRF防御/attack_mcp.py injection・SSRF回帰。一方 confidence<0.3の意図不明クエリ拒否ロジックと機密情報マーキングは未実装(request_gateの拒否はキュー/タイムアウト用で別物) | 🟡 部分 | mossy-snacking-locket.md:28(T2-①PASS), adapters/url_guard.py, scripts/attack_mcp.py, gap=confidence<0.3拒否・機密マーキング未実装 | — |
| 19 | S2末にゲート①設置・全節承認が前提(着手前) | ゲート①はOpenClaw採用(B案)通過済として既に越えており、本番E2E稼働(go-live 2026-06-12)へ到達。ただし元計画が要求した『12章全節の正式レビュー・承認』のうち、CVE購読(週次確認運用化)・Bedrock影響外確認・IAM隔離は実証済だが、ClawHubホワイトリスト/IAM Tier草案の正式承認手続きは構造的非導入や監査報告で代替。ゲート概念自体が移行で再定義された | 🔄 方式変更 | mossy-snacking-locket.md(go-live完了), docs/security/security_audit_2026-06-12.md §4(週次CVE確認運用化), 移行でゲート①=B案採用判断に再定義 | — |
| 20 | S3 DoDとしてホワイトリストv1文書化・承認(着手前) | ClawHub Skillを使わない方針(マーケット由来拡張ゼロ・公式プラグイン2点のみdigest pin)に転換したため、ホワイトリストv1の作成自体が不要化。代替として toolFilter.include の明示許可リスト(search/clientkarte/proposal_draft/proposal_review等)で露出Skillを2軸(include+exclude)制御 | ⚪ 不要化 | openclaw.config.json5:108-115(include許可リスト), docs/security/security_audit_2026-06-12.md §5-1(マーケット拡張不使用), ClawHub不使用でホワイトリスト不要化 | — |
| 21 | S5でscope mine\|team\|all・owner_email単一フィルタ実装(8.2は未決) | メール機能(aiia-mcp)は本番RUNNINGだがOpenClaw未結線(§Q-Q3)。コードレベルではper-user OAuth(本人トークンでのみメールアクセス＝越権が原理的に不可・oauth_token_store.py)とRLS本人行のみで『他人のメールは見えない』を構造保証。ただし scope=mine\|team\|all の明示スコープ切替パラメータの実装は本repoに見当たらず＝本人限定(mine相当)のみ。team/all scopeは未実装 | 🟡 部分 | oauth_token_store.py:1-9,121-154(per-user本人限定), migrations/0006(本人行RLS), gap=scope mine\|team\|all切替未実装・OpenClaw未結線 | — |
| 22 | S12(M6.25-6.5)から正式運用開始(着手前) | 本番化フェーズ未到達(現在パイロット前段階)。CVE監視は週次確認の運用化を計画(security_audit §4)・購読はDay0実施。ClawHub四半期レビューは不使用で不要化。IAM Tier0-3切替は未実装。S12正式運用は計画のまま | 🔴 未実装 | docs/security/security_audit_2026-06-12.md §4(週次確認は予定), gap=S12正式運用未到達・IAM Tier切替なし | — |
| 23 | v1.1で確定・運用必須ルール、S12初回再レビュー予定 | ClawHub不使用方針に転換し5軸スコア/四半期再レビューの運用自体が不要化。代替の供給審査ゲート(版pin+権限宣言レビュー+署名検証=item31)はnext_req P5の保険レイヤとして将来計画化 | ⚪ 不要化 | docs/security/security_audit_2026-06-12.md §5-1, docs/next/teamagent_next_requirements.md:159,193(P5供給ゲート), ClawHub不使用で5軸レビュー不要化 | — |
| 24 | Day0購読・S12正式運用、CIにtrivy/snyk | GHSA一次確認(2026-06-09に gh で30+件直確認)・採用版2026.6.5 digest pin・MCP SDK CVE対応(1.27.2固定)・OSパッケージ脆弱性trivy追跡まで実施。パッチSLAは『High/Critical発出時48h以内にピン更新→検証→再デプロイ』として監査§4で運用化宣言。一方CIへのtrivy/snyk常設組込は未配線(CIはbandit/gitleaksのみ)。週次確認は運用化予定段階 | 🟡 部分 | docs/security/security_audit_2026-06-12.md §3#2,§4, mossy-snacking-locket.md:226(GHSA直確認), .github/workflows/ci.yml(trivy/snyk未配線), gap=CI trivy/snyk常設なし | — |
| 25 | 登録前静的解析〜本番egress監視まで運用ルール化 | bandit静的解析はCI/pre-commit常設(ci.yml:60 bandit -r src/teamagent -ll)。egressはVPCエンドポイント+internal私設網(docker-compose mcpnet internal:true)+IMDSv2でネット面制御。ただし strace監査/VPC Flow Logs異常egressアラート/Skill register-unregister-versionのaudit_log記録は未実装。マーケットSkill登録運用自体が不使用で大半が不要化 | 🟡 部分 | .github/workflows/ci.yml:60(bandit), docker-compose.yml:134(internal網), gap=strace/VPC Flow Logs/register audit_log未実装(Skill登録不使用で一部不要) | — |
| 26 | Runbook化・S13で切替≤1h確認(運用化はMVA-12未決) | 主経路Bedrock(IAM/aws-sdk・APIキー不要)は本番稼働。副経路はVSEO/動画分析で Gemini Vertex SA方式を実運用(commit 027e763)＝副B(Vertex)の実装実績あり。ただし『Anthropic直接API副経路A』のRunbook化とS13切替≤1h訓練は未実施(S13計画のまま) | 🟡 部分 | openclaw.config.json5:120-150(Bedrock主), commit 027e763(Vertex SA), gap=副A Runbook・S13切替訓練未実施 | — |
| 27 | S12でTier0-3切替本番化(デフォルトTier0・Tier1以上申請制) | IAM Roleによる権限分離はタスク粒度で実装(OpenClaw=最小・MCP=金庫アクセス、fargate.tf)＝外殻/境界の構造分離は達成。しかし計画が要求した『Tier0-3のSkill別requiredTier宣言・AssumeRole実行・Tier別IAM Policy』の細粒度は未実装。現状は2タスクロールの粗粒度分離+RLS。Tier体系はS12計画のまま | 🟡 部分 | infra/terraform/fargate.tf:166-234(2ロール分離), gap=Skill別requiredTier/AssumeRole/Tier別Policy未実装 | — |
| 28 | P3でPolicyEngineに集約(WS-C本人解決が種として2026-06-09済) | 種であるWS-C(本人解決の単一真実源)は完了済。identity.py が build_rls_metadata を『user_email→RLS GUCメタへの唯一の変換点』として集約し、mcp_gateway がそれを一元適用＝4回コピペの温床を構造的に解消。slack_bot.py を200行台へ縮小する完全なPolicyEngine middleware化(P3)はまだだが、MCP境界での集約は前倒し達成 | 🟢 前倒し | src/teamagent/identity.py:115-162(唯一の変換点), mcp_gateway/server.py:97-159, docs/next/teamagent_next_requirements.md:198(WS-C済), commit faafcf0/b5ac0a5 | — |
| 29 | DLPマスク/監査ログ/最小スコープを一貫適用(PII境界) | 監査ログ(CloudTrail+構造化ログ identity_resolved/spoof_rejected)・最小スコープ(IAM Deny+toolFilter+RLS+OAuth readonly scopes)は一貫適用。DLPマスクはログ/Sentry経路で適用。一方 生成文出口のDLPマスク(item14と同根)は未完。最小スコープ・監査の死守ラインは達成、DLPマスクの生成時適用が差分 | 🟡 部分 | sentry.py(マスク), security.tf:139-160(CloudTrail), fargate.tf:174-187(IAM Deny), mcp_gateway/server.py(構造化監査), gap=生成時DLPマスク | — |
| 30 | 不変条件・NFR(RLS/per-user OAuthを100%クエリ適用・本人外漏洩0件目標) | 計画通り強制をランタイムで実装。スクレイプ/メール系は既定OFF(env flag・toolFilterで非露出、url_guardもimportされない)。RLSはENABLE+FORCE ROW LEVEL SECURITY(0001/0006)でsuperuser以外も強制。per-user OAuthはKMS EncryptionContext(user_email)束縛で他人行複製しても復号不能。mcp_gateway は require_rls fail-closed＝実装の善意に依存しない構造強制。P0ハーネスでRLS-through-MCP漏洩0/インジェクション越権0を実DBで実証(commit cd95e1b) | ✅ 一致 | migrations/0001:118-119,0006:46-47(FORCE RLS), oauth_token_store.py:106-118(KMS context), mcp_gateway/server.py:127-142(fail-closed), commit cd95e1b(漏洩0実証) | — |
| 31 | P5の保険レイヤ(自前ゲート通過分のMCPのみregistry登録可) | 現状の外部ツールはOpenClaw公式プラグイン+自前teamagent-mcpのみで、版pin(digest pin v2026.6.5)+権限宣言(toolFilter/IAM)は実装済。ただしP5が要求する『署名検証+registry登録ゲート』という自動化された供給審査ゲートは未構築(将来P5計画)。現状は手動コードレビュー+digest pinで代替＝外部Skillエコシステムを使わない方針で攻撃面を縮小 | 🟡 部分 | docker-compose.yml:25(digest pin), docs/next/teamagent_next_requirements.md:159,193(P5供給ゲート将来), gap=署名検証/registry登録ゲート未構築 | — |
| 32 | P3でgitleaks+banditをCIゲート化 | 計画前倒しで達成。.github/workflows/ci.yml に bandit(-r src/teamagent -ll)と gitleaks(gitleaks-action@v2、build が needs:[lint-and-test,gitleaks])が既にCIゲートとして配線済＋pre-commitにも gitleaks/bandit補助。.gitleaks.toml にSlack/Anthropic/Gemini固有ルール追加。監査で全git履歴の実トークンコミット形跡ゼロを実証。P3を待たず先行ゲート化 | 🟢 前倒し | .github/workflows/ci.yml:45,59-79(bandit/gitleaks gate), .gitleaks.toml, .pre-commit-config.yaml:53-74, docs/security/security_audit_2026-06-12.md(履歴ゼロ) | — |
| 33 | 決定事項(採用確定)、WS-B隔離雛形2026-06-09済 | 計画通り全要素実装。docker-compose.yml: read_only:true/cap_drop ALL/no-new-privileges/digest pin(@sha256)/pids512/mem2g/internal私設網。fargate.tf openclaw_task: bedrock:InvokeModelのみ+Secrets/KMS/RDS/DynamoDB明示Deny。config: exec.mode:deny/fs.workspaceOnly/browser private遮断。営業データ非接触はIAM Deny+ネット分離+toolFilterで構造保証。監査でIAMシミュレーション+VPC内実測により『外殻は営業データに構造的に到達不能』を実証 | ✅ 一致 | docker-compose.yml:25-134, fargate.tf:166-187, openclaw.config.json5:69-70, docs/security/security_audit_2026-06-12.md §2(到達不能実証) | — |
| 34 | 済(WS-A mcp_gateway実装済・WS-C本人解決2026-06-09済・900テストgreen) | 完全実装・実証済。mcp_gateway/server.py がSTRICTモードでOC申告の user_email/user_groups/user_role を破棄(identity_spoof_rejected ログ)、slack_user_id をサーバ側でSlack解決、role常にmember固定、解決不能はrequire_rls下でfail-closed。faithfulness.py で捏造引用検出。本番エントリは resolver(SLACK_BOT_TOKEN)無しなら起動拒否。P0敵対ハーネスで身元詐称→無効化を実DB+live MCPで実証。テストは786 fns(repo現状)green | ✅ 一致 | mcp_gateway/server.py:97-159,221-293, identity.py:25,115-162(member固定/anti-spoof), orchestrator/faithfulness.py, commit cd95e1b(身元詐称実証), tests 786 fns | — |
| 35 | HITLはP2本番で解放、dmScope per-channel-peer必須 | dmScope:per-channel-peer は config(session.dmScope)に必須設定済＝計画通り。一方HITL(propose→confirm)は重操作tool自体をtoolFilter.exclude(*write*/*confirm*/*reply*/proposal_deck)で非露出にする形で実質封鎖中＝P2本番でのHITL解放はまだ。mcp_gateway docstringにも『重操作のHITL化はWS-Dで別tool化』と将来明記。現状は書込非露出で代替 | 🟡 部分 | openclaw.config.json5:64(dmScope per-channel-peer), openclaw.config.json5:114(write/confirm exclude), mcp_gateway/server.py:14(HITLはWS-D将来), gap=HITL propose→confirm未実装(P2解放前) | — |

## スケジューラ・自動化（8項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | AWS EventBridge を常設し、(a)朝8時=メール②自動バッチ (b)月9時=週次サマリー⑤自動バッチ (c)5分間隔のプロセス死活監視 の3系統を定期駆動する。 | EventBridge は terraform に一切存在しない(infra/terraform 配下に eventbridge.tf / aws_scheduler / aws_cloudwatch_event_rule なし＝grep/find で0件)。②朝メール・⑤週次サマリーの自動バッチも死活監視cronも未稼働。唯一の定期ループ video_approval_poller.py は asyncio・既定OFF(USE_VIDEO_APPROVAL_POLLING=false・interval 300s)で旧 slack_bot 単一プロセス内。死活監視は EventBridge ではなく Fargate/CloudMap のコンテナ healthCheck(interval=30s, fargate.tf:353/402)で代替的に存在するのみ＝計画の5分死活監視とは別物。 | 🔴 未実装 | docs/v3.2/architecture_plan_vs_current.html:138,152,175(自己判定 b-no・terraform実リソースに EventBridge なしと明記:255行); find infra -iname '*eventbridge*'→0件; src/teamagent/runtime/video_approval_poller.py:129-142(asyncio while+sleep); slack_bot.py:2035(既定OFF); fargate.tf:353,402(container healthCheck=死活監視の代替) | ②⑤自動バッチ・専用死活監視cronは設計のみで実装着手なし。死活はコンテナ healthCheck で部分代替されるが、計画した EventBridge 5分監視という形では未実装。 |
| 2 | EventBridge 前提の定期実行基盤を導入。単一プロセス維持なら APScheduler が軽量との代替案も提示(TODO P1)。 | 汎用の定期実行基盤(EventBridge/APScheduler 等)は未導入。ただし別目的のスケジューラとして ingest 専用 systemd timer をコード化(deploy/systemd/teamagent-ingest.timer=週次 月18:00 UTC + .service oneshot + scripts/run_ingest.sh)。これは②⑤メール/サマリー用ではなくナレッジ ingest 用で、かつ git 未追跡(?? deploy/ , ?? scripts/run_ingest.sh)＝コミットも本番実走もされていない。 | 🔄 方式変更 | deploy/systemd/teamagent-ingest.timer(OnCalendar=Mon *-*-* 18:00:00)/.service/README.md; git status→'?? deploy/' '?? scripts/run_ingest.sh'(未コミット・未追跡); data/ingest_sources.yaml:136(「Sprint3/4でEventBridge cron化」のTODOコメント残存=未実施); architecture_plan_vs_current.html:226-227(APScheduler代替案) | ②⑤死活監視向けの基盤としては未導入。EventBridge→systemd timer(ingest限定)に方式が変わり対象も縮小。timer自体も未コミット・本番未デプロイ。 |
| 3 | message.channels を Events API でサブスクライブし、過去案件ベクトル検索→ephemeral でプロアクティブ提示。confidence<0.7 で Block Kit 確認ダイアログ、ユーザーON/OFF設定を DynamoDB 保持、channels:history スコープ申請。営業3名で1週間ドッグフード。 | suggest_skill は存在しない(src/teamagent/skills/ に suggest 系ディレクトリ・ファイルなし、コード全体 grep で suggest_skill 0件)。message.channels の Events API サブスク・confidence ダイアログ・ON/OFF DynamoDB も未実装。channels:history スコープは ingest 用クライアント(slack_channel_ingest_client.py)が参照するのみで、自動サジェスト用途ではない。プロアクティブ提示は v2.1 で既に『@メンション起動(リアクティブ)』へ方針転換され、最終的に OpenClaw の agent loop(リアクティブ応答)に収斂。 | 🔴 未実装 | ls src/teamagent/skills/(suggest なし); grep -rl 'suggest_skill' src tests→0件; src/teamagent/adapters/slack_channel_ingest_client.py:7(channels:history は ingest 用); docs/v3.2/teamagent_master_todo_v1.md:265-270(計画); docs/archive/v2.1/...:788,1016(プロアクティブ→@メンション転換の前史) | Skill 自体が未着手。プロアクティブ自動サジェストという機能コンセプト自体が v2.1 でリアクティブ化され、Sprint7 計画は実質棚上げ。 |
| 4 | Slack User Group @managers 作成+IAM 権限マッピング、月曜9:00 の週間サマリー生成 Lambda(Salesforce 連携)、マネージャー権限チェック middleware、KPI/リスク案件の Block Kit テンプレート。Salesforce 連携可否がブロッカー。 | summarize_progress / 進捗サマリー Skill は未実装(skills/ に該当なし・grep 0件)。月曜9:00 生成 Lambda なし(active Lambda 0)、@managers User Group・権限 middleware・Salesforce 連携いずれも未着手。Salesforce 連携可否のブロッカーは未解消のまま。 | 🔴 未実装 | grep -rl 'summarize_progress' src→0件; ls src/teamagent/skills/(progress/summary 系なし); docs/v3.2/teamagent_master_todo_v1.md:311-316(計画・12.1/12.2); :374,468(Salesforce連携可否=Sprint12開始時ブロッカー); architecture_plan_vs_current.html:255(active Lambda 0) | Skill・Lambda・User Group・Salesforce 連携すべて未着手。ブロッカー(Salesforce)も未判断。 |
| 5 | 毎朝8:30に当日対応すべきメールを優先度別 Slack DM 配信(FR-MailWF-1 MUST)、毎週月曜9:00 にマネージャー向け週次ダイジェスト配信。定時バッチで起動。 | 本番 OpenClaw 系では未稼働。別トラックの AiLa(メールサマリー・repo ~/Documents/AI-IA-UAE)に朝配信スケジューラ機構がコード実装済み:aila-batch.timer(平日9:30 JST)+aila-notify.timer(毎分)、delivery/slack.py の Block Kit 配信、224 tests。ただし AWS 未展開(専用EC2起動は user gate V1-V3 待ち)・OpenClaw 結線(§Q3)未完。本番想定の自動配信は『EventBridge cron(平日9:30)→aiia-mcp batch→各自DM』(§Q6)で設計済みだが未デプロイ。週次サマリー(月曜9:00)は AiLa にも実装なし。朝サマリーは別経路で1人E2E実績(§U-1)あり。 | 🟡 部分 | /Users/s-komata/.claude/plans/mossy-snacking-locket.md:52,106,144,152,372,381(aila-batch.timer 平日9:30・EventBridge cron→aiia-mcp §Q6・AiLa別EC2・V1-V3 user gate); docs/v3.0/teamagent_requirements_v3.0.html:1866(FR-MailWF-1 毎朝8:30 MUST); 移行メモリ project_teamagent_openclaw_migration.md(§Q-Q2完了/§Q3未結線) | 朝サマリーのスケジューラはAiLa側でコード化済みだが本番AWS未展開・OpenClaw未結線・8:30でなく9:30実装。週次月曜9:00サマリーは未実装。本番E2E自動配信は未到達。 |
| 6 | infra/terraform/eventbridge.tf で EventBridge 15分 cron を作り、Gmail History API(users.history.list)を historyId カーソルで増分取得。CloudWatch にログ。 | eventbridge.tf は存在しない(find/grep 0件)。15分 cron 増分同期スケジューラは未実装。ただし mva_spec v1.1 自身が S1-01〜S2-08 を『OpenClaw コネクタイベント→自前ハンドラ→pgvector の繋ぎ込みと読み替える』と方針転換を明記しており、自前 EventBridge 前提は計画段階で既に置換。現状の定期 ingest は(別系の)未コミット systemd 週次 timer。Gmail historyId 増分は connector_sync_state テーブル設計(cursor列)はあるが本番稼働の増分同期 cron は未稼働。 | 🔄 方式変更 | docs/v3.1/teamagent_mva_spec_v1.1.html:2205(S2-07→eventbridge.tf 計画)/:2026(EventBridge Schedule 15min cron)/:2103(v1.1でOpenClawコネクタへ委譲と読み替え明記)/:2149-2153(connector_sync_state cursor設計); find infra -iname '*eventbridge*'→0件 | 自前 EventBridge 15分 cron は未実装かつ計画上 OpenClaw コネクタ委譲へ方式変更。OpenClaw 側の増分同期コネクタも本番ではメール未結線(§Q3)で実稼働せず。 |
| 7 | OpenClaw Skill summarize_progress を register、毎週月曜に Slack 週次サマリを自動投稿(5営業の週次サマリ自動投稿・Manager評価≥4/5 が DoD)。 | summarize_progress スキルは未実装(grep 0件・skills/ に該当なし)。OpenClaw への register もなし(openclaw.config.json5 の toolFilter.include に summarize_progress は不在)。毎週月曜の Slack 週次サマリ自動投稿機構も未実装。OpenClaw のプロアクティブ自動投稿に必要な heartbeat は P1 で 0m=無効。 | 🔴 未実装 | grep -rl 'summarize_progress' src→0件; infra/openclaw/openclaw.config.json5:108-114(toolFilter.include に summarize_progress なし)/:13(heartbeat 0m=自動投稿の前提機構が無効); docs/v3.1/teamagent_mva_spec_v1.1.html:1830-1833,2998(S8/Phase4-b 計画) | Skill・register・週次自動投稿すべて未着手。自動投稿の土台(heartbeat/cron)も P1 で無効。 |
| 8 | OpenClaw を L0 外殻として採用し、cron/heartbeat による自発(プロアクティブ)起動で朝ブリーフ・停滞案件アラート・競合監視等を能動的に回す。P1パイロットはリアクティブ読取のみ、ゲート通過後 P2 本番でプロアクティブ/HITL を解放。 | OpenClaw 採用・本番 go-live 済(E2E実返信 isError=false/3.3秒・2026-06-12)で、プロアクティブの土台となる heartbeat/cron 機構はプラットフォーム native に実在(openclaw.config.json5 に heartbeat: { every: '0m' } キーが配線済み)。ただし計画通り P1 では意図的に無効化(0m)中で、朝ブリーフ等の自発トリガは未稼働。解放は P2/WS-D 設計どおり後回し。具体スキル(deal_radar/morning_brief)もコード未実装。 | 🟡 部分 | infra/openclaw/openclaw.config.json5:12-13(「P1はプロアクティブ無効(heartbeat 0m)。P2/WS-Dで解禁」+ heartbeat配線実在); docs/next/teamagent_next_requirements.md:146-147,168,199-200(L0 cron/heartbeat自発起動・P2でプロアクティブ解放)/:80(deal_radar/morning_brief=将来); mossy-snacking-locket.md:271,366,469-474(heartbeatはtext限定/cronはpresentation可・go-live済); grep deal_radar/morning_brief→0件 | プラットフォーム機構は配線済み・本番稼働中だが、プロアクティブ自発起動は計画どおりP1で無効。実際の朝ブリーフ等トリガ稼働とdeal_radar/morning_briefスキルはP2待ちで未実装。 |

## インフラ・IaC・CI（16項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | 本番DBは db.r7g.large、Multi-AZ、pgvector知識ベースをデータ層baselineとして構築 | rds.tf で aws_db_instance.main を定義。実際の変数値は db.t4g.micro（variables.tf:23 default＝Dev/PoC用、コメントに『本番は db.r7g.large を想定』）、db_multi_az=false（variables.tf:53 default、『本番 true 推奨』）。pgvector はパラメータグループ＋init SQLで対応。本番E2E稼働中の共有RDSは dev規模(t4g.micro/単一AZ)のまま。Multi-AZ昇格は変数1つで切替可能だがP2送り(計画書§F:648/618『RDS Multi-AZ はP2／パイロットは既存RDSで可』) | 🟡 部分 | infra/terraform/rds.tf:99-125, variables.tf:20-54(db_instance_class default=db.t4g.micro, db_multi_az default=false), mossy-snacking-locket.md:648,618(Multi-AZはP2) | db.r7g.large/Multi-AZ への昇格は未実施。コード上は変数切替のみで到達可能だが、規模・コスト承認が§9-4相当で未決のままP2へ後送り。現状はパイロット規模での意図的なdev構成 |
| 2 | IaC=AWS CDK(TypeScript)、CI=GitHub Actions | IaCは CDK ではなく Terraform(HCL・19 .tfファイル・S3+DynamoDBバックエンド・targeted apply運用)で全面構築。CIは計画どおり GitHub Actions(ci.yml)。判断は『差異』：CI方式は一致、IaC言語/ツールのみ計画から変更 | 🔄 方式変更 | infra/terraform/*.tf(main.tf:21-39 terraform backend s3, RDS/Fargate/IAM/security全てHCL), .github/workflows/ci.yml。CDKディレクトリ/tsファイルは存在せず | CDK(TS)は一度も採用されず Terraform で実装。機能的には計画意図(IaC化)を満たすが言語/フレームワークが異なる |
| 3 | CIに pytest/mypy/ruff/ruff format/bandit/gitleaks を整備し、Dependabotをpip・github-actions週次で稼働 | ci.yml に全項目実装：ruff check / ruff format --check / mypy(strict, src/teamagent) / pytest / bandit -r -ll / gitleaks(別job)。Python 3.11・3.13 マトリクス。Dependabot は pip・github-actions の2エコシステムを週次(月曜09:00 JST)でグループ更新・major無視。計画どおり | ✅ 一致 | .github/workflows/ci.yml:47-72(6ツール全部), .github/dependabot.yml:6-68(github-actions+pip週次・group化) | なし。むしろ3.11/3.13マトリクス・concurrencyキャンセル・post-merge smoke枠など計画明記分を超える整備 |
| 4 | 本番RDS接続情報を踏み台SSM経由とし .env.prod.example に追記、ローカルからは触らない方針を明文化 | bastion.tf で SSM Session Manager経由の踏み台(t4g.nano)を定義し、DB SGは踏み台SGからの5432のみ許可(rds.tf:78-86)。.env.production.template に『SGが踏み台EC2からしか接続を許可しない→MacはSSM tunnel経由』と明記、DATABASE_URLはload_secrets.shが組み立てる方式。ファイル名は .env.prod.example → .env.production.template に変わったが趣旨は完全実装 | ✅ 一致 | infra/terraform/bastion.tf:1-13, infra/terraform/rds.tf:78-86(db_from_bastion), .env.production.template:8,44(踏み台SSM経由を明文化) | ファイル名が .env.prod.example ではなく .env.production.template（実質同等） |
| 5 | 既存EC2→新EC2のBlue/Green切替、DNS切替後48hモニタリング、1ページRunbook、SLO/オンコール体制確定(目標2026-12-28) | OpenClaw移行により『EC2 Blue/Green切替』方式は廃止。実際のリリースはFargateの専用Slackアプリ/専用chで現行Botと物理分離。現行rollbackはdurable previous task revision＋ECS deployment circuit breaker＋fresh signed rollback authorization＋one-use full saved planに統一し、過去の直接desired-count変更案は廃止。p95<15s/error閾値はCloudWatchアラーム化(variables.tf:88-98 p95_latency=15000ms)。本番E2Eは2026-06-12にgo-live済＝Sprint14目標(12月)を大幅前倒し。ただしSLO(uptime99.0%)正式確定・48hモニタ・オンコール体制・1週間パイロットは未完 | 🟢 前倒し | mossy-snacking-locket.md:24(2026-06-12 E2E実返信成功), :638,729,927(専用ch物理分離=Blue/Green不要), variables.tf:88-98(p95/error閾値), docs/openclaw/deploy_runbook.md(正準rollback), docs/openclaw/golive_checklist.md(#18 P1パイロット1週間) | Blue/Green(EC2)→Fargate切替で方式変更。本番E2Eは12月予定を6月に前倒し済だが、SLO正式合意・48hモニタ・オンコール体制・1週間パイロットは未実施(計画書#18でゲート前) |
| 6 | OAuthトークン/APIキー/DB接続情報を全てAWS Secrets Managerに格納・平文ゼロ、OAuthトークンKMS暗号化、通信TLS1.2以上 | DB/Slack/bearer/gateway/Vertex等を Secrets Manager に格納し ECS task に valueFrom 注入(fargate.tf:339-344,395-400)。tfにシークレット値は無く本人が事前作成。OAuth per-userトークンは DynamoDB+KMS暗号化(本人メールを暗号化コンテキスト)で監査済。RDSは rds.force_ssl=1 でTLS強制(rds.tf:41-44)。gitleaks＋pre-commitで平文混入をCIブロック。監査でgit履歴に実トークン混入ゼロを確認 | ✅ 一致 | infra/terraform/fargate.tf:24-44,339-344,395-400(secret注入), rds.tf:41-44(force_ssl), docs/security/security_audit_2026-06-12.md:26,29,46, security.tf(KMS CMK+rotation) | 残課題：構築中に作業端末で扱った Slackトークン3本(bot/app/user)のローテーション未実施(監査docs:69・パイロット開始前に実施予定)。自動rotation Lambdaも未実装(マーカーsecretのみ・security.tf:248-258) |
| 7 | 利用者16→100→数百人へアーキ変更なしでスケール | Fargate(desired_count・水平拡張可)＋RDS(垂直スケール／Multi-AZ昇格は変数切替)＋共有RDS pgvector(RLSでマルチユーザ隔離)構成。計画書§7.3は『~40名は単一GW(垂直・Haiku+prompt caching)、本格分離(利用者別GW)はP2前に判断・同時実行上限/SPOFはP1実測待ち』とし、100人/数百人での無変更スケールは未実証(P1実測ゲート前) | 🟡 部分 | infra/terraform/fargate.tf:428-460(ECS service desired_count=1), mossy-snacking-locket.md:552,865(~40名単一GW・分離はP2判断), NFR-3 | 基盤としては水平/垂直拡張の足場あり。ただし100人・数百人規模の実測・分離方式決定はP1パイロット後(§9-7)。現状はパイロット2-3名規模で『アーキ変更なし』は未検証 |
| 8 | PostgreSQL/pgvector/Redisの日次スナップショット7日分、DR RPO24h/RTO4h、マルチAZ構成必須 | RDSは backup_retention_period=7(rds.tf:114)で7日分自動バックアップ、storage_encrypted、prod環境はdeletion_protection。ただしMulti-AZはdefault false(未有効・P2)＝マルチAZ『必須』は未達。Redisはアーキ自体に不採用(grep:ElastiCache/Redis定義ゼロ＝OpenClaw移行でセッションはephemeral/SQLite・会話メモリはタスク再起動で揮発する既知P1制限)。DynamoDBトークン表のPITR/削除保護は監査で一部未有効と指摘(docs:59) | 🟡 部分 | infra/terraform/rds.tf:114(backup_retention_period=7),116(deletion_protection), variables.tf:53(multi_az default=false), Redis定義なし(grep結果0), docs/security/security_audit_2026-06-12.md:59 | Multi-AZ『必須』未達(P2送り)＝RPO/RTO数値の実測・保証なし。Redisは計画から消滅(不要化＝OpenClaw SQLite/Slack側文脈で代替)。DynamoDB PITR一部未設定 |
| 9 | dev/staging/prod 3環境を用意し実データはprodのみ | Terraform変数 environment は『dev/staging/prod』を想定し全リソース名/SG/secret/タグに ${environment} 反映、prod時のみ deletion_protection/skip_final_snapshot分岐あり(rds.tf:115-116)＝3環境化の足場は実装済。ただし実体は dev 一本(terraform.tfvars に staging/prod の workspace/tfvars個別運用は確認できず・本番E2Eも teamagent-dev cluster 上で稼働:228)。staging環境は未構築 | 🟡 部分 | infra/terraform/variables.tf:7-11(environment=dev/staging/prod), rds.tf:115-116(prod分岐), mossy-snacking-locket.md:228(teamagent-dev cluster で本番運用) | コードは環境パラメータ化済だが、実際は dev 環境のみ稼働しそこで本番運用。staging は未構築、prod専用環境への分離も未実施(パイロット段階のため) |
| 10 | 1チケットで VPC/RDS/ECR/ECS Fargate/Lambda/EventBridge/Secrets/CloudWatch をTerraformで一括apply | VPC(default利用+VPCエンドポイント)/RDS PG16+pgvector/ECR/ECS Fargate(2タスク別IAM)/Secrets Manager/CloudWatch(log group+alarm)/CodeBuild は全てTerraform化・apply済(本番稼働)。一方 Lambda(gmail-drive-slack-sync) は IAMロール/S3のみ作成で関数本体はコメントアウトのプレースホルダ(lambda_iam.tf:108-126)＝ingestは Lambda ではなく systemd timer(週次)で代替実装。EventBridge は terraform に定義なし(grep:0)＝OpenClaw移行でスケジューラ方式変更。朝メール配信のEventBridge cronはAiLa側(別経路・計画書:372,381) | 🔄 方式変更 | infra/terraform/{rds,ecr,fargate,security,lambda_iam,vpc_endpoints,codebuild}.tf, lambda_iam.tf:108-126(Lambda本体コメントアウト), deploy/systemd/teamagent-ingest.timer(週次ingest), EventBridge定義なし(grep結果0) | Lambda(sync)関数本体は未デプロイ＝ingestはsystemd timer化で代替(本番未実走)。EventBridgeは常設なし＝朝配信はAiLa別経路。コア(VPC/RDS/ECR/ECS/Secrets/CloudWatch)は一括構築の意図どおりapply済 |
| 11 | terraform applyが冪等(2回目で差分ゼロ)になることをS1 DoDとする | 本番go-liveは『plain apply禁止＝targeted/plan確認のうえ本人実行』運用(計画書:618,647『terraformはtargeted』)。README:31にも apply手順記載。go-live実ログでは ECR→roles/SG/cluster→taskdef→service の順に targeted apply を実施し2026-06-12にE2E到達。冪等性(2回apply差分ゼロ)を明示的に検証したログ/記述は確認できず。lifecycle ignore_changes=[password]等の差分抑制は実装済(rds.tf:122-124) | 🟡 部分 | infra/terraform/rds.tf:122-124(ignore_changes), README.md:31, mossy-snacking-locket.md:618,647(targeted apply), :734(順序付きtargeted apply手順) | 運用は『全apply冪等』ではなく targeted apply 前提に変更。『2回叩いて差分なし』のDoD検証ログは見当たらず冪等性は未実証。default VPC/サブネット data参照など環境依存箇所も残る |
| 12 | AWS ap-northeast-1 を第一候補とし、Mac miniはPoC限定 | 全Terraform/Bedrock/RDS/Fargateが ap-northeast-1(東京)で構築・本番稼働(variables.tf:4 aws_region default, terraform.tfvars はaccount718959508629/ap-northeast-1)。本番E2EもAWS東京で実証(計画書:24)。Mac miniは本番経路に不在(AiLa本体のみ個人Mac+cloudflaredで暫定稼働だが会社ナレッジ本線はAWS) | ✅ 一致 | infra/terraform/variables.tf:1-5(aws_region=ap-northeast-1), terraform.tfvars:4(account/region), mossy-snacking-locket.md:618(会社AWS 718959508629/ap-northeast-1) | なし(MVA-9のDay0判定はAWS確定で決着)。AiLaメール本体のみ個人Mac暫定だが本領域の主対象=会社ナレッジ基盤は完全AWS |
| 13 | RDS Multi-AZでRTO≤120s/RPO≤5分達成、DB断時は3回リトライ後fail-honest、client-boost等はfail-open | Multi-AZは変数化済(db_multi_az・default false)も未有効＝RTO/RPO数値目標は未達・未実測(P2/§9-4承認待ち、計画書:648,618)。DB断のリトライ/fail-honest・client-boost fail-open はアプリ層検索パイプライン(client名boost等)に該当機能あるが、本IaC領域では検証範囲外 | 🔴 未実装 | infra/terraform/variables.tf:50-54(db_multi_az default=false), mossy-snacking-locket.md:648,618(Multi-AZはP2・§9-4でコスト増承認待ち) | Multi-AZ未有効のためRTO≤120s/RPO≤5分は構造的に未達。実施Phase/コスト増が§9-4で承認待ちのまま。インフラ面では着手なし(変数defaultはfalse) |
| 14 | 全PRでruff/mypy/pytest/bandit必須、新importはci.yml手動列挙、基盤依存厳密pin、main CI成功率≥95% | ci.yml で ruff/ruff format/mypy(strict)/pytest/bandit/gitleaks を push・PR両方で必須化。依存は --no-deps + ci.yml手動列挙方式を踏襲(claude-agent-sdk==0.2.87 等を厳密pin・ci.yml:37-45)。SDKは==pin、uv.lockで全依存ロック。新import追加時のci.yml追記運用は確立済(メモリのhttpx教訓どおり) | ✅ 一致 | .github/workflows/ci.yml:32-60(no-deps+手動列挙+全ツール), :42(claude-agent-sdk==0.2.87 pin), uv.lock(920KB全依存ロック) | CI成功率≥95%の実測値ダッシュボードは本監査では未確認(数値トラッキングの仕組みは見当たらず)。ツール構成・pin・列挙方式は計画完全準拠 |
| 15 | 全スキルのI/O契約をPydantic v2で固定・run()純ロジック化、カバレッジ≥80%をCIゲート | Pydantic v2(>=2.9.0)+pydantic-settings を依存に採用(pyproject.toml:53-54)しスキルI/O契約に使用。一方カバレッジゲートは未配線：pytest-cov は依存にあるが(pyproject.toml:77)、ci.yml の pytest 実行は `pytest tests/ -q` のみで --cov/--cov-fail-under=80 等のゲートなし(grep:fail_under該当0)。§9-5で『最初から強制か段階引上げか未決』のとおりゲート化は見送り中 | 🟡 部分 | pyproject.toml:53-54(pydantic v2),77(pytest-cov), .github/workflows/ci.yml:57(pytest -q のみ・covフラグなし), cov-fail-under grep=0件 | カバレッジ≥80%のCIゲートは未実装(pytest-cov導入のみで閾値強制なし)。Pydantic v2採用とrun()純ロジック化の全スキル徹底度は本IaC監査では未精査(§8 P3扱い) |
| 16 | OpenClawをECS Fargate(per-task IAMで外殻/境界分離)に配置、RDS Multi-AZ、~40名は単一GW(垂直)、本格分離はP2前に判断・同時実行/SPOFはP1実測 | fargate.tf で OpenClaw(外殻)とteamagent-mcp(境界)を2タスク・別IAMロールで分離配置し本番稼働。OpenClawタスクロールはBedrock InvokeModelのみ＋Secrets/KMS/RDS/DynamoDB明示Deny(fargate.tf:166-188)、MCPタスクロールのみDB/Secret到達可＝per-task IAM権限分離を計画どおり実装・敵対ハーネスで隔離実証済。単一GW(desired_count=1)で稼働中。RDS Multi-AZのみP2送り(未有効)。同時実行上限/SPOF/利用者別GW分離はP1実測ゲート前で未決 | 🟢 強化 | infra/terraform/fargate.tf:103-251(execution/task role分割・OpenClaw Deny句166-188・MCP限定Secret202-240), SG mcpnet相当(270-300 8787はOpenClaw SGのみ), mossy-snacking-locket.md:24(本番E2E),552 | 配置・per-task IAM分離は推奨を超えて実装+敵対実証済。RDS Multi-AZは未有効(P2)。同時実行上限/SPOF/利用者別GW分離方式はP1実測(§9-7)待ちで未決のまま。state永続化なし(ephemeral・会話メモリ揮発)はP1既知制限 |

## 監視・運用・コスト（35項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | CloudWatchのみで監視。Sentry/Datadogは不採用というのが計画baseline | 実態は計画より厚い。CloudWatch(metric filter cost_usd/latency_ms/error_count + alarm)+CloudTrail+Sentry SDK(本番vectorinc.sentry.io動作確認済)+FastAPI管理ダッシュボード(usage_events/runtime_metrics, src/teamagent/dashboard/)+usage_recorder。Datadogは未導入。OpenClaw移行後は cloudwatch_fargate.tf でFargate用dashboard+alarmを別途追加 | 🟢 強化 | infra/terraform/cloudwatch.tf, security.tf(CloudTrail), src/teamagent/observability/sentry.py(2.60.0/PIIスクラブ), src/teamagent/dashboard/{app,queries,render}.py, master_todo v1.5 changedlog L418(Sentry本番動作確認), cloudwatch_fargate.tf | 計画baseline=CloudWatch単一に対し実装は多層(Sentry/管理画面/CloudTrail)へ拡張。Datadogのみ計画通り未導入 |
| 2 | 検索系 p95 ≤ 3秒(コールドスタート+3-wayルーター考慮)。SLO/アラームの正式定義 | OpenClaw常駐でコールドスタート無し・受付リアクションで即応。だが実測はL2二重LLM+prompt caching未効でtool 12s/合成10.6sの全体20.2s=p95目標超過。p95≤3秒の正式SLO/アラーム定義は未整備(alarm閾値はp95>15s) | 🟡 部分 | plan L26(全体20.2s/cache_read=0/p95≤15s超過), cloudwatch.tf p95_latency_high(閾値=var.p95_latency_threshold_ms, 既定運用15s) | 3秒SLOの正式定義・専用アラーム未整備。p95はゲート目標15s基準。チューニング(L2 Haiku化/caching)はP1実測後の課題として残存 |
| 3 | Bedrock呼出ログをS3+KMSで有効化。完了扱い(Day3 security.tf, apply待ち) | security.tf に aws_bedrock_model_invocation_logging_configuration + 専用S3バケット+KMS CMK+バケットポリシーをコード化。enable_bedrock_invocation_logging フラグでgate | ✅ 一致 | infra/terraform/security.tf L170-241(s3_config/KMS暗号化/text+embedding delivery), KMS key L14-75 | applyの実走証跡は未確認(リージョン1設定のみ制約のためフラグ既定OFFの可能性)。コードは完備 |
| 4 | Logs Insights運用クエリ集を保存(PR#28、Sprint4で運用開始)。完了扱い | docs/v3.2/ops/cloudwatch_queries.md にQ1-Q8(エラー/コスト/遅延/利用パターン/Skill別)10個を保存済 | ✅ 一致 | docs/v3.2/ops/cloudwatch_queries.md(2026-05-22 Day2/10クエリ), master_todo L138/L446(PR#28) | 本番EC2前提のクエリ。OpenClaw移行後はlog group名が/teamagent/dev/{openclaw,mcp}に変化しており、クエリのlog group参照は要更新(Fargate版dashboardのlogウィジェットでは新log groupを参照済) |
| 5 | 3メトリクスフィルタ+3アラーム。完了扱い(Day3 cloudwatch.tf, apply待ち) | cloudwatch.tf に3 metric filter(cost_usd/latency_ms/error_count)+3 alarm(daily_cost>閾値/p95 latency/error spike)+SNS topic完備。OpenClaw移行で cloudwatch_fargate.tf に同設計のMCP版(McpCostUSD/McpToolError/spoof_rejected)+dashboardを追加し、こちらが実配備済 | 🟢 強化 | cloudwatch.tf L34-126(3 filter+3 alarm), cloudwatch_fargate.tf L9-153(MCP版filter/alarm/dashboard), plan L256(alarm3本配備済・日次でログ/コスト要約) | 旧EC2版は実runtimeがOpenClawに置換され実効はFargate版。閾値はvar化されているが、$5/15s/3件の具体値はterraform.tfvars要確認 |
| 6 | Bot側受入実装完了、👤のプロジェクト作成+DSN投入のみ未(🔴) | observability/sentry.py(DSN空でno-op/before_sendでxoxb-/sk-ant-/AKIA/メール/長文を再帰スクラブ/request_idをtag昇格)+@app.error+loop.set_exception_handlerで二重キャッチ。Sentry本番(vectorinc.sentry.io)で動作確認済(Day4) | ✅ 一致 | src/teamagent/observability/sentry.py(12KB/SDK2.60.0), master_todo L417-418(v1.4/v1.5 Sentry本番動作確認・例外捕捉) | todo一覧ヘッダのL141は🔴のままだが、changelog L418で本番動作確認済=実質完了。ただしこれは旧EC2 Bot(slack_bot.py)向け。OpenClaw runtimeへのSentry結線は別途要 |
| 7 | SNS Topic作成済、Chatbot連携待ち(🔴) | SNS topic(alarms)はcloudwatch.tfに作成済(メールsubscription)。AWS Chatbot→Slackの連携はterraformに aws_chatbot リソース不在。コンソール手動作業として未実施 | 🔴 未実装 | cloudwatch.tf L20-30(SNS topic+email subscription), master_todo L143/L537/L619(Chatbot連携=👤user作業待ち🔴), repo全体に aws_chatbot リソース無し | Chatbot Slack連携が未着手(手動コンソール作業)。現状アラート通知はメールのみ |
| 8 | 営業16名オンボ資料(Notion+動画)、運用Runbook、インシデントフロー、RDS snapshot日次/RTO4h、16名コスト試算 | deploy系Runbookは存在(docs/openclaw/deploy_runbook.md, docs/v3.2/ec2_cutover_runbook.md/migration_runbook, docs/poc/admin_dashboard_runbook.md)。go-live伴走パッケージ(§P)あり。だが営業16名オンボ資料(Notion+動画)・OAuth失効/RDS断/rate limitの運用Runbook・正式DR手順は未整備 | 🟡 部分 | docs/openclaw/deploy_runbook.md, docs/v3.2/teamagent_migration_runbook_v3.2_draft.md, commit 73e2463(§P go-live伴走), master_todo L242-250(S6計画/未着手) | Sprint6予定の本番運用向けオンボ資料・運用Runbook・DR手順は未着手。現存Runbookはデプロイ/移行手順中心で、運用障害シナリオ(OAuth失効/RDS断)は未カバー |
| 9 | 応答p95<5s・可用性99%・精度70%のSLO定義+SLAドキュメント(営業時間内対応) | 正式SLO/SLAドキュメントは未作成。運用上のゲート目標(同時4でp95≤15s/エラー<1%)はプランに存在し実測対象だが、SLO文書として確定していない | 🔴 未実装 | plan L256/L549/L643(実測ゲートp95≤15s/エラー<1%=ゲートでありSLO文書ではない), master_todo L246(S6-09 SLO定義=未着手) | SLO/SLA正式文書なし。なお実運用基準値は計画間で不整合(v2.2=3s, S6=5s, ゲート=15s)。Sprint14のSLO文書化(項目30)で確定予定 |
| 10 | 5Skill×正常異常各10シナリオ+Locust(16名×30req/min×1h)+朝8:30スパイク(16並列DM)+Bedrockコスト試算+E2Eリグレッション(GHA matrix) | VSEO/動画パイプラインの堅牢性負荷試験(5シナリオ+敵対1・全モック)は実走済(並行20件19.75x/DL失敗嵐graceful/poller1000件冪等/race1件修正)。しかし16名同時のLocustシステム負荷試験・朝スパイク試験は未実施。E2Eリグレッションはpytest 945 green | 🟡 部分 | docs/v3.2/load_test_results.md(2026-06-04・VSEO堅牢性のみ・モックI/O), plan(同時4 p95実測=ゲートだが16名Locust未走), master_todo L320-325(S13計画/未着手) | 16名×30req/minのLocustシステム負荷試験・朝スパイク試験は未実施。既存負荷試験はVSEO skill単体の堅牢性(モック)であり、デプロイ済エージェント全体への負荷ではない |
| 11 | Sentryエラートラッキング統合、DataDog APM+CloudWatchカスタムメトリクス、AWS Budgets 80%通知整備 | Sentryは前倒し実装・本番動作確認済。CloudWatchカスタムメトリクス(EMF的なcost/latency/error metric filter)も実装済。DataDog APMは未導入(計画通り保留)。AWS Budgets 50/80/100%通知はコンソールで設定済(メール2件) | 🟡 部分 | sentry.py(前倒し済), cloudwatch.tf/cloudwatch_fargate.tf(カスタムメトリクス), master_todo L34/L286-287(Budgets設定済✅), L338(DataDog未着手) | DataDog APMのみ未着手。Sentry・カスタムメトリクス・Budgets通知は前倒しで実装/設定済(Sprint1〜Day4で先行) |
| 12 | RDS自動スナップ7日保持+S3クロスリージョン、DR計画(東京→us-east-1 RTO4h/RPO1h)、Runbook(インシデント/SLO違反/cost spike)、模擬障害2シナリオ訓練 | RDS定義はrds.tf に存在(バックアップ保持設定は要確認)。S3クロスリージョン・正式DR計画(RTO/RPO)・cost spike Runbook・障害訓練は未着手。CloudWatchログ保持は全リソース30日 | 🔴 未実装 | infra/terraform/rds.tf, retention_in_days=30(fargate.tf/aiia_mcp.tf/lambda_iam.tf), master_todo L340-342(S14計画/未着手) | クロスリージョンDR/RTO・RPO計画/インシデントRunbook/障害訓練すべて未着手。本番化フェーズ(Sprint14=12月)タスクで現時点はパイロット前段階 |
| 13 | top-3 retrieval hit rate / クエリあたりコスト / レイテンシp50・p95 / ユーザー満足度(reaction絵文字)を採取 | usage_events(request_id/cost_usd/latency_ms/input_output_tokens/status/skill)を1リクエスト1行で記録し管理ダッシュボードで集計。コスト実測$0.0125/検索・p95実測20.2s等は取得済。retrieval hit rateはgold set評価(eval)で別途計測。reaction絵文字満足度の自動採取は未確認 | 🟡 部分 | src/teamagent/runtime/usage_recorder.py(UsageEvent全フィールド), migration 0007_usage_events.sql/0008_runtime_metrics.sql, src/teamagent/dashboard/queries.py, plan L26(p95/コスト実測) | コスト/レイテンシ/利用統計は採取基盤完備。reaction絵文字による満足度の自動集計パイプラインは未確認。hit rateはeval側 |
| 14 | pytestフル+主要クエリ50種、Secretsローテ確認/CWログPII grep/prompt injection耐性、障害訓練(Bedrock/RDS/Slack API 3シナリオ) | pytestフル(945 green)+セキュリティ監査報告書(docs/security/security_audit_2026-06-12.md)+敵対ハーネス(attack_mcp:身元詐称/SSRF/prompt injection全fail実証)済。障害訓練(Bedrock/RDS/Slack 3シナリオ切替)は未実施。PIIスクラブはsentry before_sendで実装 | 🟡 部分 | commit dc0c002(security_audit報告書), cd95e1b(P0敵対ハーネス実DB+live MCP), plan #16 attack(SSM経由worker EC2から実走/全fail), 945 tests green | セキュリティ監査・injection耐性・リグレッションは実施済(前倒し)。障害訓練3シナリオの切替手順実演は未実施(Sprint13/14本番化タスク) |
| 15 | 600クエリ/日を同時50並列で再現、目標p95<15秒・エラー率<1% | 同時50並列のシステム負荷試験は未実施。実測されているのは同時4ピーク(ゲート目標p95≤15s/エラー<1%)であり、これはOpenClaw単一プロセス上限を測る現実的スコープに縮小されている | 🔄 方式変更 | plan L549(最大の未知数=OpenClaw単一プロセス同時実行上限/同時4でp95≤15s/エラー<1%), master_todo L325(S14負荷試験計画/未走) | 50並列の大規模負荷試験は未実施。OpenClaw移行で目標が同時4の実測ゲートに現実化。50並列はP2以降の課題 |
| 16 | TeamAgent-Bedrock-Monthly $50/月、TeamAgent-Server-Monthly $267/月、50/80/100%通知をs-komata@/AIagentAdmin@へ | AWS Budgets 2枠($50/$267)+通知メール2件をAWSコンソールで設定済(Day2実績と突合済✅)。ただしterraformコード(aws_budgets_budget)は不在=IaC化されておらずコンソール手動管理 | ✅ 一致 | master_todo L34/L284-287(設定済✅・2通知先), 実装plan L45(通知メール2件設定済), repo内にaws_budgets_budgetリソース無し(コンソール設定) | Budgets自体は設定済だがコード管理外(手動)。Chatbot/Slack通知連携は別項目で未(メール通知のみ) |
| 17 | prompt caching ON必須/Query RouterにHaiku/embeddingローカルE5/Bedrock us-east-1固定/月次コスト実績レビュー | 外側OpenClawはHaiku4.5、embeddingはローカルembedder同梱(MCPイメージ)。prompt cachingはOpenClaw経由で未効(cache_read=0)が実測で判明=L2二重LLMで効かず未最適化。ルーターはdetect_skillルールベース→OpenClaw agent loop(LLM)に置換。月次レビューはダッシュボード/CloudWatchで可能だが定例化未確認 | 🟡 部分 | plan L26(prompt caching未効cache_read=0/L2 Haiku化が候補), commit 253db0f(MCPイメージにembedder同梱), config(amazon-bedrock haiku), plan L139(Haiku/Sonnet切替) | prompt caching ONが実効していない(最大の最適化未達)。ルーター方式はLLM agent loopへ変更。月次レビューの定例運用は未確立 |
| 18 | 80%通知→PJリードがレビューし不要Skill一時停止、100%通知→予算引上げ承認(CTO)もしくはHaiku強制切替 | フラグでSkill個別ON/OFFは可能(toolFilter/USE_*_TOOLS)。だが予算%連動の自動エスカレーション運用ルール・承認フローはドキュメント化/自動化されていない | 🔴 未実装 | plan toolFilter/fail-closed既定OFF(Skill停止の技術手段はある), master_todo(エスカレーション運用ルールの定義タスク無し) | コスト%連動のエスカレーション運用ルール・承認フローは未整備(運用ルールの計画段階) |
| 19 | CloudWatchダッシュボードで取込件数/失敗/Embedding投入レイテンシをリアルタイム表示。OAuth失効/レート制限/Embedding失敗を#opsにアラート | ingestは構造化ログ+冪等upsert(ON CONFLICT)で記録。だがingest専用のCloudWatchダッシュボードや#opsへのOAuth失効/レート制限/Embedding失敗アラートは未配線。本日(6/15)ingestをsystemd timer週次自動化(コード化のみ・本番未実走) | 🟡 部分 | deploy/systemd/teamagent-ingest.{service,timer}(6/14作成), src/teamagent/ingest/pipeline.py(構造化ログ), cloudwatch_fargate.tf(MCP用dashboardはあるがingest専用指標は無し) | ingest監視ダッシュボード・#opsアラート(OAuth失効/レート制限/Embedding失敗)が未配線。systemd timerは本番未実走 |
| 20 | 全Agent呼出/Tool実行/データアクセスを{ユーザー/クエリ/Tool/応答/トークン数}でCloudWatch Logs+S3にアーカイブ、保持1年(S3 Glacier) | usage_events(request_id/user/skill/cost/latency/tokens/status)をRDSに1行記録。MCP境界でidentity_resolved/spoof_rejected/mcp_tool_error/company_sharedを構造化ログ。CloudWatchログ保持は全リソース30日。S3 Glacier 1年アーカイブは未設定 | 🟡 部分 | usage_recorder.py(UsageEvent), mcp_gateway/server.py L118-204(identity/tool監査ログ), CloudTrail(security.tf), retention_in_days=30(全log group), S3 Glacierライフサイクル無し | 監査記録自体は取得(RDS+構造化ログ+CloudTrail)。ただしCWログ保持30日・S3 Glacier 1年アーカイブ未設定。監査内容も計画の{応答本文}は意図的に非保持(PII配慮でquery_charsのみ) |
| 21 | 検索3秒/Mail朝サマリー30秒/提案生成3〜5分。可用性99.5%(8-22時月次) | 区分別の実測:検索は実測p95約20s(目標超過・caching未効が主因)、メール朝サマリーはAiLa別経路で配信実績あり、提案生成は設計段階。可用性99.5%の正式SLO/計測は未整備。alarm閾値はp95>15s | 🟡 部分 | plan L26(検索20.2s/p95超過), plan §V(AiLaメール別経路配信実績), cloudwatch.tf(p95 alarm), 可用性監視の死活エンドポイント常設なし | 検索SLOが実測で未達(caching未効)。可用性99.5%の計測/SLA未整備。区分別SLOの正式文書なし |
| 22 | pgvector top-20検索1秒以内、Embedding投入100ms/件以内 | pgvector(dense)+Cohere Rerank+RLS+Contextual Retrievalで検索実装。検索全体20.2sのうちtool部12s(検索+rerank)で、pgvector単体1秒以内の専用計測値は未確認。Embedding投入100msの計測も未確認 | 🟡 部分 | plan L26(tool 12s内訳), 検索アーキ(pgvector+rerank+CR), 専用ベンチ計測値なし | pgvector検索/Embedding投入の個別レイテンシ計測が未確認。全体p95が目標超過のため、検索単体が1秒以内かは要実測 |
| 23 | 月次運用コスト¥95K〜¥105K以内(v1.5上限¥250Kの約40%) | Budgets枠は$50(Bedrock)+$267(Server)≒約¥47K設定で、NFR-10の¥95-105Kとは別系統の数字。実測は単発検索$0.0125・AiLaメールはmax_budget_usdガード内。16名フル稼働の月次実コスト確定値は未集計(パイロット前) | 🟡 部分 | master_todo L286-287(Budgets$50/$267), plan L26($0.0125/検索), 実装plan(16名×30クエリで$32-64/月試算=上限ギリギリ) | NFR-10の¥95-105K枠と実装Budgets($50/$267≒¥47K)の数値が不整合。16名本番フルの月次実績確定は未(パイロット前段階) |
| 24 | Logs Insightsで全Agent履歴を任意条件検索(SHOULD) | 構造化JSONログ(structlog)+Logs Insightsクエリ集(Q1-Q8)を整備。Fargate dashboardにもlogウィジェット(identity/error検索)を配備。任意条件検索は可能 | ✅ 一致 | docs/v3.2/ops/cloudwatch_queries.md, cloudwatch_fargate.tf L139-150(logウィジェットでevent/tool/reason検索) | SHOULD要件として基盤は整備済。クエリのlog group参照はOpenClaw移行後の新log group(/teamagent/dev/{openclaw,mcp})に追従が必要 |
| 25 | Secrets Managerラッパーと構造化ログ基盤を実装(S1チケット・着手前) | 構造化ログ基盤=structlog(JSON Lines)で全面実装済(cost_usd/latency_ms/request_id/event)。Secrets連携=oauth_token_store.py(KMS復号/__repr__秘匿)等で実装。専用の汎用Secretsラッパーモジュールは個別adapterに分散 | ✅ 一致 | 全src structlog使用, src/teamagent/adapters/oauth_token_store.py, secrets_rotation_policy.md, plan(bearer認証/KMS復号) | S1着手前→実装完了に前進。汎用Secretsラッパーは単一モジュールでなくadapter群に分散している点が設計差 |
| 26 | agent_invoke/tool_call/assistant_responseをaudit_log記録、1リクエスト中Tool5回超で停止Guard(着手前) | MCP境界でtool実行ごとに構造化ログ(identity_resolved/mcp_tool_error等)+usage_events記録。Tool回数制限はOpenClaw外側のmax_turns/cost_cap設計に移行(計画では cost_cap$0.5/max_turns(8))だがconfigに未設定(P1はheartbeat 0mのみ) | 🔄 方式変更 | mcp_gateway/server.py(tool単位ログ), plan L553/L1038(max_turns/cost_capでコスト上限停止), openclaw.config.json5(costCap/maxTurns未設定・P2/WS-D予定) | audit記録は実装済。Tool5回Guardは固定回数からLLM agent loopのmax_turns/cost_cap方式へ変更。ただしその上限値もconfig未設定(P2 WS-Dで解禁予定) |
| 27 | CloudWatchメトリクスでクエリ数/平均応答時間/Tool呼回数/エラー率の運用ダッシュボードを構築(着手前) | 2系統で構築済:(1)FastAPI管理ダッシュボード(src/teamagent/dashboard/・usage_events/runtime_metricsからKPI集計)(2)CloudWatch Fargate dashboard(teamagent-dev-openclaw-pilot・cost/spoof/tool_error/CPU/Mem/logウィジェット)。後者は実配備済 | 🟢 強化 | src/teamagent/dashboard/{app,queries,render}.py, cloudwatch_fargate.tf L99-153(dashboard), plan L256(openclaw-pilot配備済・日次監視), docs/poc/admin_dashboard_runbook.md | S6着手前計画に対し、CloudWatch版+独自FastAPI管理画面の2系統を前倒し構築。計画超え |
| 28 | k6で16→100名想定、Bedrock同時実行スロットリング検証、コスト試算更新(着手前) | k6シナリオによる16→100名負荷試験は未実施。実測は同時4ピーク(OpenClaw単一プロセス上限の現実的測定)。Bedrock throttleリトライ実装はあり(providers)が、スロットリング限界の負荷検証は未走 | 🔴 未実装 | plan L549(OpenClaw単一プロセス上限が最大の未知数/同時4実測), throttleリトライ(plan L138), k6/100名シナリオ未存在 | k6 100名負荷試験・Bedrockスロットリング限界検証は未実施(本番化フェーズS11・現在パイロット前段階) |
| 29 | Runbook整備、RDSスナップ復旧、Slack緊急停止スイッチ、LLM主→副切替訓練(着手前) | Slack緊急停止=USE_OPENCLAW_FRONTEND flag(1分ロールバックで現行Bot復帰)が可逆性として担保。インシデントplaybook(外殻即停止→現行Bot復帰)はプラン記載。RDS復旧訓練・LLM主副切替訓練・RTO/RPO実演は未実施 | 🟡 部分 | plan §9-9(USE_OPENCLAW_FRONTEND flag1つで1分ロールバック), plan L1029(インシデントplaybook外殻即停止), 訓練実施記録なし | 緊急停止スイッチ(flag)は実装済。RDSスナップ復旧/LLM主副切替の障害訓練・RTO≤1h/RPO≤15分の実演は未実施(本番化フェーズ) |
| 30 | SLO文書化、月次レビュー会枠取り、全社展開アナウンス案、Go/No-Go②判定会(着手前) | SLO文書・月次レビュー定例・全社展開アナウンス案は未作成。Go/No-Goゲートは①(OpenClaw採用=B案)通過済で、本番E2E稼働(パイロット前段階)。Go/No-Go②(負荷/セキュリティ運用/障害訓練/SLOの4条件)は未到達 | 🔴 未実装 | plan(ゲート①通過・go-live完了2026-06-12), 本番リリース目標Sprint14=2026-12-28, SLO文書/月次レビュー定例なし | S14本番化タスク群は未着手。現在はP1パイロット前段階で、Go/No-Go②の4条件(負荷/セキュリティ運用/障害訓練/SLO)はいずれも未充足 |
| 31 | 開発¥80K(S1-S14総額)/運用月¥100K-¥1Mレンジ/個人開発ツール別承認の3層に整理。LLM推論最大¥600K/外部API最大¥100K試算上限 | コスト構造の3層整理は仕様書(F-9)で確定済。実装側はBudgets枠($50/$267)・単発$0.0125実測・max_budget_usdガードで運用コストを管理。¥1Mレンジ上限・個人ツール別承認フローの運用化は未確立 | 🟡 部分 | mva_spec F-9(3層確定), master_todo Budgets$50/$267, plan(max_budget_usd/cost_cap) | コスト構造の整理(計画)は確定。だが運用上の¥100K-¥1Mレンジ管理・個人ツール別承認フローは未運用化 |
| 32 | request_id伝播・構造化JSONログ・Bedrock usage/cost(in/out/cache tokens・model_id・latency・cost)を100%記録。架空scoreログ0 | request_idは全面伝播(usage_events主キー・mcp_tool_errorタグ・sentry tag昇格)。structlog JSON Lines全面。usage_eventsにcost/latency/input_output_tokens記録。cache tokens/model_id列の有無は要確認。架空scoreログ排除は検索の実score使用で担保 | ✅ 一致 | usage_recorder.py(request_id/cost_usd/latency_ms/input_tokens/output_tokens), mcp_gateway L204(request_id), sentry.py(request_idタグ昇格), structlog全面 | cache_read/cache_write tokens・model_idの専用列記録は未確認(cost_usd集約はあり)。request_id/構造化/cost記録の核は実装済 |
| 33 | 1クエリ平均$0.01-0.02/p50 7-11秒を維持改善。日次$5・p95 15s・5分3errでアラート | コスト実測$0.0125/検索=範囲内。p50/p95は外側ルーティング込みでp95実測20.2sが目標超過(caching未効)。アラート(日次$5/p95 15s/5分3err)はcloudwatch.tf/cloudwatch_fargate.tfにコード化・Fargate版配備済 | 🟡 部分 | plan L26($0.0125/p95 20.2s超過), cloudwatch.tf/cloudwatch_fargate.tf(3アラート), plan L256(alarm3本配備済) | コストは範囲内・アラートは配備済。だがp95レイテンシが目標15s/p50 7-11sを超過(20.2s)=性能目標未達。caching有効化/L2 Haiku化が残課題 |
| 34 | 同時4ピークを単一プロセスでp95≤15s/エラー<1%で安定処理、1スキル例外時もプロセス継続、DB障害時はDB依存スキルfail-honest・非依存は継続 | 1スキル失敗隔離・fail-closed/fail-honest設計は実装(ThreadPool max_workers=4で1人失敗隔離、mcp_rls_fail_closed)。VSEO堅牢性負荷試験で失敗隔離/graceful degradation実証済。同時4 p95≤15sの実測ゲートは未クリア(実測20.2s超過) | 🟡 部分 | load_test_results.md(失敗隔離/graceful実証), plan L142(1人失敗隔離), mcp_gateway(fail_closed), plan L26(p95超過/ゲート未達) | 堅牢性(失敗隔離/fail-honest)は実装・テスト済。同時4のp95≤15sゲートは未達(caching未効で20.2s)。1週間パイロット実測も未実施 |
| 35 | OpenClawのCVE運用(月次パッチ/版pin/赤チーム)の月あたり運用工数をP1で実測しP2 Go判断材料に。flag1つで現行Bot復帰(1分ロールバック)を常時担保 | 版pin(OpenClaw base 2026.6.5/claude-agent-sdk==0.2.87/mcp)・赤チーム(attack_mcp/敵対ハーネス)は実装済。USE_OPENCLAW_FRONTEND flagによる可逆性=プラン担保。CVE運用工数の月あたり実測はP1パイロット(1週間)が未走のため未取得 | 🟡 部分 | plan §9-9(flag1分ロールバック), commit cd95e1b(敵対ハーネス), config版pin(base 2026.6.5), plan L643(運用工数記録=P1ゲート) | 版pin/赤チーム/ロールバック手段は整備済。CVE運用工数の実測はP1パイロット未実施のため未取得(P2 Go判断材料が未収集) |

## UI・UX（11項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | Sprint2末(文書状態🔴未着手)に営業2名でドッグフード。専用ch開設・評価フォーム・結果集計・docs/v3.2/feedback_sprint2.md作成。 | リポジトリ・ディスク上に #teamagent-beta 設定、評価フォーム、feedback_sprint2.md のいずれも存在しない。代わりにOpenClaw go-live後は『招待された専用ch/DM許可リストの本人1名＋パイロット参加者』方式で実E2E(@AiLa→3.3秒返信)まで到達。形式ばったSprint2ベータ手続きは実施されず、本番E2E稼働(パイロット前段)へ前倒しで進んだ。 | 🔄 方式変更 | find で feedback_sprint2.md 不在(docs/v3.2/配下なし)/master_todo_v1.md:171-174 が🔴のまま/go-live実証=mossy-snacking-locket.md §T0-T1・§S3(commit 7367507)/DM許可リスト=infra/openclaw/openclaw.config.json5:56 | 計画通りの2名ベータ手続き・評価フォーム・feedback_sprint2.md は未作成のまま。実利用ログ集計の体系化も未。ただし価値検証は本番E2E実走で代替進行中。 |
| 2 | S4(6/27-7/10)にDMをベータユーザーallowlist制にする(🤖+👤・2h)。 | 実装済み。OpenClaw config で dmPolicy:"allowlist" + allowFrom:[U09CX1CCBLN]、session.dmScope:"per-channel-peer"(多人数会話分離) を設定。理由=WSに社外ゲスト前提・DM経由で会社の脳に触れさせない。Fargateで承認CLI不可のため明示allowlist化。 | ✅ 一致 | infra/openclaw/openclaw.config.json5:56 dmPolicy:"allowlist"・57 allowFrom・64 dmScope:"per-channel-peer"/commit 95d279c/mossy-snacking-locket.md §T2-DM許可リスト制/master_todo_v1.md:218(S4-10) | パイロット拡大時は allowFrom への手動追記運用。allowlistの管理UIは無いが要件範囲外。実質計画通り達成。 |
| 3 | S4-11でApp Homeタブ実装(おすすめクエリ・検索履歴・6h)。要件UR-6(ホームタブ最近の活動/案件一覧/Skillショートカット・SHOULD)、UR-7(進捗バー・SHOULD)、UR-8(もう一度試すボタン・SHOULD)。 | 未実装。src/infra全域に views_publish / app_home_opened / 進捗バー / 『もう一度試す』ボタンのコードは存在しない(docs内の要件記述のみ)。現行UIは @AiLa メンション→スレッド返信＋👀受付リアクションのみ。OpenClawはエラー時にisErrorを返すが再試行ボタンUIは無し。 | 🔴 未実装 | grep views_publish\|app_home_opened\|進捗バー\|もう一度試す → src/infra ヒット0(docs/v3.0/requirements_v3.0.html:1980-1982 UR-6/7/8 と master_todo_v1.md:219 S4-11 のみ) | App Homeタブ・進捗バー・再試行ボタンは着手ゼロ。いずれもSHOULD優先度かつ最終形(S15)寄りで、現行パイロット段階では未対象。 |
| 4 | S5でベータ5名運用。Slack canvas+3分動画のオンボーディング資料、#teamagent-beta開設、週次Google Form FB、20hの運用伴走。 | Slack canvas/3分動画/Google Form/#teamagent-beta のいずれもディスク・リポジトリに存在しない。代わりに go-live伴走パッケージ(§P deploy_runbook/smoke/observability)を整備し、DM許可リスト制で本人＋パイロット参加者を招く運用に移行。AiLa側はメンション『連携して』→2リンク(Google/Slack OAuth)配布のオンボーディング導線(§Q-O/§V7)を設計・一部実装。 | 🔄 方式変更 | find で canvas/動画/Google Form資産なし/master_todo_v1.md:222-230(S5-01〜05)/go-live伴走=mossy-snacking-locket.md §P(commit 73e2463)/連携導線=§Q-O・§V7(リンク配布) | 5名ベータの形式的運用(canvas/動画/Google Form)は未着手。オンボーディングはOAuth連携導線として別形(リンク配布)で先行。週次FBの仕組みも未構築。 |
| 5 | S5でベータ→16名全展開、周知、使い方カード+3分動画作成。 | 16名展開・周知・使い方カード・3分動画はいずれも未着手。現状は本番E2E稼働(パイロット前段)でDM許可リストに本人＋少数参加者のみ。16名分allowlist拡張(master_todo S6-11)も未実行。 | 🔴 未実装 | teamagent_implementation_plan_v3.2_draft.md:201-206/master_todo_v1.md:252(S6-11 16名allowlist拡張・未)/現状=mossy-snacking-locket.md §T2(allowFrom少数) | 全16名展開・オンボーディング資料一式は未。go-live(6/12)直後でパイロット投入前のため計画スケジュール上も先。 |
| 6 | OR-4(MUST)=提案書をブラウザ内でcontenteditable+Tailwindで微修正。OR-5(MUST)=編集履歴をPostgreSQLに10版分保存(版管理)。 | 部分実装。VSEO動画分析スライド(src/teamagent/skills/video_algorithm/slides.py)が contenteditable 付き素タグ＋意味クラス＋hover/focusアウトラインで『営業がブラウザで直接編集可』なHTMLを出力(OR-4の精神を別サーフェスで実現)。ただし(a)対象は提案書本体でなくVSEOスライド、(b)Tailwindでなく自前CSS、(c)OR-5の編集履歴PostgreSQL 10版保存は未実装(migrations 0001-0011に該当テーブル無し)。 | 🟡 部分 | src/teamagent/skills/video_algorithm/slides.py:6,89-90,114-161(contenteditable)/OR-4,5=docs/v3.0/requirements_v3.0.html:1955-1956/版管理テーブル不在=infra/migrations/0001-0011に edit_history/revision 無し/提案書設計=html-agent-floofy-fountain.md | 提案書向けの本格WYSIWYGエディタ・差分/プレビュー・10版版管理(OR-5)は未。現状はVSEO HTMLのインライン編集可属性のみ。提案書生成パイプライン自体が設計段階(html-agent-floofy-fountain.md)。 |
| 7 | UR-1(MUST)=@TeamAgentメンションで起動・誤検知ゼロ・応答3秒以内。 | 達成かつ前倒し。go-liveで @AiLa メンション→OpenClaw agent run→スレッド返信を本番実証。実測 isError=false・3.3秒(応答3秒目標に概ね合致)・👀受付リアクションで処理中可視化。app_mention購読(app_mentions:read)有効化で起動。検索全行程(embedder 535ms→pgvector hit5/top0.794→Bedrock合成10.6s)もログ実証。 | 🟢 前倒し | mossy-snacking-locket.md §S3(commit 7367507・3.3秒/スレッド返信1781231927.429389/👀)・§T1(全行程ログ)/ackReaction:"eyes"=infra/openclaw/openclaw.config.json5:51/UR-1=requirements_v3.0.html:1975 | L2合成込みの検索は10秒級で『応答3秒』は受付リアクション＋ストリーミング前提の目標。誤検知ゼロは定量計測未だが起動はメンション限定で実用上達成。 |
| 8 | UR-2(MUST・Phase4)=主要SkillをSlash Commandで起動。FR-SlackSuggest-5の /teamagent-search も。 | 未実装。/create-proposal /analyze-video /morning-summary /teamagent-search いずれもsrc/infraにコード無し(docs記述のみ)。現行起動は @メンション一本。OpenClaw移行で起動はagent loop(LLM意図解釈)に寄り、Slash定義は配線されていない。 | 🔴 未実装 | grep /create-proposal\|/morning-summary\|/teamagent-search → src/infra ヒット0(docs/v3.0/requirements_v3.0.html:1976 UR-2・1916 FR-SlackSuggest-5 のみ) | Slash Commandのapp定義・ハンドラとも未着手。Phase4(S14)スコープのため計画上も先。 |
| 9 | UR-3(MUST)=Workflow Builder+Block KitボタンUIで『ボタンを押すだけ』起動。UR-5(MUST)=ボタンからBlock Kit Modalでパラメータ入力。TR-9=Block Kit送信/ephemeral/Modal Tool。 | Workflow Builder・Block Kit Modal入力フォームは未実装。ただしBlock Kitボタン自体は別系統で前進: 旧slack_bot.pyにbuild_search_blocks(divider/section/URLボタン『Drive/Slackで開く』)が存在(現本番はOpenClaw経由のため非アクティブ)。AiLa朝メールは全URLボタン化(『対応する/後で/対応済み』署名GET)。OpenClawはpresentation機構/[[slack_buttons:]]でBlock Kitボタン出力可と一次確認(要実機検証)。Modal入力・Workflow Builder連携は無し。 | 🟡 部分 | build_search_blocks=src/teamagent/runtime/slack_bot.py:216-289(URLボタン)/AiLa全URLボタン化=mossy-snacking-locket.md §V4・62-63/OpenClawボタン一次確認=§Q(366,380)/Modal/WorkflowBuilder grep ヒット0/UR-3,5=requirements_v3.0.html:1977,1979 | Workflow Builderトリガー・Block Kit Modal(双方向入力フォーム)は未着手。現状ボタンは全てURL/署名GET型(block_actions往復を避ける設計)。最終形S15スコープのため計画上も先。 |
| 10 | UR-4(MUST)=同一スレッド内でSkill跨ぎでも履歴継承。MR-P3-4=『もう少し詳しく』『別の案件で』で前回条件継承。SR-1=スレッドID単位で直近20ターンRedis保持/TTL24h。 | OpenClaw agent loopが session.dmScope:"per-channel-peer" でチャンネル/相手ごとに会話文脈を分離保持し、スレッド返信に文脈をぶら下げる方式で実現。旧設計のRedis 20ターン保持はOpenClawのセッション管理に置換。Skill跨ぎ継承はOpenClawのagent contextに内包(LLMが履歴を保持)。一方で2人混線テスト(互いのスレッド/文脈が混ざらない)は未実施(残タスク#17)。 | 🔄 方式変更 | session.dmScope:"per-channel-peer"=infra/openclaw/openclaw.config.json5:64/混線テスト未=mossy-snacking-locket.md §T #17(251)/UR-4=requirements_v3.0.html:1978・MR-P3-4=1815・SR-1=2077 | per-channel-peerでの文脈分離は構成済みだが、混線(2人同時)実機テストが未消化=本番での履歴継承/分離の正しさは未実証。RedisベースのSR-1/SR-4(20ターン/TTL/リセット条件)はOpenClaw方式に吸収され元仕様通りではない。 |
| 11 | 7.2=引用付きサマリー+▸関連メール/Drive/Slack各リンク+『出典:documents.id=...』の固定フォーマット。8.2 MVA-6で出力形式(a)サマリーのみ/(b)リンクのみ/(c)両方=現案、はS6開始前に判定予定(未決)。 | 実装済。検索SkillはSearchOutput(answer=LLM合成サマリー + hits[file_name/page_num/drive_url/source_type/source_uri/score])を返し、MCPゲートウェイがmodel_dump()でJSON化してOpenClawへ渡す(server.py:208)→OpenClawがスレッド返信。旧slack_bot.pyのformat_search_response/build_search_blocksは『サマリー+参考資料(出典ラベル+score+Drive/Slackで開くリンク/ボタン)』=(c)両方表示を体現。本番go-liveでhit5件/top0.794+Bedrock合成の引用付き返信を実証。 | ✅ 一致 | SearchOutput生成=src/teamagent/skills/search/skill.py:187-235/schema=schema.py:SearchHitOut(file_name/drive_url/source/score)/MCP化=mcp_gateway/server.py:208 model_dump→JSON/(c)両方=slack_bot.py:193-216(format_search_response)・216-289(build_search_blocks)/本番実証=mossy-snacking-locket.md §T1(hit5/top0.794)/MVA-6=mva_spec_v1.1.html:2729-2730 | 厳密な『出典:documents.id=...』表記はメモリ目視レベルで固定化未検証。関連メール/Drive/Slackの3カテゴリ分割表示(7.2の▸構造)はOpenClaw返信整形に委ねられ、slack_bot.py側の表示形そのままではない。MVA-6の正式『両方』確定判定の文書記録は未確認。実質(c)両方で稼働。 |

## LLM・オーケストレーション（26項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | Sonnetベースの3分類LLMルーターをapp_mention統一入口で実装(計画baseline) | 本番ではOpenClaw外殻のHaiku4.5 agent loop(LLM)が意図ルーティングを担う(openclaw.config.json5 model=jp.anthropic.claude-haiku-4-5、go-live 2026-06-12でE2E実証)。一方、旧単一プロセスBotのintent.detect_skill(366行・正規表現12+分岐)とrouter.py(QueryType 4分類・rule-based、bedrock注入時のみHaikuフォールバック)も残存。3-way recall/compose/analyze分類のSonnetルーターという当初設計そのものは未実装 | 🔄 方式変更 | infra/openclaw/openclaw.config.json5:28,132 (Haiku4.5外側ループ); src/teamagent/skills/intent.py(366行・detect_skill); src/teamagent/skills/router.py:95-123(SkillRouter rule-based+Haikuフォールバック・閾値0.6); plans/mossy-snacking-locket.md:24 go-live実証 | 3分類のSonnet単一LLMルーターは作られず、(a)本番=OpenClaw Haiku loop (b)旧Bot=regex detect_skill の二系統。当初の意図ルーター案は置換された |
| 2 | 5機能セットを提供(計画baseline) | detect_skillが12スキルへ拡張: search/clientkarte/proposal_draft/proposal_review/operation_log/video_analysis/video_algorithm(VSEO)/video_approval/tiktok_search/mail_constraints/workspace_search/chitchat。当初5機能を大きく超える | 🟢 強化 | src/teamagent/skills/ ディレクトリ12スキル; src/teamagent/skills/intent.py:285-366 detect_skill 優先順位ツリー | 週次/月次サマリーは現状EventBridge常設なしで自動化未(別領域)。機能セット自体は拡張済 |
| 3 | Agent SDK orchestratorで4サブエージェントをLambda/Fargate展開 | Agent SDK(claude_agent_sdk)でin-process @tool化したSDK runner(orchestrator/sdk_runner.py run_sdk_agent)は実装済・6-bis準拠・85MB実deck生成済。ただしFargate展開されているのはOpenClaw外殻+3つのMCPバックエンド(teamagent-mcp/video-mcp/aiia-mcp)であり、SDK orchestrator自体は4サブエージェント構成でのLambda/Fargate展開ではない。L2 SDK runnerはscript経由(run_orchestrator_prod.py)のみで本番runtime未配線 | 🟡 部分 | src/teamagent/orchestrator/sdk_runner.py:277 run_sdk_agent(create_sdk_mcp_server/@tool); run_sdk_agent呼出元=scripts/run_orchestrator_prod.py・poc_sdk_agent_demo.py・eval_orchestration.pyのみ(runtime/には無し); infra/terraform/fargate.tf(OpenClaw+teamagent-mcp), aiia_mcp.tf | 4サブエージェントのLambda/Fargate展開は未。SDKループはコード完成も本番ライブ経路はOpenClaw L0→MCP直叩き(L1)で、SDK L2は明示マルチステップ用に温存(未結線) |
| 4 | USE_LLM_ROUTERでHaiku4.5判定に置換完了とTODO上は記載 | router.py SkillRouterはハイブリッド実装(rule-based優先、confidence<0.6かつbedrock注入時のみHaiku 4.5でJSON判定)。完全LLM置換ではなくフォールバック止まり。USE_LLM_ROUTER envフラグはコードに存在せず、bedrock client注入有無で分岐 | 🟡 部分 | src/teamagent/skills/router.py:95-123 LLM_FALLBACK_THRESHOLD=0.6 / _route_llm; docstring line1「Sprint2でHaikuベースに差替予定」のまま; grep USE_LLM_ROUTER→src内ヒットなし | TODO上は完了扱いだが、コード実体は低confidence時のみのHaikuフォールバックで、router.pyは依然rule-based主体。本番OpenClaw移行でこのrouter.py経路自体が傍流化 |
| 5 | source指定構文でGmail/Drive横断検索をルーターに追加 | router.py/intent.pyにsource指定構文(source:gmail等)の実装は見当たらない。Drive/Slack/Sheetsはingest(pgvector)に取り込み済でsearchが横断するが、ルーター段でのsource明示フィルタ構文は未 | 🔴 未実装 | src/teamagent/skills/router.py(QueryType=meta/conditional/compare/contentのみ・source無し); intent.pyにsource構文無し | source指定構文によるGmail/Drive明示振り分けは着手なし。横断検索自体はingest+search側で実現 |
| 6 | src/prompts/metadata_extract/v1/system.md を新設し既存3PDFでSonnet 4.6メタ抽出を単発実行成功 | scripts/extract_metadata.py が Sonnet 4.6 で1PDF→industry/client/vendor/target/budget/service/keywords/proposed_at をJSON抽出しpgvector metadata JSONBに保存する形で実装済。ただし指定パス src/prompts/metadata_extract/v1/system.md にはファイル無し(promptはscript内/別構成) | 🔄 方式変更 | scripts/extract_metadata.py:1-40(Sonnet4.6でPDFメタJSON抽出・proposals_chunks metadata保存); prompts/配下にmetadata_extractディレクトリ無し(chitchat/search/proposal等のみ) | 機能(Sonnetメタ抽出)は実装されたが、計画の指定prompt配置パス(src/prompts/metadata_extract/v1/system.md)とは異なるscript同梱方式。3PDF動作確認の証跡はコードからは未確認 |
| 7 | openclaw.jsonでamazon-bedrockプロバイダ有効化、Sonnet4.6でhello疎通 | openclaw.config.json5でmodels.providers.amazon-bedrock有効化済(東京リージョン・auth:aws-sdk・Haiku4.5+Sonnet4.6両モデル登録)。go-liveでBedrock実応答(isError=false/3.3秒・Sonnet4.6合成$0.0125)を本番E2E実証。PoCを超えて本番稼働 | 🟢 前倒し | infra/openclaw/openclaw.config.json5:121-148(amazon-bedrock provider・haiku4.5/sonnet4.6 inference profile実測ID); plans/mossy-snacking-locket.md:24,25 §S3/§T1 E2E実証(Bedrock converse Sonnet4.6 10.6s) | なし。PoC計画項目は本番稼働で達成(当初はopenclaw不採用→再採用の経緯あり) |
| 8 | Agent SDKをBedrock経由で組み込み3.5-sonnet以上を呼ぶ(MUST) | sdk_runner.pyがclaude_agent_sdk(query/tool/create_sdk_mcp_server)をCLAUDE_CODE_USE_BEDROCK=1でSonnet4.6に対し駆動。本番ライブはOpenClawがBedrock(Haiku4.5/Sonnet4.6)を呼ぶ形で稼働。SDK組み込み自体は完成しコード経路あり | ✅ 一致 | src/teamagent/orchestrator/sdk_runner.py:32-39 import claude_agent_sdk; :305-331 ClaudeAgentOptions(model=…); docstring CLAUDE_CODE_USE_BEDROCK=1; bedrock_client.py model_id sonnet-4-6 | SDK経由は本番runtime未配線(scriptのみ)だが、Bedrock経由のSonnet4.6呼び出し自体は本番OpenClaw経路で達成済。MUST要件の本質は満たす |
| 9 | pgvector検索をSDK Tool化し検索パイプライン全行程を動かす | SearchSkillをToolSpec化しfactory.build_production_tools()で束ね、sdk_runner build_skill_toolsが@tool化。本番ではteamagent-mcp(streamable-http)のsearch toolとして公開され、go-liveでembedder_embed(E5 1024dim)→pgvector_search(hit5/top0.794)→Sonnet合成の全行程を本番実証 | ✅ 一致 | src/teamagent/orchestrator/factory.py:71-99 build_production_tools(SearchSkill); sdk_runner.py:241 build_skill_tools; mcp_gateway/server.py(MCP公開); plans/mossy-snacking-locket.md:25 §T1全チェーン実証 | なし(SDK@tool化・MCP公開・本番E2E全達成) |
| 10 | 自前loopでなくSDK標準Tool Useループでエージェント実行 | sdk_runner.run_sdk_agentがSDKのquery()ループに委譲(自前loop.py不使用)。max_turns/max_budget_usd/同一ツール繰返し拒否/timeout/RLS fail-closedガード内蔵。本番ではOpenClawのagent loopが同等のTool Useループを担いgo-live実証 | ✅ 一致 | src/teamagent/orchestrator/sdk_runner.py:339 async for message in query(…)(SDKループ主体)・docstring「自前loop.pyは使わない」; :317-331 max_turns=8/max_budget_usd; OpenClaw loop=plans §T1 | SDK版は本番未配線だが、Tool Useループ実行という要件は本番OpenClaw経路で達成済 |
| 11 | SlackSuggest(軽)とProposal(重)をconfidence≥0.7で振り分け | intent.detect_skillが提案系(_DRAFT_RE/_REVIEW_RE)と検索を優先順位ツリーで振り分け(rule-base・confidence値は持たない)。router.py側はconfidence値を持つ(compare0.7/conditional0.8等)が検索内分類用。≥0.7数値閾値での軽重Skill振り分けという形ではない。本番はOpenClaw LLM loopが振り分け | 🔄 方式変更 | src/teamagent/skills/intent.py:341-347(_REVIEW_RE/_DRAFT_RE優先判定・confidence無し); router.py:107 LLM_FALLBACK_THRESHOLD=0.6/confidence値; OpenClaw=本番振り分け実体 | confidence≥0.7数値ゲートでの軽/重Skill振り分けは未。実体はrule-base優先ツリー(intent)+本番LLM loop(OpenClaw)に置換 |
| 12 | 5系統のToolを定義(MUST・v3.0新規) | 動画分析(video/video_algorithm/video_approval skill・video-mcp本番Gemini Vertex)、Gmail(mail_constraints skill・aiia-mcp本番RUNNING)、Slack操作(slack_bot/Block Kit/ephemeral)、Drive(ingest取込・workspace_search)、TikTok/YouTubeスクレイプは実装。Web検索(Gemini DR)は動画/VSEO文脈で実装。多くがMCP toolとして公開(toolFilter include: search/clientkarte/proposal_*/tiktok_search/video_*) | 🟡 部分 | src/teamagent/skills/{video,video_algorithm,video_approval,mail_constraints,workspace_search,tiktok_search}; openclaw.config.json5:108-115 toolFilter; infra/terraform/aiia_mcp.tf(Gmail), video-mcp本番 | Gmail下書き/Drive書込は物理封鎖・HITL未配線(toolFilter excludeで*write*/*reply*/*confirm*を除外)。aiia-mcpはOpenClaw未結線(§Q-Q3残)。各tool定義は揃うが書込系・統合は段階的 |
| 13 | カレンダー読取Toolと5分キャッシュ(SHOULD) | workspace_search skillがGCalendarClient.from_user_token().list_events()で本人OAuthによるカレンダー読取を実装(per-user・USE_WORKSPACE_TOOLS=1でopt-in・既定OFF・本番未露出)。一方Tool結果の同一クエリ5分キャッシュ機構はコード上確認できず | 🟡 部分 | src/teamagent/skills/workspace_search/skill.py:77-89 GCalendarClient list_events(calendar読取実装); factory.py:119-130 USE_WORKSPACE_TOOLS既定OFF; toolFilter excludeにworkspace_search(本番非露出) | カレンダー読取はコード完成も本番未露出(per-user OAuth前提)。5分キャッシュは未実装。SHOULD項目 |
| 14 | S5-01でSkillハンドラ内にAgent SDK組み込み | sdk_runnerでSkillをSDK @tool化しSDKループで連鎖実行する形を実装(6-bis合格・85MB deck生成)。設計はSkill内部にSDKを埋め込むより「SkillをSDKのツールにする」L2 orchestrator方式に発展。本番ライブはOpenClaw外殻loopがTool連鎖を担う | 🔄 方式変更 | src/teamagent/orchestrator/sdk_runner.py(SkillをSDK toolとして連鎖); 計画docs/next L2=orchestrator/温存; OpenClaw外殻が本番連鎖主体 | Skillハンドラ内蔵ではなく外殻(OpenClaw L0)+境界内L2(SDK)の2層に再設計。SDK L2自体は本番runtime未配線 |
| 15 | Sonnet4.6主軸・caching有効・安価クエリHaiku自動ルート(Day0でping200+caching確認) | 本番=OpenClaw外側ループHaiku4.5(安価・速い)、重い処理/L2合成はSonnet4.6という二段モデル運用が稼働(openclaw config・go-live実証)。prompt cachingはbedrock_client/sdk_runnerでcache_read/cache_creationトークン計上ロジックあり。安価→Haiku/重→SonnetのモデルルートはOpenClaw config+L2分担で実現 | ✅ 一致 | openclaw.config.json5:132(Haiku既定/外側)・140(Sonnet重処理); bedrock_client.py:111-112,179-184 cache_read/creationコスト計算; sdk_runner.py:91-99 cache tokens; plans §T1 Sonnet4.6合成実測 | prompt cachingのcacheRetention明示はBedrock公式doc未記載(unverified)としてOpenClaw configから削除(§O)。モデル二段ルート・Sonnet主軸は達成 |
| 16 | SDK Agentループとsearch_pgvector/get_document Toolを定義 | Agentループ(run_sdk_agent)とsearch相当(SearchSkill)はToolSpec/@tool化済。ただしツール名はsearch_pgvector/get_documentという命名ではなくSearchSkill.name等。get_document単独Tool(ドキュメント取得)はSkillとして独立定義されていない可能性(searchが内包) | 🟡 部分 | src/teamagent/orchestrator/sdk_runner.py(loop+build_skill_tools); factory.py build_production_tools(search/clientkarte/proposal); get_document名のSkill無し | Agentループとpgvector検索Toolは達成。疑似コードのsearch_pgvector/get_documentという粒度・命名とは差異(SearchSkillに統合)。本番L2未配線 |
| 17 | スレッド単位Memoryで前文脈応答(Phase2 DoD) | 本番はOpenClaw外殻が会話記憶(state)を持つ設計だが、go-live時にFargate readonly rootfs撤去でstateはephemeral層=会話メモリ揮発が既知P1制限として残る(EFS永続化はP2)。teamagent側skill/runtimeに独立したスレッドMemory実装は確認できず(operation_logはスレッド会話取得用でMemoryではない) | 🟡 部分 | plans/mossy-snacking-locket.md:20「会話メモリ揮発は既知P1制限どおり」「EFS access pointはP2」; slack_bot.pyのthread_tsはoplog用途; OpenClaw会話記憶=SQLite設計(docs/next:168)も揮発 | OpenClawに会話記憶機構はあるが本番では揮発中(永続化P2)。スレッド単位Memoryの安定動作はDoD未達 |
| 18 | 過去5ターン参照の文脈保持(S6) | 過去5ターン参照の文脈保持実装は確認できず。S5-06のMemory自体が本番揮発状態のため、その改善版である5ターン参照は未着手 | 🔴 未実装 | grep「過去5ターン/conversation_history」→該当実装なし; S5-06 Memoryが揮発(plans:20) | 前提のスレッドMemoryが本番揮発のため5ターン参照は着手なし |
| 19 | P1でmanifest駆動L1規約ルータをshadow並走・一致率ログ・gold set50検証 | manifest(CapabilityManifest)駆動のL1規約ルータは新設されていない。本番ルーティングはOpenClaw外殻のHaiku LLM loop。旧Bot側は依然intent.detect_skill(手書き正規表現)。triggers DSL/manifest単一真実源は要件書の計画段階 | 🔴 未実装 | src/にCapabilityManifest/manifest triggers実装なし(grep該当なし); intent.py:285-366 detect_skill手書きのまま; docs/next:189 P1=shadow並走は計画行 | L1規約ルータ(manifest駆動)はP1計画のまま未着手。本番はOpenClaw LLM loopが代替し、shadow並走/gold set50検証も未実施 |
| 20 | P2でintent.py 366行を削除、手書きregex 0行 | intent.pyは依然366行で存在(wc -l実測=366・要件書記載の行数と完全一致)。削除されていない。手書き正規表現(_REVIEW_RE/_DRAFT_RE/_KARTE_TRIGGER等多数)が現役 | 🔴 未実装 | wc -l src/teamagent/skills/intent.py = 366(要件書「366行」と一致); intent.py全体が正規表現ヒューリスティック; docs/next:190 P2=intent.py削除は計画行 | P2未着手。intent.py 366行は丸ごと残存。OpenClaw本番化後も旧Bot god-file解体は未開始 |
| 21 | P2でdetect_skill 4回→1回 | slack_bot.py内でdetect_skillが複数箇所(461・1094・1421・1546行)から呼ばれる構造が残存。1回に集約されていない | 🔴 未実装 | grep detect_skill src/teamagent/runtime/slack_bot.py → 4箇所(intent.detect_skill import+呼出 line461,1094,1421,1546); docs/next:190 P2=4回→1回は計画行 | P2未着手。複数回再評価のまま |
| 22 | P5でL2 SDK orchestratorを本配線(USE_AGENT_ORCHESTRATOR)、run_agent MCP toolで委譲、manifest自動@tool化 | sdk_runner(L2)はコード完成・6-bis合格だが本番未配線。USE_AGENT_ORCHESTRATORフラグはコードに存在せず要件書記述のみ。run_agent MCP toolは未定義(MCP toolFilterはsearch/clientkarte/proposal/tiktok/video等の個別read系のみ・run_agent無し)。manifest駆動の自動@tool化も未(factory.build_production_toolsは手書きToolSpec列挙)。run_sdk_agent呼出はscriptのみ | 🟡 部分 | sdk_runner.py(L2実体・6-bis); grep USE_AGENT_ORCHESTRATOR→コード無し・docs/next:193,207のみ; openclaw.config.json5:108-115 toolFilterにrun_agent無し; factory.py手書きToolSpec; run_sdk_agent呼出=scripts/run_orchestrator_prod.py等のみ | L2コードは存在も本番への本配線(run_agent MCP tool・USE_AGENT_ORCHESTRATOR・manifest自動@tool化)はP5計画のまま未実装。runtime→orchestrator参照ゼロ問題は未解消 |
| 23 | P5でマルチステップ横断をL2委譲で解放 | 単一L2 run_agent呼び出しでのVSEO→提案→PPTX自動連鎖は本番未解放(run_agent MCP tool未定義・L2未配線)。各スキルは個別には存在するがOpenClawから個別tool直叩き(L1)であり、横断はユーザー/外殻の複数指示頼み。ProposalDeckSkillはfactory未配線 | 🔴 未実装 | run_agent MCP tool無し(openclaw toolFilter); 現状ダイジェスト「ProposalDeckSkill未配線」「AI駆動フル提案5段は設計段階(plans/html-agent-floofy-fountain.md)」; docs/next:193 P5計画行 | マルチステップL2委譲はP5計画のまま未解放。提案フル自動生成は設計段階(floofy-fountain.md) |
| 24 | chitchat軽量応答(0コスト即応)、高速路保持か否かは§9-8でP1実測確定 | chitchat skill+prompt(prompts/chitchat/v1/system.md)とintent._is_chitchat(task-first雑談判定:能力質問/挨拶/お礼・業務語あれば検索へ)を実装。旧Bot経路では機能。ただし本番OpenClaw前面化では全リクエストがHaiku loopを通るため『LLM非介在の0コスト即応』は薄まる(§6.3で予告された通り)。去就のP1実測確定はパイロット段階で未完 | 🟡 部分 | src/teamagent/skills/chitchat/ + prompts/chitchat/v1/system.md; intent.py:272-282 _is_chitchat(task-first); docs/next:148「0コスト即応はOpenClaw前面化で薄まる・§9でP1実測決定」 | chitchat機能自体は実装も、OpenClaw経路では0コスト即応の前提が変化。高速路の去就はP1パイロット実測待ち(未確定) |
| 25 | streamable-http接続・OpenClawがread系直叩き(L1)・run_agentでL2委譲 | streamable-http接続は本番達成(run_mcp_http_server.py・bearer認証・teamagent-mcp.teamagent.internal:8787/mcp・SG隔離・go-live実証)。OpenClawが個別read系tool直叩き(L1)も達成(toolFilter include)。しかしrun_agent MCP tool(L2委譲の入口)は未定義=マルチステップ委譲の口は未実装 | 🟡 部分 | scripts/run_mcp_http_server.py(streamable-http+bearer); openclaw.config.json5:99-100 transport streamable-http・:108-115 read系include; plans §T2-③ SG隔離実証; run_agent tool無し | 接続方式(streamable-http)とL1直叩きは本番実証済。run_agentによるL2委譲口のみ未実装 |
| 26 | §9-2で方針確認待ち(明示マルチステップ時のみL2、純化案不採用想定) | USE_AGENT_ORCHESTRATORフラグ未実装でL2本番起動条件は未確定。皮肉にも本番OpenClaw外殻は『全リクエストをHaiku LLMループに通す』形で稼働中=要件書が不採用想定とした純化案に近い構成が現に動いている(ただしL2はSDKでなくOpenClaw loop)。§9-2の方針(単発L1温存/L2明示起動)は意思決定・実装とも未着手 | 🔴 未実装 | grep USE_AGENT_ORCHESTRATOR→docs/next:207(検討事項)のみ・コード無し; 本番=OpenClaw全req Haiku loop(openclaw.config.json5:28,132); docs/next:207「全reqをLLMループに通す純化案は不採用想定(方針確認待ち)」 | L2起動条件フラグ・単発L1温存の方針は未決・未実装。実態は外殻LLM loop全通過で、要件書の懸念した方向に既に倒れている(要方針再確認) |

## その他（20項目）

| # | 計画(仕様書) | 現状(実装) | 判定 | 根拠 | 残差分 |
|---:|---|---|---|---|---|
| 1 | Sprint 5(計画上7/11〜7/24)末に振り返り会を開きベータ継続のGo/No-Go判断、FB起因バグの即時修正枠を確保(master_todo S5-11) | S5は今日(6/15)時点で未到来(計画開始7/11)。形式上の『S5末振り返り会』は未実施。ただし実態として別軸のGo/No-Goゲート①(OpenClaw採否=B案)は通過し、2026-06-12に本番E2E実返信成功(go-live)。FB起因の即時修正は移行作業のローカル検証ループ(validate→起動→agent --local)として運用中だが、Sprint5の枠としては未着手 | 🔴 未実装 | docs/v3.2/teamagent_master_todo_v1.md:236 (S5-11 7/11〜7/24)。currentDate=2026-06-15。go-live=mossy-snacking-locket.md L12/migration memory | Sprintスケジュール(S5=7/11〜)が未到来のため、当該の管理イベントとしては未実施。実装の進捗はSprint計画より前倒しだが、計画文の『S5末振り返り会』という儀式は未開催 |
| 2 | S6(7/25〜8/7)で全社展開お知らせ草稿+経営承認、16名分のSlack allowlist拡張、M3末8/7にMVA完成判定/KPIレビュー(master_todo S6-02ほか) | S6は未到来。Slack allowlistは現状『DM dmPolicy:allowlist＋allowFrom=[U09CX1CCBLN](=本人1名のみ)』で16名拡張は未実施。経営承認/お知らせ草稿/完成判定会は記録なし | 🔴 未実装 | docs/v3.2/teamagent_master_todo_v1.md:238-243 (S6 7/25〜8/7)。infra/openclaw/openclaw.config.json5:56-57 allowFrom 1名。mossy-snacking-locket.md L30 | allowFromは1名のみ(infra/openclaw/openclaw.config.json5:56-57)。16名展開は未着手。MVA完成判定会・経営承認も未実施。Sprintスケジュール未到来 |
| 3 | S14(11/28〜12/28)末=2026-12-28に本番リリース(営業16名展開)+経営報告、継続改善ルーチン確立(master_todo 14.11/14.12、本番運用ターゲット) | 本番E2E稼働(OpenClaw go-live)は2026-06-12に達成済=計画より大幅前倒しでパイロット前段階に到達。ただし『営業16名展開』『経営報告』『週次KPI/月次retro』は未実施。現状はパイロット参加者1名・1週間パイロット未実施 | 🟢 前倒し | docs/v3.2/teamagent_master_todo_v1.md:330,344-345 (S14)。go-live 2026-06-12 = mossy-snacking-locket.md L12。残=§T1✅/T2-②・T3・T4 (L28) | 本番E2E稼働は前倒し達成だが、16名展開+経営報告+継続改善ルーチンは未着手。露出トークン3本rotation(§T4)・2人混線テスト・1週間パイロットが残り。正式リリース目標12/28は未到来 |
| 4 | 5/23に質問リスト(teamagent_subsidiary_questions_v2.md)をメール送付、6/5まで返信なければD案デフォルト。Sprint1終了基準④ | 質問リスト文書は存在(docs/v3.1/teamagent_subsidiary_questions_v2.md)。master_todoに『5/22メール送信済・返信待ち』と記録。ただし最終決定はヒアリング結果ではなく、§Dトポロジ決定+PoC+go-liveでOpenClaw採用を直接確定したため、このヒアリング軸は意思決定の主経路ではなくなった | 🔄 方式変更 | docs/v3.1/teamagent_subsidiary_questions_v2.md 実在。master_todo:116『5/22メール送信済・待ち』。採用は§D/go-live経路=mossy-snacking-locket.md L531/1044 | 質問送付は完了記録あり。子会社返信の受領・反映の証跡はrepoに無い。OpenClaw採否は別経路(PoC/§D/go-live)で確定したため、この質問駆動の判断フローは事実上バイパスされた |
| 5 | S2 W2(6/6〜6/12)でB案(FastAPIラップ)とD案のコスト比較、子会社120ユーザー運用回答(運用時期/期間/トラブル/ロールバック)を整理(implementation_plan v3.2) | v3.2計画のB/D案二択(FastAPIラップ vs 自前Bot維持)という枠自体が、実際にはOpenClaw外殻⇔streamable-http MCP⇔金庫内Skillという第3の確定アーキ(§D)に置換された。FastAPIラップでの見積もりは行われず、OpenClaw(Fargate)前面で実装・go-liveまで到達。120ユーザー回答整理の証跡なし | 🔄 方式変更 | implementation_plan_v3.2:124『B案=FastAPIラップ』。実アーキ=mossy-snacking-locket.md L1044『MCPトランスポート=streamable-http採用確定』。infra/openclaw/ Fargate構成 | 『FastAPIラップ』という想定実装は採用されず(MCPはstreamable-http entrypointを採用)。コスト比較表の成果物はrepoに見当たらない。120ユーザー回答整理も未確認 |
| 6 | v3.0要件のMUST。短期Redis/中期pgvector(project)/長期PostgreSQL(user profile)の3層メモリを自前実装 | 自前の3層メモリは未実装。Redis依存ゼロ(repo全体grep該当なし)、project単位メモリ/user_profileテーブルなし。会話継続はOpenClawのagent loopネイティブ機構(session:{dmScope:'per-channel-peer'})で代替=セッション内文脈保持のみ。中期=会社共有のpgvector(205doc)は検索ナレッジであり計画の『project単位メモリ蓄積』とは別物 | 🔴 未実装 | grep redis=0件(repo全体)。memory_store/user_profile/long_term テーブルなし。infra/openclaw/openclaw.config.json5:64 session:{dmScope:'per-channel-peer'}のみ | Redis短期/project中期/user長期の3層構造は一切実装されていない。OpenClawのセッション機構は短期会話継続のみで、TTL24h・直近20ターン・project経緯蓄積・ユーザープロフィールの永続化は無い |
| 7 | v3.0 MUST。セッションリセットの3条件と、長期メモリPIIの暗号化保存 | 明示コマンド/未活動7日/案件クローズ宣言によるSessionリセットは未実装(該当コード/設定なし)。長期メモリPII暗号化も対象の長期メモリ自体が存在しないため未実装。なお隣接機能としてOAuth refresh tokenのat-rest暗号化(KmsCipher/AWS KMS)は実装済だが、これは計画の『長期メモリのPII暗号化』とは別物 | 🔴 未実装 | grep session reset/案件クローズ/未活動7日=0件。oauth_token_store.py:84 KmsCipher(OAuthトークン用・長期メモリ用ではない) | Sessionリセット3条件は完全未実装。長期メモリが存在しないためPII暗号化も対象なし。KmsCipherはOAuthトークン保護用で代替にはならない |
| 8 | v3.0 SHOULD(圧縮/開示)/MAY(チーム)。中期メモリ圧縮、長期メモリ開示UI、チーム単位メモリ | いずれも未実装。前提となる中期/長期メモリ層自体が存在しないため、圧縮アーカイブ・開示UI・チーム単位メモリすべて着手なし | 🔴 未実装 | grep 圧縮/開示UI/team memory=該当なし。3層メモリ基盤が不在(SR-1,2,3参照) | SHOULD/MAY項目だが、基盤の3層メモリが無いため派生機能もゼロ |
| 9 | v3.0 SHOULD。X(Twitter)をオンデマンド検索補強に使い、スクレイプは公式API経由必須 | X/Twitter連携は一切実装されていない(twitter/tweepy/x_search 該当コードゼロ、要件文書にも実装記述なし)。外部トレンド探索はTikTok(Puppeteer)とYouTube/scrape系で構成され、Xは対象外のまま | 🔴 未実装 | grep twitter/tweepy/x_search=0件(repo全体・docs含む)。Trend&Market Discoveryドメイン=tiktok_searchのみ(docs/next/teamagent_next_requirements.md:59) | X連携は完全に未着手。SHOULD止まりで優先度が下がり、Trend&Market DiscoveryドメインはTikTokに集約された |
| 10 | v3.0 Phase5 DoD。Personal AI Coach Phase1を実装、営業100人規模へ拡張(別冊要件v3.0) | Personal AI Coachは要件文書(teamagent_personal_ai_coach_requirements_v3.0.html)のみ存在し、実装コードは皆無。src/skills/README.mdに『Personal AI Coach Skill(機能⑥、Phase5)』と将来予定として記載があるのみ | 🔴 未実装 | docs/v3.0/personal_ai_coach_requirements_v3.0.html(要件のみ)。src/skills/README.md:22『機能⑥、Phase5』。coach実装コードのgrep該当0件 | 実装コードなし。要件ドキュメント段階。Phase5(S16)はロードマップ最終段で未到来 |
| 11 | S5 DoD=5名が1日1回以上利用/FB≥20件/満足度≥3.5、FB収集フォーム+計測整備(mva_spec) | 5名ベータ選定/オンボーディング/FBフォーム/GA・reaction計測はいずれも未実施。現状の本番アクセスはDM allowFromに1名(本人)のみ。OpenClawには受付リアクション(ackReaction:'eyes')があるがFB計測機構ではない | 🔴 未実装 | infra/openclaw/openclaw.config.json5:57 allowFrom 1名。FBフォーム/GA計測のコード該当なし。mva_spec S5 DoD | ベータ5名の選定・FB収集基盤・計測指標いずれも未着手。Sprintスケジュール(S5=7/11〜)未到来かつ参加者1名 |
| 12 | S6 DoDでMVAリリースタグを切り、16名キックオフ会開催可能状態+オンボーディング資料整備(mva_spec) | MVAリリースタグ未作成(repoのgit tag確認範囲で該当なし、branchはdev運用)。16名キックオフ会・オンボーディング資料も未整備。なお『@AiLa 連携して』オンボーディングフロー設計(§Q-Onboard-IMPL)は計画書に存在するが、16名展開向けの正式資料・キックオフ準備は未完 | 🔴 未実装 | branch=dev運用(git log)。§Q-Connect『部長配布は会社正式エンドポイント確定後』=mossy-snacking-locket.md L27。MVAタグ該当なし | リリースタグ・キックオフ準備・オンボ資料いずれも未完。enrollment公開URLは会社正式エンドポイント(connect.vectorinc.co.jp)待ちで部長配布は保留中 |
| 13 | 未決12論点に各判定タイミングで暫定決定を出す(mva_spec 8.2) | 主要論点の多くは別経路で実質決着済:Embeddingモデル=LocalE5焼込み、取込方式/インフラ場所=RDS pgvector(東京)+Fargate金庫、他人データ参照=署名済みSlack caller＋resolver成功を必須にした会社共有RLS、外部スクレイプ=tiktok/video allowlist+url_guard、ClawHub流用=導入禁止で決定、OAuthスコープ=gmail.modify/per-user。ただし12論点を一覧で『暫定決定台帳』として明示管理した形跡は薄く、決定は各実装/§D/security_audit等に分散 | 🟡 部分 | ClawHub無効化=overview_v3.2:351。識別=mcp_gateway/server.py＋caller_claim.py。Embedder=factory.py LocalE5Embedder。url_guard=e82def7。決定の分散管理 | 個別論点は実装決定で潰れているが、12論点を統合した決定一覧の維持は確認できない。政策変動代替経路(R19類)は監査で対応も台帳化は弱い |
| 14 | R16-R19をリスク台帳化し、Sprintレトロでステータス更新(mva_spec 12.6 v1.1新規) | R16-R19という番号付き台帳としてSprintレトロで更新する運用は確認できない。ただし対応実態はある:R16=CVE週次watch+min-safe版pin+CVE-2026-52869/52870非該当実証(security_audit)、R17=ClawHub完全無効化+スキル導入禁止、R18/R19=移行メモリに『OpenClaw advisory週次watch・全トークン一斉失効runbook』として教訓化。番号体系での台帳追跡ではなく、監査報告書+メモリに分散して対応 | 🟡 部分 | docs/security/security_audit_2026-06-12.md:56-58(CVE対応)。overview_v3.2:351(ClawHub無効化)。mossy memory L24『advisory週次watch・ClawHub禁止・一斉失効runbook』 | R16-R19のID付きリスク台帳+Sprintレトロ更新サイクルという形式は未確立。実質的なリスク対応(CVE/ClawHub/トークン失効)は実施されている |
| 15 | 既存clientkarteをDeal Intelligenceドメインにマップ(next_requirements §4-2) | clientkarteは実装済(src/teamagent/skills/clientkarte/ schema.py+skill.py)で、提案履歴・温度感推移・推奨ネクストアクションをpgvector+Bedrockで1枚に合成。さらにMCP gatewayのbuild_production_tools()に配線済かつOpenClaw toolFilterのincludeに'clientkarte'があり、本番OpenClaw経由で呼べる状態。ただし7ドメイン再編という構造リファクタ自体は未実施(現状フラットなskills/配置) | ✅ 一致 | src/teamagent/skills/clientkarte/skill.py(ClientKarteSkill)。factory.py:86 ToolSpec配線。openclaw.config.json5:110 toolFilter include 'clientkarte' | スキル単体は本番配線まで到達。ただし計画の『1ドメイン=1パッケージ=1 manifest(CapabilityManifest)』へのドメイン束ね直しは未着手で、ディレクトリは旧フラット構造のまま |
| 16 | 既存operation_logをDeal Intelligenceにマップ、RLS担当者スコープ一貫適用(next_requirements §4-2) | operation_logスキルは実装済(src/teamagent/skills/operation_log/ skill.py+schema.py=Slackスレッド読取→deal_phase/action/next_step/BANT構造化)。ただしbuild_production_tools()に未配線かつOpenClaw toolFilter includeにも非掲載=現在OpenClaw経由では呼べない(到達不能)。RLSは検索系で一貫適用されているがoperation_log経路はMCP未公開 | 🟡 部分 | src/teamagent/skills/operation_log/skill.py 実在(@register)。factory.py build_production_tools()にoperation_log無し(search/clientkarte/proposal系/video/tiktokのみ)。openclaw.config.json5:110-111 includeに非掲載 | スキルコードは存在するがMCP production tools未配線・toolFilter非掲載で本番OpenClawから到達不能。旧Slack Bot経路の遺産で、OpenClaw移行後の再公開がされていない |
| 17 | 将来計画(未着手)。deal_radar/morning_brief/定点モニタ(next_requirements §3/§4-2) | 実スキルは未実装。ただしinfra/openclaw/HEARTBEAT.mdにmorning_brief(1d)/deal_radar(4h)/competitor_trend(1d)のプロアクティブ・タスクとしてYAMLテンプレ化済=P2/WS-Dで解禁予定の設計テンプレ。P1ではheartbeat無効(every:0m)で稼働せず | 🟡 部分 | infra/openclaw/HEARTBEAT.md:11-24(morning_brief/deal_radar/competitor_trendテンプレ・P1無効)。docs/next/teamagent_next_requirements.md:80(将来) | 将来計画通り未着手だが、OpenClaw heartbeat機構を使う設計テンプレが先行整備された点は計画の具体化。実稼働はP2待ちでコードハンドラ無し |
| 18 | 既存tiktok_searchをTrend&Market Discoveryにマップ(next_requirements §4-5) | tiktok_searchは実装済(keyword/hashtag検索、空振り時keywordフォールバック)かつOpenClaw経由で本番稼働実証済。2026-06-12『@AiLa TikTokで会社員あるある検索』→Puppeteer実スクレイプ10本(16s)→Gemini分析→Slack整形のチェーン全通・エラー0。USE_TIKTOK_TOOLS=1ゲートでMCP公開、toolFilter includeに'tiktok_search'掲載。実行は金庫内Puppeteer | ✅ 一致 | src/teamagent/skills/tiktok_search/skill.py。factory.py:147-150 USE_TIKTOK_TOOLS。E2E実証=mossy-snacking-locket.md L26(027e763/Vertex)。toolFilter include 'tiktok_search' | 本番E2E実証済=計画より前倒し。7ドメイン構造リファクタは未実施だが機能配線は完了 |
| 19 | 上位動画メタ+Gemini横断分析、triggers/cost_class=highを宣言(next_requirements §4-5) | 上位動画メタ取得+Gemini横断分析は実装・本番稼働済(tiktok_search skill+gemini_client、Vertex経由$0.0008/18s実測)。一方trigger宣言とcost_class=highの宣言は未実装:tiktok_searchスキルにcost_classフィールドは無く(あるのはruntimeのtotal_cost_usd)、triggers/cost_classはnext_requirementsが提案する将来のCapabilityManifestアーキの一部で、コードには未導入。意図判定は旧intent.py手書き分岐+OpenClaw agent loopが担う | 🟡 部分 | 分析稼働=mossy L26(Vertex $0.0008)。tiktok_search/schema.py:61 total_cost_usd(cost_classフィールド無し)。cost_class宣言はdocs/next §4-5:100/§9のCapabilityManifest設計のみ(grep cost_classはdocs限定) | 動画メタ+Gemini分析の本体は稼働。ただし宣言レジストリ(CapabilityManifestのtriggers/cost_class)は設計段階でコード未実装。cost_classはruntimeコスト集計(total_cost_usd)で代替されている |
| 20 | 将来計画(未着手)。Reel/Shorts拡張・定点モニタ(next_requirements §3/§4-5) | Reel/Shorts専用拡張・定点モニタは未実装。ただしvideo_algorithm/video系スキルはYouTube/Shorts/TikTok/IGを横断対象として扱う設計で、Shortsメタ分析の一部は既存動画パイプラインでカバー。定点モニタはHEARTBEAT.mdのcompetitor_trend(1d)テンプレに将来枠として記載があるのみ(P1無効) | 🔴 未実装 | docs/next/teamagent_next_requirements.md:59(将来Reel/Shorts拡張・定点モニタ)。infra/openclaw/HEARTBEAT.md:21 competitor_trend(P1無効テンプレ) | 将来計画通り未着手。Reel/Shorts専用機能・定点モニタの実稼働コードは無い |
