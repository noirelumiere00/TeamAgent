# This Compose deployment path is intentionally decommissioned.
#
# The release runtime is valid only when it is built and verified by
# infra/openclaw/build-image.sh from an exact clean Git HEAD. That helper binds
# source provenance, produces a linux/arm64 image, exercises the hardened
# runtime contract, generates a Trivy SBOM, and gates Critical/High
# vulnerabilities and embedded secrets before any optional push.
#
# Do not add an upstream OpenClaw image, a local `build:`, or any publication
# path here. Production deployment consumes the verified digest recorded by
# the helper's manifest through the ECS runbook.
name: teamagent-openclaw-decommissioned
services: {}
