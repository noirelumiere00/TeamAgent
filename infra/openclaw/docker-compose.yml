# This Compose deployment path is intentionally decommissioned.
#
# This empty file is retained only to make the decommissioning explicit; it is
# not a deployment or publication interface. infra/openclaw/build-image.sh is
# a local-validation-only helper. It produces an unsigned local image and
# evidence from an exact clean Git HEAD, never pushes, and never obtains or
# represents production publication credentials.
#
# Do not add an upstream image, local `build:`, registry login/push, ECS
# consumer, or other release path here. Production may consume only a release
# repository digest produced by the trusted source-free promoter and bound by
# a valid signed active receipt through the canonical one-time saved-plan flow.
name: teamagent-openclaw-decommissioned
services: {}
